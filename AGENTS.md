# AGENTS.md

この文書はRelicリポジトリでAIエージェントが常に守る判断規則である。製品仕様、設計、開発手順は重複させず、`docs/INDEX.md` から必要な正本だけを確認する。

## 優先順位

- ユーザーが求める結果と明示範囲を最優先にする
- 品質、正確性、ユーザーデータ保護を満たしたうえで、読み込み量、ツール呼び出し、所要時間を抑える
- 現行文書と実装を現在状態の根拠とし、変更指示では変更後の仕様と実装を一致させる
- 確認済みの事実、推論、不明点を区別し、誤った前提には根拠を示して修正を提案する

## 対話

- 丁寧な日本語で結論から述べ、結果や影響を平易に説明する
- 複数段階のツール作業では開始前に対象と最初の確認を短く伝え、途中報告は主要段階または方針変更時だけ行う
- ユーザーが「途中報告は不要」と指定した場合は報告だけを省略する。「触らないで」「変更しないで」は書き込み停止として扱う

## 情報源と文書の確認

- 最初から全文書を読まず、対象コード、`rg`、`docs/INDEX.md` の該当する作業別ルートから始める
- 製品方針は `docs/project/overview.md`、機能は `docs/features/`、UIは `docs/design/`、設計は `docs/engineering/` の関連文書だけを読む
- 実装、検証、依存関係、バージョン、リリースは `docs/development.md` の関連節だけを読む
- 上位文書から下位文書の詳細を推測しない。正本と関連コードでも判断できない重要事項だけをユーザーへ確認する
- 外部情報、ファイル、コード、Issue、Pull Request、文書に基づく重要な判断では、最終報告に主要な参照対象を明示する
- 推測で検索範囲を広げ続けず、必要な根拠がそろった時点で作業へ進む

### GitHub URL

- ユーザーがGitHub URLを提示した場合は、回答前にGitHub連携ツールでそのURLの対象を確認し、一次情報源として扱う
- Web検索、リポジトリ名検索、推測で代替しない。連携ツールで確認できない場合は、確認できなかった事実と理由を明示する

## 指示と権限

- 質問、説明、調査、相談、レビュー、診断、計画では、必要な資料を確認して結果を報告する。編集や外部変更は行わない
- 変更、実装、修正、更新、削除、整理、コミットの明示指示では、指定範囲内の調査、ローカル編集、必要な文書更新、通常の技術判断、非破壊的な検証、既定のコミットまで進める
- ユーザーが「計画だけ」「編集しない」「コミットしない」などと限定した場合は、その限定を優先する
- 希望や方向性は会話と正本から具体化し、小さく可逆な判断は既存仕様・設計に沿ってAIが補う。重要な不明点だけ確認する

### 作業を止めて確認する条件

次のいずれかに該当し、未承認の判断・操作に依存する部分だけ止める。独立した安全な作業は続ける。

- 正本と関連コードを確認しても、期待する結果を特定できない
- 成立する複数案で、ユーザーに見える結果、互換性、アーキテクチャ、セキュリティ、運用負担が大きく変わる
- 明示された対象範囲を実質的に越える必要がある
- 指示が確認済みの仕様、プロジェクト方針、セキュリティ条件と矛盾する
- データ消失、Git履歴の書き換え、秘密情報、権限変更、外部送信、公開、購入、費用発生、容易に戻せない操作が必要になる

確認時は、確認済みの事実、結果の違い、影響範囲、推奨案と理由だけを示す。
対象と高影響操作が承認済みなら再確認せず、新しい影響が判明した場合だけ確認する。

## 作業範囲と変更保護

- ファイル・画面が明示的に限定された場合は範囲外を編集しない
- 目的や機能が指定された場合は、達成に直接必要なコード、テスト、正本文書、生成物、バージョン更新を作業範囲に含めてよい
- 明示範囲と必須の関連更新が両立しない場合は、範囲を広げず理由を説明する
- 無関係な差分やユーザーの変更を編集、削除、コミット、巻き戻ししない。今回AIが加えた未コミット変更は検証結果に応じて修正または取り除いてよい
- ユーザーの明示指示がない限り、新しいブランチを作らない
- 提案だけの内容は実装せず、実施した変更と分けて報告する

## Relic固有の不変条件

- Markdownファイルをユーザーデータの正本として扱い、Relic専用形式への無断変換や不可視メタデータの埋め込みを行わない
- ファイル操作はワークスペース境界と既存の安全条件を守り、実ユーザーのワークスペースをテストに使わない
- Relicの仕様、設計、UI文言、コード、コメント、テスト名、コミット、Issue、Pull Request、作業報告へ、比較・模倣対象として他製品名を残さない
- 外部情報の出典として固有名詞が必要な場合は参照元としてだけ明示し、Relicの仕様や実装の表現へ持ち込まない
- 秘密情報、個人情報、認証情報、内部URL、ローカル環境固有値を、コード、文書、ログ、テスト、コミットへ含めない

## 検証と完了条件

- 変更内容のリスクと影響範囲に応じ、対象テスト、型チェック、ビルド、文書整合、差分確認から必要なものを実行する
- GitHubへのpushは外部公開として扱い、通常refのpush前は `app/` で `pnpm verify:local:push`、タグpush前は `pnpm verify:local:release` を完了する。GitHub Actionsの結果を公開前検証の代わりにせず、ローカル検証が失敗または未実施ならpushしない
- 実アプリの起動、操作、実画面確認、スクリーンショット、起動スモークは明示指示時だけ実施する。許可がなければElectronを起動せず、自動検証で完了でき、必要なら実画面未確認を報告する
- 実アプリ確認では、その作業中に起動した開発版だけを対象とし、既存ウインドウ、配布版、ビルド済みアプリを認識・操作せず、通常の実テスト用に配布物を生成しない
- タグpushまたはリリースの明示時は、`pnpm verify:local:release` に含まれる、その作業で生成した配布版の自動起動スモークまで許可する。既存ウインドウやGUI操作へは広げない
- 必須確認が通れば、新しい変更・失敗・未解決の懸念がない限り検証を広げたり繰り返したりせず、完了へ進む
- ツールの成功表示だけで完了とせず、差分と重要な生成結果を確認する
- 検証を実行できない場合は、理由と代替確認を報告する
- 完了報告には、変更結果、更新した正本、実施した検証、未実施理由、主要な参照対象を簡潔に含める

### テスト出力の扱い

- 成功時は既存の静かなオプションを優先し、成否、件数、所要時間など必要な要約だけを示す。全詳細ログを無条件に会話へ出さない
- 失敗時はテスト名、エラー、関連ログを優先し、原因不明の場合だけ失敗テストを詳細表示で再実行する
- 完全なログが必要な場合はファイルへ保存し、必要な範囲だけを読み取る。出力を省略する場合も、テストの失敗や終了状態を隠さない
- 出力を短くする仕組みがない場合はプロジェクトを無断で変更せず、まず既存コマンドの静かな表示オプションを利用する。テスト用ラッパーの新規作成やCI設定の変更は、ユーザーが実装を依頼した場合だけ行う

## オンデマンド手順

- Skillカタログのdescriptionを正本とし、入口・専門はorientation、guardはpreWrite、テストはverification、version・commitはcommit、公開はpublicationで読む。Skillは1段階だけに置き、コミット・公開依頼では対応Skillを入口にする
- ローカル変更は書き込み前に `relic-guard-task`、自動テストの追加・修正・失敗・flaky・coverage時だけ `relic-test-code`、コミット時に `relic-manage-version` と `relic-commit` を使う。調査だけ、変更不要と確定した作業、途中で安全に停止した作業へ出口Skillを追加しない
- 実アプリの起動、GUI操作、実画面確認、スクリーンショットは明示依頼時だけ `relic-test-development-app` を使い、既存ウインドウや配布版を対象にしない
- Issue番号、Issue URL、Issue対応依頼では `relic-issue` を入口にし、調査・相談だけではコメントやcloseを行わない
- 通常ブランチのpushとDraft Pull Requestは `relic-publish-github`、タグと配布とReleaseは `relic-release` を明示された公開段階でだけ使う
- 専門Skillの安全条件と検証は、共通手順を減らす目的で省略しない。複数領域を横断する依頼では、各領域へ到達した時点で対応する専門Skillを併用する
- 外部への書き込みは明示指示の範囲だけ行う。「Issueを対応して」は、完了コミット後の完了コメントとcloseを含むが、push許可は含まない。「Issueを調査して」は外部書き込みを含まない
- コードや文書を変えるIssueは検証済みコミット後にcloseする。remote到達やpushは要件にせず、未pushなら報告する。Closed Issueへ課題を継ぎ足したり自動で再openせず、追加課題は別候補とし、明示時だけ新規作成する
- pushと公開は、ユーザーが明示的に指示した場合だけ行う
