# 2026-05-02

## やったこと

- `docs/ui/DESIGN.md` を新規作成（デザインシステム：カラーパレット・タイポグラフィ・スペーシング・コンポーネント定義）
- `AI.md` を新規作成し、全ルールを一元管理
- `CLAUDE.md` を書き換え（ルールの記述を禁止し、`AI.md` への参照のみに）
- `AGENTS.md` を新規作成（他のAIエージェント向けエントリーポイント。`AI.md` への参照のみ）
- `docs/README.md` を `docs/INDEX.md` に改名（人間向けREADMEとAI向けインデックスの役割を分離）
- `README.md`（root）をプロジェクト概要として整備
- `docs/INDEX.md` に `DESIGN.md` のエントリーを追加
- `.gitignore` のバグ修正（`.#` → `#`）と項目追加（`.obsidian/`・`out/`・`*.tsbuildinfo`・`coverage/`・`.webpack/`）
- `docs/.obsidian/` を `git-filter-repo` で全履歴から削除
- `git push --force origin main` で反映

## 計画・決定

- すべてのAIエージェントは `CLAUDE.md` または `AGENTS.md` を入口に `AI.md` を読む構造に統一
- ルールの追加・変更は必ず `AI.md` のみを編集する
- セッション開始時に `docs/ui/DESIGN.md` を読む手順を `AI.md` に追加
- `docs/README.md` はAI向け索引として `docs/INDEX.md` に改名。`README.md` は人間向けに使い分け

## 構想・アイデア

- 特になし

## 問題・課題

- 特になし
