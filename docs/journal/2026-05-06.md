# 2026-05-06

## やったこと

フェーズ1〜3の未実装項目をバッチで埋め始めた。今日はバッチ1として3項目を実装した。

### 新規ワークスペース作成フロー（フェーズ1）

- `shared/ipc.ts` に `createNewWorkspaceChannel` と `RelicApi.createNewWorkspace` を追加
- `workspaceHandlers.ts` に `createNewWorkspaceChannel` ハンドラーを追加
  - `dialog.showSaveDialog` でユーザーが保存場所とフォルダ名を指定
  - 指定パスに `mkdir` してワークスペース登録
- `preload.ts` にバインドを追加
- `App.tsx` の `FilesSidebar` に「新規ワークスペースを作成」ボタンを追加
  - `isCreatingWorkspace` state と `handleCreateNewWorkspace` ハンドラーを追加
  - `FilesSidebarProps` に `isCreatingWorkspace` / `onCreateWorkspace` を追加
- `styles.css` に `.secondary-button` スタイルを追加

### タブ0枚時の新規ノート作成画面（フェーズ2）

- `PaneViewProps` に `onCreateNote: (name: string) => void` を追加
- `PaneView` の空状態 UI を改善：「ノートがありません」メッセージ + ノート名入力フォーム + 「新規ノートを作成」ボタン
  - ワークスペースが未設定の場合はフォームを非表示
  - 作成後に自動でファイルを開く `handleCreateNoteFromPane` を実装
- `styles.css` に `.empty-pane-message`・`.empty-pane-form` を追加

### サイドバー可変幅（フェーズ2）

- `App.tsx` に `sidebarWidth` state（デフォルト260px）と resize ハンドラーを追加
  - `useRef` 3本でリサイズ中フラグ・開始 X・開始幅を管理
  - `document` の `mousemove` / `mouseup` を `useEffect` で登録
- `<aside className="sidebar">` に `style={{ width: sidebarWidth }}` を適用
- サイドバー末尾に `.sidebar-resize-handle` を追加（`position: absolute; right: 0; width: 4px`）
- `styles.css` の `.sidebar` から固定 `width: 296px` を削除し、`min-width: 180px` に変更
- `.sidebar-resize-handle` / `.sidebar-resize-handle:hover` を追加

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 21ファイル / 141テスト全通過

## 追記（バッチ2）

### ファイル移動・フォルダ移動（フェーズ1）

- `FileTree` を `FileTreeItem` + `FileTree` に分割
- `FileTreeItem` に `draggable` 属性を追加、`dragstart` で `application/relic-item` データをセット
- フォルダアイテムに `dragover` / `dragleave` / `drop` ハンドラーを追加（ドロップ先ハイライト付き）
- `FileTree`（isRoot）に親フォルダなしドロップ用のルートドロップゾーンを追加
- `handleMoveFile` / `handleMoveFolder` をApp.tsxに追加し、既存の `moveMarkdownFile` / `moveFolder` IPCに接続
- `FilesSidebarProps` に `onMoveFile` / `onMoveFolder` を追加
- CSS: `.file-tree-row.drag-over`・`.file-tree--drag-over` を追加

### ピン留め（フェーズ1）

- `shared/ipc.ts`: `togglePinChannel` チャネルと `WorkspaceState.pinnedPaths: string[]` を追加、`RelicApi.togglePin` を追加
- `AppSettings` に `pinnedPaths: Record<string, string[]>` を追加（ワークスペースID別管理）
- `appSettings.ts` に `parsePinnedPaths()` を追加
- `workspaceService.ts` の `toWorkspaceState` でアクティブワークスペースのピン一覧を返す
- `workspaceHandlers.ts` に `togglePinChannel` ハンドラーを追加（toggle して保存）
- `preload.ts` に `togglePin` を追加
- `FilesSidebar` にピン留めセクションを追加（上部に固定表示）
  - `findNodeByPath` ヘルパーでノードを解決
- `FileTreeItem` にピンボタンを追加（ホバー時表示、固定済みは常時表示）
- CSS: `.file-tree-row-wrap`・`.file-tree-pin-btn`・`.pinned-section`・`.pinned-section-heading` を追加

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 21ファイル / 141テスト全通過

## 追記（バッチ3）

### 左右2ペイン分割の不足補完（フェーズ2）

- `editorStore.ts` の `toggleSplit` を修正
  - 分割解除時に右ペインのタブを左ペインに移動する（以前は単純に破棄していた）
  - 右ペインのアクティブタブが左にない場合は左ペインのアクティブタブになる
  - 右ペインの履歴も左ペインにマージ

### ワークスペース設定ファイル（フェーズ1）

- `app/src/main/settings/workspaceSettings.ts` を新規作成
  - `WorkspaceSettings` インターフェース（`pinnedPaths`, `workspacePath`）
  - `readWorkspaceSettings(userDataPath, workspaceId)` — `userData/workspaces/{id}.json` を読む
  - `writeWorkspaceSettings(userDataPath, workspaceId, settings)` — 同パスに書く
  - ファイルが存在しない場合はデフォルト値を返す
- `AppSettings` から `pinnedPaths` フィールドを削除し、ワークスペース設定に移行
  - `appSettings.ts` から `parsePinnedPaths` 関数を削除
  - `workspaceService.ts` の `toWorkspaceState` の pins 取得を引数ベースに変更
  - `workspaceHandlers.ts` の `buildWorkspaceState` で `readWorkspaceSettings` から pins を取得
  - `togglePinChannel` ハンドラーを `writeWorkspaceSettings` を使うよう更新
- テスト `workspaceSettings.test.ts` を追加（3テスト）

### アプリ再起動後の前回ワークスペース復元（フェーズ1）

- `lastWorkspaceId` を `app-settings.json` に保存し、`getWorkspaceStateChannel` が起動時に読み込む実装は既存で正常に動作していることを確認

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 22ファイル / 144テスト全通過

## 追記（P9-runtime-verification / ファイルツリービュー）

### 実施

- `readWorkspaceFileTree` の実装と既存テストを確認
  - `.md` ファイルとフォルダのみ返す
  - ファイル表示名から `.md` を外す
  - 非Markdownファイルを除外する
- `App.test.tsx` にファイルツリーの追加検証を追加
  - フォルダを開閉できる
  - 開いているファイルに `active` クラスが付く
  - サイドバー幅のドラッグ変更が最小180px・最大500pxに制限される

### 確認

- `pnpm test -- App.test.tsx fileTree` 通過（22ファイル / 152テスト）
- `pnpm exec tsc --noEmit` 通過

### メモ

- P9の確認方針を、実アプリ操作前提ではなく、コード確認・自動テスト・ビルドでもチェック可能な運用に変更。
- `docs/dev/feature-checklist.md` の「4. ファイルツリービュー」は、コード確認と自動テストで確認できたためチェック済みに変更。

## 追記（P9-runtime-verification / ワークスペース管理）

### 実施

- `App.test.tsx` にワークスペース操作ボタンの追加検証を追加
  - 「フォルダを開く」ボタンから `openWorkspace` を呼び、返却されたワークスペース状態を画面へ反映する
  - 「新規ワークスペースを作成」ボタンから `createNewWorkspace` を呼び、返却されたワークスペース状態を画面へ反映する
- 既存実装・既存テストで以下を確認
  - `prepareWorkspace` が `attachments/` と `templates/` を自動作成する
  - `addOrActivateWorkspace` がワークスペースを重複登録せず、`lastWorkspaceId` を更新する
  - `activateWorkspace` が登録済みワークスペースへ切り替える
  - `toWorkspaceState` が保存済み `lastWorkspaceId` からアクティブワークスペースを復元する

### 確認

- `pnpm test -- App.test.tsx workspaceService workspaceSettings` 通過（22ファイル / 154テスト）
- `pnpm exec tsc --noEmit` 通過

### メモ

- `docs/dev/feature-checklist.md` の「2. ワークスペース管理」は、コード確認と自動テストで確認できたためチェック済みに変更。

## 追記（P9-runtime-verification / ファイル・フォルダ操作）

### 実施

- `App.test.tsx` にファイル・フォルダ操作のレンダラー検証を追加
  - RenameBar から `renameMarkdownFile` を呼ぶ
  - コマンドパレットから `duplicateMarkdownFile` を呼ぶ
  - コマンドパレットから確認ダイアログ後に `moveItemToTrash` を呼ぶ
  - ファイル・フォルダのドラッグ&ドロップで `moveMarkdownFile` / `moveFolder` を呼ぶ
  - ピンボタンから `togglePin` を呼び、ピン留めセクション表示と解除操作を確認する
- 既存メインプロセステストで以下を確認
  - Markdownファイル作成、リネーム、複製、移動
  - フォルダ作成、リネーム、移動
  - ゴミ箱移動
  - `[[ファイル名]]` のリンク先不存在時のファイル作成

### 確認

- `pnpm test -- App.test.tsx markdownFiles folders trash` 通過（22ファイル / 159テスト）
- `pnpm exec tsc --noEmit` 通過

### メモ

- React `act(...)` 警告は引き続き出ている。P9安定化メモとして `docs/dev/feature-checklist.md` に未完了項目を追加し、今の機能確認は継続する。
- `docs/dev/feature-checklist.md` の「3. ファイル・フォルダ操作」は、コード確認と自動テストで確認できたためチェック済みに変更。

## 追記（P9-runtime-verification / エディタ編集機能）

### 実施

- `Editor.test.tsx` を追加
  - `EditorView.dispatch` による入力変更が `onChange` に通知される
  - CodeMirror の `undo` / `redo` コマンドが動作する
  - `[[` 入力時のファイル名補完候補を生成できる
  - ライブプレビュー装飾がカーソル行を除外し、カーソル外のMarkdown記法を装飾対象にする
- `buildLivePreviewDecorations` と `buildWikiLinkCompletionSource` をテスト可能にするため named export 化
- jsdom で CodeMirror の測定処理が落ちないよう、`Range#getClientRects` / `getBoundingClientRect` の不足分をテストセットアップに追加
- 既存 `App.test.tsx` でファイルクリックから `readMarkdownFile` を呼び、タブ/エディタへ反映されることを確認済み
- `useAutoSave` の実装で、入力停止後1秒の debounce 後に `writeMarkdownFile({ content, path })` を呼ぶことを確認

### 確認

- `pnpm test -- Editor.test.tsx App.test.tsx editorStore markdownFiles` 通過（23ファイル / 163テスト）
- `pnpm exec tsc --noEmit` 通過

### メモ

- React `act(...)` 警告は引き続き出ている。P9安定化メモで追跡中。
- `docs/dev/feature-checklist.md` の「5. エディタ（編集機能）」は、コード確認と自動テストで確認できたためチェック済みに変更。

## 追記（P9 実機稼働検証）

### ボタン操作が反応しない不具合を修正

- 実機確認中に「フォルダを開く」などのボタンを押しても何も起きない不具合を確認
- 原因は `BrowserWindow` の `preload` パスが実際の Vite 出力先とずれており、`window.relic` が読み込まれていなかったこと
- `app/src/main/main.ts` の preload 指定を `path.join(__dirname, "preload.js")` に修正
- `button`, `input`, `select`, `.workspace` に `-webkit-app-region: no-drag` を追加し、Electron のドラッグ領域が操作を吸わないよう補強
- `pnpm package` で `out/Relic-darwin-arm64/Relic.app` を再生成
- 更新後の `Relic.app` で「フォルダを開く」クリック時に macOS の「開く」ダイアログが表示されることを確認

### P9 の先頭にローカライズ基盤を追加

- `docs/dev/feature-checklist.md` の先頭に「1. ローカライズ基盤」を追加
- アプリのデフォルト表示言語を英語にし、`en.json` / `ja.json` と `t()` 経由でUI文言を管理する方針にした
- 設定画面で `System / English / 日本語` を選択できること、設定保存と再起動後反映、英語・日本語双方の表示確認をP9項目に追加
- 既存の各章番号を1つずつ後ろへずらした

### AI作業ルールを追加

- `AI.md` に「作業区切り時の文書更新確認」を追加
- 実装・検証・修正・調査の完了報告前に、現在フェーズと正本文書を確認し、必要な文書更新を判断するルールを明文化
- P9では、実機確認済み項目は `docs/dev/feature-checklist.md` を更新し、不具合修正のみで未確認の場合はチェックを付けず理由を書く運用にした

### 確認

- `pnpm test -- workspaceService App.test.tsx` 通過（22ファイル / 145テスト全通過）
- `pnpm package` 通過

## 追記（P9 ローカライズ基盤 実装）

### 実装

- `EditorSettings` に `language: "system" | "en" | "ja"` を追加し、設定保存・読み込み対象に含めた
- `app/src/renderer/i18n.tsx` を追加し、`System` 選択時はブラウザ言語が日本語なら `ja`、それ以外は `en` を使う解決ロジックを実装
- `app/src/renderer/locales/en.json` / `ja.json` を追加し、主要UI文言を辞書管理へ移行
- 設定画面に `Language` セレクトを追加し、`System / English / 日本語` を選べるようにした
- サイドバー、ペイン、検索、Git、ツール、コマンドパレット、クイックスイッチャー、ツールバー、フロントマターの主要固定文言を `t()` 経由へ移行
- アプリの初期表示は英語を基準にし、テストの期待値も英語表示へ更新

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test -- App.test.tsx FrontmatterForm.test.tsx workspaceService workspaceSettings` 通過（22ファイル / 145テスト全通過）
- `docs/dev/feature-checklist.md` は実機確認後にチェックする運用のため、今回はチェック未更新

## 追記（P9 ローカライズ基盤 実機確認）

### 追加修正

- 未設定時の表示言語を `en` に変更し、アプリのデフォルト表示を英語に統一
- `System` は設定画面の選択肢として残し、OS言語追従に使用
- renderer の主要コンポーネントに残っていた固定日本語文言を追加で翻訳キーへ移行
  - ピン留めツールチップ、検索パターン、ツール入力例、Git remote 接続メッセージ、Rename/Move の補助文言、ツールバー挿入テンプレート
- 英語表示で設定セレクトの値が切れないよう、設定フォームの select 幅を調整

### 実機確認

- `pnpm package` で `out/Relic-darwin-arm64/Relic.app` を再生成
- 生成した `Relic.app` で設定画面に `Language` が表示されることを確認
- `System` → 日本語表示、`English` → 英語表示、`日本語` → 日本語表示へ切り替わることを確認
- `English` 選択後にアプリを再起動し、英語表示が維持されることを確認
- `docs/dev/feature-checklist.md` の「1. ローカライズ基盤」を全項目チェック済みに更新

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test -- App.test.tsx FrontmatterForm.test.tsx workspaceService workspaceSettings` 通過（22ファイル / 145テスト全通過）
- `pnpm package` 通過

## 追記（P9 検証運用の見直し）

### 方針

- GUI操作・実アプリ操作でしか分からない確認は、ユーザーが明示した場合を除き無理に実施しない
- トークン消費を抑えるため、コード確認・自動テスト・ビルドで判断できる範囲を優先する
- 操作が必要な項目はチェックせず、保留理由を完了報告に書く
- 上記を `AI.md` の P9-runtime-verification ルールへ追記

### ワークスペース管理のコード確認

- `prepareWorkspace` が `attachments/` と `templates/` を作成することは `workspaceService.test.ts` で確認済み
- 同じパスのワークスペースを重複登録せずアクティブ化することは `workspaceService.test.ts` で確認済み
- 登録済みワークスペースをアクティブに切り替えることは `workspaceService.test.ts` で確認済み
- フォルダ選択ダイアログ・新規ワークスペース作成ダイアログ・再起動後の実画面復元はGUI操作が必要なため、`feature-checklist.md` のチェックは未更新

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test -- workspaceService workspaceSettings` 通過（22ファイル / 145テスト全通過）

## 追記（P9 ファイル・フォルダ操作のコード確認）

### 追加テスト

- `moveMarkdownFile` の単体テストを追加
  - Markdownファイルを別フォルダへ移動できること
  - 移動先に同名ファイルがある場合は上書きしないこと
- `moveFolder` の単体テストを追加
  - フォルダを別フォルダへ移動できること
  - 移動先に同名フォルダがある場合は上書きしないこと

### 確認済みの範囲

- Markdownファイル作成、新規フォルダ作成、ファイルリネーム、フォルダリネーム、ファイル複製、ゴミ箱移動、未作成リンクからのファイル作成は既存テストで確認済み
- ファイル移動・フォルダ移動は今回追加した単体テストで確認済み
- ピン留め/解除は IPC 実装と workspace settings の読み書きは確認できるが、ピンボタン表示・並び順は実画面確認が必要
- `docs/dev/feature-checklist.md` の「3. ファイル・フォルダ操作」は、実機操作未確認のためチェック未更新

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test -- markdownFiles folders trash App.test.tsx workspaceSettings` 通過（22ファイル / 149テスト全通過）

## 追記（バッチ4）

### ライブプレビュー：カーソル行だけMarkdown記法を表示（フェーズ3）

- `Editor.tsx` に `buildLivePreviewDecorations` 関数と `livePreviewPlugin` (ViewPlugin) を追加
  - `syntaxTree(state)` で構文木を取得し、可視範囲のノードを走査
  - カーソルのある行以外は `Decoration.replace` でマーカーを非表示、`Decoration.mark` でスタイルを付与
  - 対応記法：ATXHeading1〜6（HeaderMark を hide → cm-live-h1〜h6）、StrongEmphasis（cm-live-bold）、Emphasis（cm-live-italic）、InlineCode（cm-live-code）、Strikethrough（cm-live-strike）、==highlight==（正規表現、cm-live-highlight）
  - コードブロック内部はスキップ
  - GFM 拡張（`markdown({ extensions: GFM })`）を追加して Strikethrough ノードを有効化
- `EditorProps` に `livePreview?: boolean` を追加、`buildExtensions` の引数にも追加
- `App.tsx` の「プレビュー」ボタン: `<Preview>` の代わりに `<Editor livePreview>` を描画するよう変更
  - FrontmatterForm は引き続き表示
  - ツールバーは両モード常時表示（仕様どおり）
- `styles.css` に `.cm-live-h1〜h6`・`.cm-live-bold`・`.cm-live-italic`・`.cm-live-strike`・`.cm-live-code`・`.cm-live-highlight` を追加

### Markdown記法サポート補完：脚注（フェーズ3）

- `marked-footnote` パッケージを追加（`pnpm add marked-footnote`）
- `Preview.tsx` に `import markedFootnote from "marked-footnote"` を追加し `marked.use(markedFootnote())` で登録
- `[^1]` / `[^1]: 注釈` 記法がページ末尾に脚注として表示されるようになった
- `<u>テキスト</u>` は DOMPurify のデフォルト許可リストで既に通過済みを確認（変更なし）
- 他の記法（テーブル、リスト、引用、コードブロック、数式、内部リンク等）は既実装で問題なし

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 22ファイル / 144テスト全通過

フェーズ3 完了。次はフェーズ7。

## 追記（バッチ5）

### ライト / ダーク / システム追従テーマ（フェーズ7）

- `shared/ipc.ts` に `AppTheme = "light" | "dark" | "system"` 型を追加し、`EditorSettings.theme` フィールドを追加（デフォルト: `"system"`）
- `main/settings/appSettings.ts` の `parseEditorSettings` に `theme` パースを追加
- `renderer/App.tsx` に `useEffect` で `document.documentElement` の `data-theme` 属性を設定するロジックを追加
  - `"system"` のときは属性なし + `prefers-color-scheme` 変更を監視
  - `"light"` / `"dark"` のときは `data-theme=light/dark` を設定
- 設定サイドバーに「テーマ」セレクト（システム/ライト/ダーク）を追加
- `styles.css` をCSS変数ベースにリファクタリング
  - `:root` に `--bg`, `--rail-bg`, `--sidebar-bg`, `--popup-bg`, `--text`, `--text-2`, `--text-3`, `--border`, `--border-soft`, `--border-medium`, `--btn-bg`, `--hover`, `--input-bg` を定義
  - `sed` で主要な硬直カラー値（`#1f2328`, `#555b62`, `rgba(31,35,40,...)`, `rgba(255,255,255,0.72)` 等）を変数参照に置換
  - `[data-theme="dark"]` ブロックとシステム追従 `@media (prefers-color-scheme: dark)` ブロックを追加
- テスト環境に `window.matchMedia` モックを追加

### タイトル一覧生成（フェーズ7）

- `shared/ipc.ts` に `generateTitleListChannel`, `GenerateTitleListInput` 型, `RelicApi.generateTitleList` を追加
- `main/ipc/workspaceHandlers.ts` に `generateTitleListChannel` ハンドラーを追加
  - ファイルツリーを走査して `.md` ファイルを収集
  - `filterFolder` で対象フォルダを絞り込み、`sortBy` でファイル名/更新日時順に並べ替え
  - `- [[ファイル名]]` 形式の箇条書きを出力ファイルに書き込む
  - 同名ファイルがある場合は `uniqueFilePath` で別名保存
- `preload.ts` にバインドを追加
- `renderer/App.tsx` に `ToolsSidebar` コンポーネントを追加（タイトル一覧・目次のフォームUI）
- ナビレールに「ツール (T)」ボタンを追加、`SidebarView` に `"tools"` を追加

### 目次生成（フェーズ7）

- `shared/ipc.ts` に `generateTableOfContentsChannel`, `GenerateTableOfContentsInput` 型, `RelicApi.generateTableOfContents` を追加
- `main/ipc/workspaceHandlers.ts` に `generateTableOfContentsChannel` ハンドラーを追加
  - 指定フォルダを再帰的にスキャン（`includeSubfolders` オプション付き）
  - フォルダは `**フォルダ名/**` ヘッダー + インデント、ファイルは `[[ファイル名]]` の箇条書き
  - 同名ファイルがある場合は `uniqueFilePath` で別名保存

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 22ファイル / 144テスト全通過

フェーズ7 残り：条件指定マージ、見出しによるファイル分割、機能トグル、エラー表示整理、日常利用できる状態まで磨く。

## 追記（バッチ6）

### 機能トグル（フェーズ7）

- `shared/ipc.ts` に `FeatureToggles` 型・`defaultFeatureToggles`・`getFeatureTogglesChannel`・`saveFeatureTogglesChannel` を追加
  - `git`, `tools`, `frontmatter`, `rightPanel`, `focusModes` の5フラグ
- `main/settings/appSettings.ts` に `featureToggles: FeatureToggles` フィールドを追加し、`parseFeatureToggles()` 関数を実装
- `workspaceHandlers.ts` に get/save ハンドラーを追加
- `preload.ts` に `getFeatureToggles` / `saveFeatureToggles` バインドを追加
- `App.tsx` に `featureToggles` state を追加し初期ロード・保存を実装
  - レール: gitボタン・ツールボタンをトグルで表示制御
  - 右パネルボタンをトグルで表示制御
  - `PaneView` に `showFrontmatter` prop を追加し `FrontmatterForm` の表示制御
  - タイプライターモードを `featureToggles.focusModes` と AND 条件に
- `SettingsSidebar` にトグル設定UIセクション（5チェックボックス）を追加
- `workspaceService.test.ts` の `baseSettings` に `featureToggles` を追加

### 条件指定マージ（フェーズ7）

- `shared/ipc.ts` に `MergeFilterType`, `MergeSortBy`, `MergeFilesInput` 型と `mergeFilesChannel` を追加
- `workspaceHandlers.ts` に `mergeFilesChannel` ハンドラーを追加
  - `filterType: "all" | "folder" | "tag"` で対象ファイルを絞り込み
  - `sortBy: "name" | "mtime" | "ctime"` で並び替え
  - `insertFilenameHeading: boolean` でファイル名の見出しを挿入するか制御
  - `uniqueFilePath` で既存ファイルとの衝突を避けて出力
- `preload.ts` に `mergeFiles` バインドを追加
- `ToolsSidebar` に条件指定マージのフォームUIを追加

### 見出しによるファイル分割（フェーズ7）

- `shared/ipc.ts` に `SplitHeadingLevel`, `SplitFileByHeadingInput` 型と `splitFileByHeadingChannel` を追加
- `workspaceHandlers.ts` に `splitFileByHeadingChannel` ハンドラーを追加
  - 指定した見出しレベル（H1/H2/H3）でファイルを分割
  - 見出しテキストをサニタイズしてファイル名に使用
  - `uniqueFilePath` で既存ファイルとの衝突を避けて出力
- `preload.ts` に `splitFileByHeading` バインドを追加
- `ToolsSidebar` に見出し分割フォームUIを追加

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 22ファイル / 144テスト全通過

フェーズ7 残り：エラー表示整理、自分用ビルドとして日常利用できる状態まで磨く。

## 追記（バッチ7）

### エラー表示整理（フェーズ7）

- `workspaceError` の表示をサイドバー末尾から**トースト通知**（画面下部中央、4秒後自動消去）に変更
  - `toastMessage` state（text + type）と `showToast(text, type)` コールバックを追加
  - `toastTimerRef` で `setTimeout` を管理、新しいエラーが来たら前のタイマーをキャンセル
  - クリックでも即消去できる
- トーストはエラー（赤左ボーダー）とinfo（グレー左ボーダー）の2種を用意
- `styles.css` にCSS変数 `--error`, `--error-bg`, `--success` を追加（`:root` / `[data-theme="dark"]` / `@media dark` の3箇所）
- 既存の `.error-note` の `color` を `#a14617` ハードコードから `var(--error)` に変更
- ツールステータス（`tool-status`）にエラー/成功クラスを追加
  - `tool-status--error`（`var(--error)` 色）、`tool-status--success`（`var(--success)` 色）
  - タイトル一覧・目次・マージ・分割の各ステータスで `"エラー:"` 判定によりクラスを切り替え
- `.toast` / `.toast--error` / `.toast--info` のスタイルを追加

### 自分用ビルドとして日常利用できる状態まで磨く（フェーズ7）

- `pnpm package` を実行、`out/Relic-darwin-arm64/Relic.app` の生成を確認
- TypeScript / テスト（22ファイル / 144テスト）全通過を再確認

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 22ファイル / 144テスト全通過
- `pnpm package` → `out/Relic-darwin-arm64/Relic.app` 生成確認

**フェーズ7 全項目完了。**

## 追記（UI 総点検）

### キーボードショートカット修正

- `⌘F` → 検索サイドバーを開く（未実装だったため追加）
- `⌘N` → 新規ノート作成（未実装だったため追加）
- `⌘⇧F` → ワークスペース全体検索（仕様通り。実装では誤ってフォーカスモードに割り当てられていた）
- フォーカスモードのショートカットを削除（仕様書に記載なし。コマンドパレット経由に統一）
- コマンドパレットに `⌘F`・`⌘N` のショートカット表記を追加

### コマンドパレット拡充

以下のコマンドを追加（spec/command-palette.md の「初期リリース対象」に明記されていた未実装項目）：
- ファイルを複製（アクティブタブのファイル、複製後に自動で開く）
- ファイルを削除（アクティブタブのファイル、確認ダイアログ付き）
- ファイル名を変更（ファイルサイドバーに誘導）
- Git: 手動プッシュ
- Git: 手動プル
- ブランチを切り替え: {ブランチ名}（カレント以外を動的列挙）

### CSS カラーシステム統一（DESIGN.md トークン準拠）

- `--bg`: `#f5f4f2`（ベージュ）→ `#fafafa`（純粋な白系グレー、設計書 `surface`/`background` トークン）
- `--text`: `#1f2328`（青みがかった黒）→ `#1c1c1c`（完全無彩色、設計書 `on-surface` トークン）
- `--text-2`: `#555b62` → `#5e5e5e`（完全無彩色、設計書 `secondary`/`on-surface-variant` トークン）
- `--text-3`: `#747981` → `#8a8a8a`（完全無彩色、設計書 `outline` トークン）
- `--rail-bg`・`--sidebar-bg`・`--popup-bg` も純粋なグレー値に修正
- ダークモード変数も設計書ダークトークン値に準拠（`#e6e6e6`, `#9e9e9e`, `#131313` 等）
- `--accent` 変数を新設し `#FF5722`（設計書 `primary-container`）に設定
- `--accent-hover`・`--link` 変数を新設（`#e64d1f`・`#00628c`）
- ハードコード値 `#df682b`, `#a14617`, `#1264a3`, `#6b7280`, `#9aa0a8`, `#6b6f76` をすべて CSS 変数参照に置換

### 確認

- `pnpm exec tsc --noEmit` 通過
- `pnpm test` 22ファイル / 144テスト全通過
