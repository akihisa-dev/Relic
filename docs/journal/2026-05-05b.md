# 2026-05-05b

## やったこと

フェーズ5（フロントマターとコマンド操作）の主要タスクを実装した。

### フロントマター読み書き共有処理

- `src/main/files/frontmatter.ts` を新規作成
  - `parseFrontmatter(content)` — `---` 区切りの YAML ブロックを解析し `{ data, body }` を返す
  - `writeFrontmatter(body, data)` — data + body からフロントマター付きコンテンツを生成
  - `updateFrontmatter(content, updater)` — 既存データをマージしながら更新
  - `parseFrontmatterCandidates(content)` — `frontmatter.md` から `## フィールド名` と箇条書きを解析
- `js-yaml` を dependencies に追加
- `frontmatter.test.ts` を追加（11テスト）

### IPC チャネル追加

- `getFrontmatterCandidatesChannel` — ワークスペース直下の `frontmatter.md` を読み込んで候補を返す
- `createFrontmatterTemplateChannel` — `frontmatter.md` の雛形ファイルを作成する（EEXIST のときエラーを返す）
- `workspaceHandlers.ts`・`preload.ts` にハンドラーと API を追加

### FrontmatterForm コンポーネント

- `src/renderer/components/FrontmatterForm.tsx` を新規作成
  - ライブプレビューモード時、Preview の上部にフォームとして表示
  - 組み込みフィールド `tags` / `aliases`（PillInput — Enter または , で追加、× で削除、候補オートコンプリート）
  - 固定システムフィールド `date`（date input）/ `status`（datalist つきテキスト入力）/ `publish`（チェックボックス）/ `url`（url input + 外部リンクボタン）/ `author`（PillInput）
  - 自由フィールド（上記20フィールド外のキー）はテキスト入力で表示・編集可能
  - フォームの変更はそのまま `onChange(content)` でタブコンテンツを更新 → 自動保存に乗る
  - ソースモードでは YAML を直接編集でき、モード切り替え時に双方向で反映される
  - js-yaml をレンダラー側でも使用（ブラウザ互換）
  - 折りたたみ/展開トグル付き

### コマンドパレット

- `src/renderer/components/CommandPalette.tsx` を新規作成
  - `⌘⇧P` で開く（App.tsx の keydown キャプチャリスナー）
  - コマンド一覧のフィルタリング・上下矢印選択・Enter 実行・Escape で閉じる
  - 実装したコマンド: 新規ノート作成、検索を開く、クイックスイッチャーを開く、サイドバー開閉、分割表示切り替え、右パネル切り替え、フォーカスモード、タイプライターモード、Git ビュー、設定を開く

### クイックスイッチャー

- `src/renderer/components/QuickSwitcher.tsx` を新規作成
  - `⌘P` で開く
  - ワークスペース内 .md ファイルをファイル名で絞り込み（最大50件）
  - 同名ファイルがある場合はフォルダパスを表示
  - 選択するとアクティブペインで開く

### 設定画面

- SettingsSidebar に `frontmatter.md を作成` ボタンを追加

### スタイル

- `styles.css` にフロントマターフォーム・モーダル・設定ボタン用スタイルを追加

### 確認

- `pnpm exec tsc --noEmit` 0エラー
- `pnpm test` 18ファイル 111テスト全通過

## 残り

- フィールド上限（20フィールド合計のバリデーション）
- フロントマター検索・フィルター
