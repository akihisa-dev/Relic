# 2026-05-13

- P18（UX/UIデザイン修正）として、グラフビューをObsidianのグラフビューに近づける作業を進めた。
- Obsidian公式ヘルプのGraph view要件を確認し、P18正本にObsidianグラフビュー実装チェックリストを追加した。
- グラフビューで、ノードホバー時の接続関係ハイライト、背景ドラッグでのパン、ホイール/トラックパッドでのズーム、キーボードでのパン/ズームを実装した。
- ノードサイズを被リンク数ベースにし、孤立ノード表示、リンク方向の矢印表示、ラベルフェード、ノードサイズ、リンク太さ、中心力、反発力、リンク力、リンク距離を調整できるようにした。
- 検索条件に一致するノートのグループ色分けを追加し、名前、パス、タグ、フォルダ、`#タグ`、`folder:フォルダ名` 形式に対応した。
- アクティブノート中心のローカルグラフと深さ設定を追加した。
- タイムラプスアニメーションはファイル履歴・時刻データに依存するため、P18のグラフUI改善では実装せず、将来の履歴/分析機能側で扱う判断にした。
- ユーザー提示のObsidianスクリーンショットを参考に、グラフ下部のノード詳細欄を撤去し、設定をグラフタブ上の右上ホバー表示へ移した。
- グラフ設定ホバーを小さい折りたたみパネルにし、最小化できるようにした。
- ノードを掴んで移動できるようにし、クリック時だけファイルを開き、ドラッグ時は開かないように整理した。
- 選択ノードが中心へ寄って配置全体が動く挙動をやめ、ノード、線、ホバー強調をObsidianに近い薄い表示へ調整した。
- `pnpm exec tsc --noEmit` と `pnpm test -- App.test.tsx` は通過した。
- 実アプリでの目視確認は未実施。
