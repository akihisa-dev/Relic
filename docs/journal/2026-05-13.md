# 2026-05-13

- P18（UX/UIデザイン修正）として、グラフビューをObsidianのグラフビューに近づける作業を進めた。
- Obsidian公式ヘルプのGraph view要件を確認し、P18正本にObsidianグラフビュー実装チェックリストを追加した。
- グラフビューで、ノードホバー時の接続関係ハイライト、背景ドラッグでのパン、ホイール/トラックパッドでのズーム、キーボードでのパン/ズームを実装した。
- ノードサイズを被リンク数ベースにし、孤立ノード表示、リンク方向の矢印表示、ラベルフェード、ノードサイズ、リンク太さ、中心力、反発力、リンク力、リンク距離を調整できるようにした。
- 検索条件に一致するノートのグループ色分けを追加し、名前、パス、タグ、フォルダ、`#タグ`、`folder:フォルダ名` 形式に対応した。
- アクティブノート中心のローカルグラフと深さ設定を追加した。
- タイムラプスアニメーションはファイル履歴・時刻データに依存するため、P18のグラフUI改善では実装せず、将来の履歴/分析機能側で扱う判断にした。
- ユーザー提示のObsidianスクリーンショットを参考に、グラフ下部のノード詳細欄を撤去し、設定をグラフタブ上の右上ホバー表示へ移した。
- グラフ設定ホバーを小さい折りたたみパネルにし、最小化できるようにした。
- ノードを掴んで移動できるようにし、クリック時だけファイルを開き、ドラッグ時は開かないように整理した。
- 選択ノードが中心へ寄って配置全体が動く挙動をやめ、ノード、線、ホバー強調をObsidianに近い薄い表示へ調整した。
- `pnpm exec tsc --noEmit` と `pnpm test -- App.test.tsx` は通過した。
- 実アプリでの目視確認は未実施。
- チャートモードをサイドバーではなくレールから開くメインタブに変更し、chronicle/date切替、検索/フィルタ、スケール操作をチャート上のホバー表示へ移した。
- チャートは保存済みの個別選択ではなく、chronicle/dateプロパティを持つファイルを全件表示するようにした。
- ファイルモードのサイドバーでは、ファイル・フォルダを掴んでドロップ移動する操作を無効にした。
- レールの区切りを「上段はビュー系、下段は設定・管理系」として整理し、グラフビューを上段に移した。
- グラフビューとチャートモードのホバーメニューに掴む部分を追加し、ドラッグで位置を動かせるようにした。
- チャートモード修正後に `pnpm exec tsc --noEmit`、`pnpm test -- App.test.tsx workspaceSettings.test.ts`、追加のレール/ホバーメニュー修正後に `pnpm exec tsc --noEmit`、`pnpm test -- App.test.tsx` が通過した。
- チャートモードのチャート面に、日付・年代境界と行境界に揃う補助線を追加した。
- 検索モードを独立したレールビューから外し、ファイルモード内の新規ファイル作成ボタン上に検索方法候補つきフォームとして統合した。
- 検索方法は、全文、ファイル名、タグ、プロパティ、正規表現をフォーム内の候補から選ぶ形にした。
- `⌘F` とコマンドパレットの検索は、ファイルモードを開いて検索欄へフォーカスする挙動に変更した。
- 旧検索サイドバーは削除した。旧検索ビュー内にあった置換UIはUI上から外れたため、残す場合は検索とは別のツール扱いで置き直す必要がある。
- チャート補助線追加後に `pnpm exec tsc --noEmit` と `pnpm test -- App.test.tsx` が通過した。
- 検索モード統合後に `pnpm exec tsc --noEmit` と `pnpm test -- App.test.tsx` が通過した。
- 今回のチャート補助線と検索モード統合は、実アプリでの目視確認は未実施。
- レールのサイドバー開閉専用ボタンを廃止し、ファイルボタンでファイルサイドバーを開閉する挙動へ統合した。
- ファイルボタン内に開閉方向を示す小さい矢印を追加し、位置をフォルダ胴体の中央に見えるよう調整した。
- 起動時の `getWorkspaceState` がアクティブワークスペースのファイルツリーを読まない問題を修正し、アプリ再起動後もファイルモードにファイルツリーとピン留めが復元されるようにした。
- レールの画面タブ入口は、再クリックで閉じず、既に開いているタブへ移動する挙動に統一した。
- レールボタンの状態表示を、表示中の `active` と開いているだけの `open` に分けた。
- タブの閉じる演出を、元の入口へ吸い込む動きではなく、タブ位置でしっとり沈むように薄く潰れて消える動きへ変更した。
- `pnpm test -- App.test.tsx`、`pnpm test -- workspaceHandlers.test.ts App.test.tsx`、`pnpm exec tsc --noEmit` が通過した。
- これらのレール/タブ/起動時復元修正は、実アプリでの目視確認は未実施。
- エディタ内フロントマターのプロパティフォームで、フォーム内入力がCodeMirror本文側に流れ込み、フロントマター開始 `---` の前へ文字が挿入される問題を調査した。
- フロントマターWidget内の追加用インライン入力欄はIME入力と本文編集の分離が難しいため撤去し、プロパティ追加と配列値追加はエディタ本文外の追加ダイアログで入力する形に変更した。
- `aliases` はそのファイル自身の別名であり、他ファイルの `aliases` 値を候補に出すと意味が崩れるため、`aliases` の値追加では候補を出さないようにした。`tags` など他の候補入力は維持した。
- 右上の分割ボタン横にソースモード切替を追加し、ONの間はエディタのライブ装飾・フォーム化を外してMarkdown構文をそのまま表示するようにした。切替時に古いCodeMirror DOMが残る問題も修正した。
- フロントマター関連修正後に `pnpm exec tsc --noEmit`、`pnpm test -- Editor.test.tsx`、`pnpm test -- Editor.test.tsx App.test.tsx` が通過した。
- 今回のフロントマター入力修正とソースモード追加は、実アプリでの最終目視確認はユーザー確認ベース。追加ダイアログ方式は強引だが一旦許容として終了した。
- エディタ内フロントマターのプロパティフォームを、内容を削除せずヘッダー操作で閉じたり開いたりできるようにした。
- ファイルモードの検索方法候補について、検索方法選択後に候補が再表示される問題を修正し、検索欄を押しただけでは候補が出ず、検索方法ボタン操作だけで候補が出るようにした。
- プロパティフォーム修正後に `pnpm exec vitest run src/renderer/components/Editor.test.tsx` と `pnpm exec tsc --noEmit` が通過した。
- 検索フォーム修正後に `pnpm exec vitest run src/renderer/App.test.tsx` と `pnpm exec tsc --noEmit` が通過した。
- ユーザー指示によりP18（UX/UIデザイン修正）を終了し、現在フェーズを `P-hold`（駐留・次フェーズ判断）へ移した。
