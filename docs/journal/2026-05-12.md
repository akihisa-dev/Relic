# 2026-05-12

## やったこと

- 年表タブの表示スケールを倍率表示ではなく、25 / 50 / 100 / 200 / 500 の年単位表示へ変更した。
- 年表の横軸ラベルを縦線中央に揃え、端のラベルは切れないよう内側へ寄せた。
- 年表バー内の負数表記と期間区切りを区別し、負数は `−`、期間は `〜` で表示するようにした。
- 年表バーのラベルは横スクロール中も読める位置へ追従させ、バー幅自体は年数に基づく元の幅を維持するよう調整した。
- 年表バー色を濃くし、ファイル名列と年表グリッドの行線・接続部・余計な上線を調整した。
- `pnpm exec tsc --noEmit` を通過確認した。
- `pnpm test -- App.test.tsx` を再実行し、全33ファイル / 320テストが通過した。
- フロントマター設定画面で、固定プロパティとカスタムプロパティのMarkdown上の書き方例を表示するようにした。
- フロントマターの書き方を、単体値でも `[]` 付きの1行配列へ揃える方針にした。
- `---` の位置が分かるよう、Markdownファイル先頭の設定欄全体の例を表示するようにした。
- 登録済みカスタムプロパティのフォーム編集後も、`[]` 付きの1行配列として書き戻すようにした。
- 既存カスタムプロパティ名の編集を、1文字ごとの即保存ではなく、blur / Enter の確定時保存へ変更した。
- フロントマター設定画面の強調表現を `docs/ui/DESIGN.md` に合わせ、オレンジ線・有彩色グラデーションを使わずグレースケールで調整した。
- `pnpm exec tsc --noEmit` を通過確認した。
- `pnpm test -- Editor.test.tsx SettingsSidebar.test.tsx` を実行し、全33ファイル / 322テストが通過した。
- `pnpm test -- SettingsSidebar.test.tsx` を実行し、全33ファイル / 323テストが通過した。

## 計画・決定

- 年表バーの幅は、文字を読むために広げず、単年・期間の実際の年数幅を優先する。
- 年表内の数値は、バーからはみ出しても読めることを優先する。
- フロントマタープロパティは、固定・カスタムを問わず、ユーザーへ見せる書き方を `プロパティ名: [値]` の1行配列に揃える。
- フロントマター設定画面のメリハリは、有彩色ではなくグレースケールの背景濃淡・境界線・文字ウェイト・等幅コード枠で付ける。

## 未決事項

- 年表タブの実アプリ目視確認は継続して必要。
- フロントマター設定画面の実アプリ目視確認は未実施。
