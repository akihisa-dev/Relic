# 2026-05-09

## やったこと

- P12 大規模リファクタリング・安全性強化を完了。
- Git / GitHub実装を `app/src/main/files/git*.ts` の責務別ファイルへ分割し、既存の `git.ts` は公開口として維持。
- Rendererの `App.tsx` からGit、検索、設定、ワークスペース/ファイル操作、テーマ、ショートカット、サイドバーリサイズ、バックリンク取得、コマンドパレット生成をhookへ切り出し。
- GitHub OAuth / Electron / Markdown preview / `.gitignore` / 開発規約の安全境界を確認・整備。
- `vite.renderer.config.ts` にmanualChunksを追加し、renderer buildの500KB超えchunk警告を解消。
- `pnpm exec tsc --noEmit`、`pnpm test`、Vite build確認を実施。

## 計画・決定

- P12完了後は `P-hold` に移動し、次フェーズはユーザー判断で決める。
- P13候補では配布ビルド確認として `electron-forge package` / `electron-forge make` を正規の確認コマンドに寄せる。

## 構想・アイデア

- GitHub機能の本格導入前に、OAuth / clone / push / pull / conflict解決を実アプリ操作で通し確認する。

## 問題・課題

- 依存関係監査は外部 registry に依存情報を送るため、P12では未実行。実行する場合はユーザーの明示許可が必要。
- `vite.main.config.ts` / `vite.preload.config.ts` の単独Vite実行はElectron Forgeのentry注入なしではrenderer HTMLを拾うため、配布確認ではForge経由のビルドを使う。
