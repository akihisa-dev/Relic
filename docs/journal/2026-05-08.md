# 2026-05-08

## やったこと

### Git画面のローカライズ修正

- Git画面のセクション見出し（Branches, Changes, Commit, Conflicts, Diff, GitHub, History, Remote, Repository, Tags 等）が英語ハードコードになっていたのを `t()` 経由へ移行
- ja.json に日本語訳を追加、en.json に対応キーを追加
- 英語モードで日本語テキストが残っていた箇所（FileTree, 検索置換プレビュー, GitHub接続ヒント, コンフリクト解決, タグ対象, Embed/Linkバッジ, 右パネル見出し等）を修正
- App.test.tsx の期待値を修正（"Outgoing" → "アウトゴーイング", "Backlinks" → "バックリンク", 重複 getByText 削除）
- 24ファイル / 170テスト全通過

### タブ右クリックコンテキストメニュー

- `editorStore.ts` に `closeOtherTabs` / `closeTabsToRight` / `closeAllTabsInPane` を追加
- PaneView にコンテキストメニューを追加（Close tab / Close Other Tabs / Close Tabs to the Right / Close All Tabs / Open in Other Pane）
- en.json / ja.json にキーを追加
- CSS にコンテキストメニュースタイルを追加

### Windows向けビルドスクリプト

- `package.json` に `package:win` / `make:win` スクリプトを追加
- `forge.config.ts` に `MakerSquirrel` と `MakerZIP（win32）` を追加

### アプリアイコン設定

- `docs/assets/ロゴ案.png`（1254×1254px）から macOS 用 `assets/icon.icns` と Windows 用 `assets/icon.ico` を生成
- `forge.config.ts` の `packagerConfig.icon` を `"assets/icon"` に設定

### デザイン刷新

- レールをダーク（`rgba(30,30,32,0.92)`）にし、アイコン色を白トーンに変更
- リンク色を `#0096c7`（明るいブルー）に変更
- グローバルスクロールバーを細く（5px）してトランスペアレントトラックに変更
- タイトルバーの背景もレールと同色に統一

### サイドバー・右パネルのスティッキーヘッダー

- 各パネルを `.sidebar-header`（固定）+ `.sidebar-body`（スクロール）に分割する構造に変更
- `.workspace { overflow: hidden }` を追加し、コンテンツが親をはみ出してスクロールされる問題を解消
- `.sidebar` / `.right-panel` に `height: 100%` を追加

### フォーカスモード削除

- `uiStore.ts` から `isFocusMode` / `toggleFocusMode` を削除
- `App.tsx` からコマンドパレットエントリ・クラス付与を削除
- `ipc.ts` / `appSettings.ts` の `FeatureToggles.focusModes` を削除
- CSS から `.app-shell--focus` ルールを削除
- 設定画面のフォーカス系モードトグルを削除
- `typewriterMode` は独立して引き続き動作（`featureToggles.focusModes` との AND 条件を解除）
- 24ファイル / 170テスト全通過

### App.tsx リファクタリング（コンポーネント分割）

4087行あった `App.tsx` を機能単位のコンポーネントファイルに分割した。

- `components/FileTree.tsx`（185行）: ファイルツリー表示
- `components/SearchSidebar.tsx`（331行）: 検索・置換サイドバー
- `components/FilesSidebar.tsx`（180行）: ファイル一覧・ワークスペース切り替え
- `components/ToolsSidebar.tsx`（286行）: ファイル加工ツール
- `components/SettingsSidebar.tsx`（209行）: 設定・自動同期・機能トグル
- `components/PaneView.tsx`（282行）: エディタペイン
- `components/GitSidebar.tsx`（745行）: Git/GitHub サイドバー

`App.tsx` 4087行 → 2111行（約半減）。24ファイル / 170テスト全通過。

### workspaceHandlers.ts 分割（AIメンテナンス性向上）

1859行あった `workspaceHandlers.ts` をドメイン別に4ファイルへ分割した。

- `workspaceHandlers.ts`（1859→359行）: ワークスペース状態・設定のみ。`buildWorkspaceState` を `export` して他ファイルから参照できるようにした
- `gitWorkspaceHandlers.ts`（573行）: Git操作20ハンドラ + 型ガード8件（createGitCommit/createGitBranch/createGitTag等）
- `fileHandlers.ts`（699行）: ファイル・フォルダCRUD・検索・置換・バックリンク・フロントマター + 型ガード11件
- `toolHandlers.ts`（259行）: merge/split/generateTitleList/generateTableOfContents + `uniqueFilePath` ヘルパー
- `main.ts` に4つの登録呼び出しを追加
- tsc --noEmit 0エラー / 170テスト全通過

### linkUpdater.ts サイレントcatch修正

`app/src/main/files/linkUpdater.ts` の2つの `readFile` try/catch が例外を完全に飲み込んでいた問題を修正。

- `readFile` catch: ENOENT はスキップ継続（リネーム後の旧パスが存在しない正常ケース）、それ以外は `console.error` でログ出力してから継続
- `writeFile`: try/catch が一切なかったため追加。失敗時は `console.error` でログ出力して継続（バッチ処理なので1ファイル失敗でも残りを処理する）
- linkUpdater のテスト10件全通過

### githubHandlers.ts 型ガード調査 → 対応不要

`githubHandlers.ts` の3ハンドラ（getGitHubAuthStatus / connectGitHub / disconnectGitHub）はすべて入力引数なし。型ガードを追加する必要はなく、既存実装は正しい。

### エラーハンドリングパターン調査 → 一貫性確認

`main/` 以下の裸の `catch {}` ブロックを全件確認した結果、すべて意図的かつ適切だった。

- `replace.ts`, `search.ts`: 正規表現バリデーション失敗 → 即座に `fail()` を返す
- `git.ts`: ファイル/ブランチ不在時の安全なデフォルト値（`false`, `[]`, `ok([])`）
- `frontmatter.ts`: YAMLパースエラー → `{ data: {}, body: content }` として安全処理
- `toolHandlers.ts`: `uniqueFilePath` の stat ENOENT 判定（ファイル不存在＝書き込み可能）
- `main.ts`: 自動同期エラーは明示コメント付きで意図的にサイレント

`RelicResult` パターンは全ハンドラで一貫して使用されており、追加対応不要。

## 計画・決定

- フォーカスモードは廃止する方針で確定
- デザインはダーク×ブルーを基調にする方針
