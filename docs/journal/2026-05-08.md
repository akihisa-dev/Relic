# 2026-05-08

## やったこと

### Git画面のローカライズ修正

- Git画面のセクション見出し（Branches, Changes, Commit, Conflicts, Diff, GitHub, History, Remote, Repository, Tags 等）が英語ハードコードになっていたのを `t()` 経由へ移行
- ja.json に日本語訳を追加、en.json に対応キーを追加
- 英語モードで日本語テキストが残っていた箇所（FileTree, 検索置換プレビュー, GitHub接続ヒント, コンフリクト解決, タグ対象, Embed/Linkバッジ, 右パネル見出し等）を修正
- App.test.tsx の期待値を修正（"Outgoing" → "アウトゴーイング", "Backlinks" → "バックリンク", 重複 getByText 削除）
- 24ファイル / 170テスト全通過

### タブ右クリックコンテキストメニュー

- `editorStore.ts` に `closeOtherTabs` / `closeTabsToRight` / `closeAllTabsInPane` を追加
- PaneView にコンテキストメニューを追加（Close tab / Close Other Tabs / Close Tabs to the Right / Close All Tabs / Open in Other Pane）
- en.json / ja.json にキーを追加
- CSS にコンテキストメニュースタイルを追加

### Windows向けビルドスクリプト

- `package.json` に `package:win` / `make:win` スクリプトを追加
- `forge.config.ts` に `MakerSquirrel` と `MakerZIP（win32）` を追加

### アプリアイコン設定

- `docs/assets/ロゴ案.png`（1254×1254px）から macOS 用 `assets/icon.icns` と Windows 用 `assets/icon.ico` を生成
- `forge.config.ts` の `packagerConfig.icon` を `"assets/icon"` に設定

### デザイン刷新

- レールをダーク（`rgba(30,30,32,0.92)`）にし、アイコン色を白トーンに変更
- リンク色を `#0096c7`（明るいブルー）に変更
- グローバルスクロールバーを細く（5px）してトランスペアレントトラックに変更
- タイトルバーの背景もレールと同色に統一

### サイドバー・右パネルのスティッキーヘッダー

- 各パネルを `.sidebar-header`（固定）+ `.sidebar-body`（スクロール）に分割する構造に変更
- `.workspace { overflow: hidden }` を追加し、コンテンツが親をはみ出してスクロールされる問題を解消
- `.sidebar` / `.right-panel` に `height: 100%` を追加

### フォーカスモード削除

- `uiStore.ts` から `isFocusMode` / `toggleFocusMode` を削除
- `App.tsx` からコマンドパレットエントリ・クラス付与を削除
- `ipc.ts` / `appSettings.ts` の `FeatureToggles.focusModes` を削除
- CSS から `.app-shell--focus` ルールを削除
- 設定画面のフォーカス系モードトグルを削除
- `typewriterMode` は独立して引き続き動作（`featureToggles.focusModes` との AND 条件を解除）
- 24ファイル / 170テスト全通過

## 計画・決定

- フォーカスモードは廃止する方針で確定
- デザインはダーク×ブルーを基調にする方針
