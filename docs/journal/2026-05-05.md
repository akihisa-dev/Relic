# 2026-05-05

## やったこと

- フェーズ4（リンク・タグ・検索）の残りを実装した

### リネーム時の内部リンク自動更新

- `src/main/files/linkUpdater.ts` を新規作成
- `updateLinksForFileRename(workspacePath, oldPath, newPath)` を実装
  - basename-only リンク（`[[ファイル名]]`）：同じフォルダ内のファイルから解決される場合に更新
  - パス付きリンク（`[[フォルダ/ファイル名]]`）：ワークスペース内任意のファイルから更新
  - エイリアス・見出し・ブロックIDを保持したまま target 部分だけ更新
  - コードブロック内のリンクは更新しない
  - 埋め込みリンク `![[...]]` も更新対象
- `updateLinksForFolderRename(workspacePath, oldFolderPath, newFolderPath)` を実装
  - フォルダリネーム後はパス付きリンクのみ更新（basename-only リンクはフォルダ内ファイル同士の相対関係が維持されるため不要）
- `renameMarkdownFile` 成功後に `updateLinksForFileRename` を呼ぶよう `markdownFiles.ts` を変更
- `renameFolder` 成功後に `updateLinksForFolderRename` を呼ぶよう `folders.ts` を変更
- `linkUpdater.test.ts` を追加（10テスト）

### 移動時の内部リンク自動更新（ファイル移動・フォルダ移動）

- `markdownFiles.ts` に `moveMarkdownFile(workspacePath, relativePath, destinationFolder)` を追加
  - 移動後に `updateLinksForFileRename` でリンクを自動更新
- `folders.ts` に `moveFolder(workspacePath, relativePath, destinationFolder)` を追加
  - 移動後に `updateLinksForFolderRename` でリンクを自動更新
- `ipc.ts` に `moveMarkdownFileChannel` / `moveFolderChannel` / `MoveMarkdownFileInput` / `MoveFolderInput` を追加
- `workspaceHandlers.ts` に移動ハンドラーを追加
- `preload.ts` に `moveMarkdownFile` / `moveFolder` を追加
- `App.tsx` にトップバーの `MoveBar` コンポーネントを追加（ファイルが開いているときに "Move" ボタン → フォルダパス入力フォームを表示）

### ファイル内置換・全ファイル一括置換プレビュー

- `src/main/files/replace.ts` を新規作成
  - `replaceInFile(workspacePath, relativePath, searchQuery, replacement, isRegex)` — 対象ファイルのみ置換
  - `searchAndReplace(workspacePath, searchQuery, replacement, isRegex)` — 全ファイルの一致行をプレビュー
  - `applySearchAndReplace(workspacePath, searchQuery, replacement, isRegex)` — 全ファイルに置換を適用
- `ipc.ts` に `replaceInFileChannel` / `searchAndReplaceChannel` / `applySearchAndReplaceChannel` と関連型を追加
- `workspaceHandlers.ts` に3本のハンドラーを追加
- `preload.ts` に対応するAPI を追加
- `App.tsx` の `SearchSidebar` に置換UIセクションを追加
  - 置換語句入力・置換後テキスト入力・正規表現チェックボックス
  - 「このファイルを置換」ボタン（アクティブファイルのみ）
  - 「一括プレビュー」ボタン（全ファイルの変更箇所を最大50件表示）
  - 確認後に「一括置換を実行」ボタンで適用
- `replace.test.ts` を追加（5テスト）

### 右パネルのアウトライン表示

- App.tsx を確認し、アウトライン表示は前回セッションで実装済みであることを確認
- `docs/dev/implementation-list.md` のチェックをつけた

### 確認

- `pnpm exec tsc --noEmit` 0エラー
- `pnpm test` 17ファイル 100テスト全通過

### `[[]]` WikiリンクのオートコンプリートとPaneView配線

- `Editor.tsx` に `@codemirror/autocomplete` を追加し、`[[` 入力後にワークスペース内ファイル名を補完する機能を実装
  - 同名ファイルが複数ある場合はパス付きラベル（例: `folder1/note`）で両方を提示
  - `basenameMap` で basename → [filePath] のマップを事前構築して高速化
- `allFilePaths` prop を `Editor` → `PaneView` → `App.tsx` JSXの2インスタンスに配線（`existingMarkdownPaths` を使用）

### 見出しリンクジャンプとブロックID挿入

- `Preview.tsx` の `buildRenderer` に `renderer.heading` を追加
  - 各見出しに `id` 属性（スラッグ化: 小文字 + スペース→ハイフン + 非単語文字除去）を付与
  - DOMPurify の `ADD_ATTR` に `id` を追加
- `Preview.tsx` に `scrollTargetHeading?: string` prop を追加
  - `useEffect` で `slugifyHeading(scrollTargetHeading)` → `querySelector` → `scrollIntoView` でスムーススクロール
- `Preview.tsx` の `handleClick` を更新し、`data-target` の `#` 以降を heading として `onOpenWikiLink(file, heading)` に渡す
- `onOpenWikiLink` の型を `(target: string, heading?: string) => void` に変更（Preview・PaneView・App.tsx）
- `handleOpenWikiLink` in `App.tsx` でファイルを開いた後、heading があれば `setLeftPaneScrollHeading` / `setRightPaneScrollHeading` を更新
- `Toolbar.tsx` にブロックID挿入ボタン（`^ID`）を追加
  - クリックで現在行末に ` ^{6文字ランダムID}` を挿入

### 確認

- `pnpm exec tsc --noEmit` 0エラー
- `pnpm test` 17ファイル 100テスト全通過

## 計画・決定

- フェーズ4のすべてのチェック項目を完了
- 次回はフェーズ5（フロントマター・コマンドパレット）へ進む

## 追記

- フェーズ6の Git タグ管理を進めた

### Git タグ機能

- `app/src/main/files/git.ts` に Git タグ一覧・作成・削除を追加
  - タグ一覧取得
  - 軽量タグの作成
  - メモ付き注釈タグの作成
  - タグ削除
  - タグごとの対象コミット・日時・メモの要約
- `app/src/shared/ipc.ts` / `app/src/preload/preload.ts` / `app/src/main/ipc/workspaceHandlers.ts` にタグ用 IPC を追加
  - `getGitTags`
  - `createGitTag`
  - `deleteGitTag`

### Git ビュー UI

- `app/src/renderer/App.tsx` の Git ビューにタグセクションを追加
  - 選択中コミットへのタグ付け
  - タグ一覧表示
  - タグ削除
  - 注釈タグかどうかの表示
- `app/src/renderer/styles.css` にタグ表示用スタイルを追加

### テスト・確認

- `app/src/main/files/git.test.ts` に Git タグのテストを追加
  - 軽量タグ
  - 注釈タグ
  - タグ削除
- `app/src/renderer/App.test.tsx` に Git ビューのタグ作成テストを追加
- `cd app && pnpm exec tsc --noEmit` 通過
- `cd app && pnpm exec vitest run src/main/files/git.test.ts src/renderer/App.test.tsx` 通過
- 既存の `act(...)` warning は継続して出るが、失敗はしていない

## 計画・決定

- Gitタグの作成・一覧・削除は完了
- Gitタグのプッシュは未着手のまま残す
