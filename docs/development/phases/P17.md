# P17

Relicのmap機能追加フェーズの正本。

このフェーズでは、Markdownファイルを正本として扱う前提を保ちながら、map機能を小さな単位で実装・検証する。
仕様が未確定の部分は実装前にユーザーへ確認し、決定済みの範囲から進める。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 画面・見た目の正本を確認する: `../../design/DESIGN.md`
- 機能仕様に関わる場合: `../../features/`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は、ユーザーが明示したmap機能追加に限る。
- Markdownファイルを正本として扱う前提を変えない。
- map機能が扱う情報の正本は、Markdown本文、リンク、タグ、フロントマター、コードブロックなど、Markdownに書ける情報を前提にする。
- map機能の意味、対象データ、表示形式、編集可否、保存形式は、実装前にユーザーへ確認する。
- 仕様書、設計書、チェックリスト、詳細な作業項目は、ユーザーの明示指示または合意なしに作成しない。
- 新しい外部サービス、地図API、クラウド連携、位置情報利用、ネットワーク前提の技術は、事前に理由と選択肢を示してユーザー確認を取る。
- 大きな設計変更、新規大型機能追加、画面全体の方針変更は、事前に理由と選択肢を示してユーザー確認を取る。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。

---

## 対象範囲

- map機能の目的・対象データ・利用場面の確認
- map機能に関わる仕様整理
- map機能に関わる小規模な設計確認
- map機能に関わる小さな単位の実装
- map機能に関わるテストの追加・補強
- map機能に関わる不具合修正
- 文書の最小限の整合性更新

対象外:

- ユーザー確認なしのmap機能仕様確定
- ユーザー確認なしのMarkdown保存形式変更
- ユーザー確認なしのローカルファイル操作仕様変更
- ユーザー確認なしの外部地図サービス・外部API導入
- ユーザー確認なしの位置情報・ネットワーク機能追加
- ユーザー確認なしの画面全体のデザイン方針変更
- 配布ビルド確認

## 作業記録

このフェーズでは、map機能追加の作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-06-13 15:28 JST

- ユーザー指示により、P17 map機能追加フェーズを開始した。P17ではMarkdownファイルを正本として扱う前提を保ちながら、map機能の目的、対象データ、表示方法、操作方法を確認し、小さな単位で仕様化・設計・実装・検証を進める。今回のフェーズ移行作業として `docs/development/phases.md` の現在フェーズをP17へ変更し、P16を終了扱いにする。作業記録更新に伴い `app/package.json` のバージョンを `0.2.187` へ上げる。

### 2026-06-13 15:30 JST

- ユーザー指示により、P17をmap機能追加の実装フェーズとして扱うよう正本の説明を修正した。仕様が未確定の部分は実装前に確認し、決定済みの範囲から小さな単位で実装・検証する前提にした。作業記録更新に伴い `app/package.json` のバージョンを `0.2.188` へ上げる。

### 2026-06-13 17:37 JST

- P17の最初の実装単位として、全Markdownファイルの更新型インデックス基盤を追加した。インデックスはRelicの設定領域に再作成可能な控えとして保存し、ファイルサイズと更新日時が変わらないファイルは再読み込みしない。先頭行 `type: map` によるMap用Markdown判定と、全文検索用の行単位テキストを持たせ、既存の全文検索をインデックス経由へ接続した。関連文書として `docs/development/coding-rules.md` と `docs/engineering/data-model.md` を更新し、対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.189` へ上げる。

### 2026-06-13 17:45 JST

- P17の次の実装単位として、左レールにMap入口を追加し、左サイドバーにMapファイル一覧と配置候補のMarkdownファイル一覧を表示した。一覧は更新型インデックスの分類を使い、Mapファイルは開けるようにし、配置候補MarkdownはMapキャンバス実装前の一覧表示に留めた。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.190` へ上げる。

### 2026-06-13 17:52 JST

- P17の次の実装単位として、Map用Markdownの読み取り・書き戻し処理を追加した。先頭行 `type: map`、Node、Line、Label、Node位置、Node固定サイズを検査し、壊れたMap用Markdownは編集対象にしない形にした。Map判定は更新型インデックスと同じ共有処理へ寄せ、関連文書として `docs/engineering/data-model.md` を更新した。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.191` へ上げる。

### 2026-06-13 17:56 JST

- P17の次の実装単位として、Map用Markdownを開いたときに通常エディタではなくMap表示へ切り替える初期表示を追加した。Nodeを固定サイズカードとして表示し、LineをSVGで描画し、LabelをLine中央に表示する。ソースモードでは従来のMarkdownエディタを開ける導線を残した。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.192` へ上げる。

### 2026-06-13 18:00 JST

- P17の次の実装単位として、ファイル名変更・移動時にMap用Markdown内の `nodes[].file` 参照も更新する処理を追加した。既存の内部リンク更新処理へ統合し、本文・フロントマター内リンクとMap参照を同じ更新単位で扱う。壊れたMap用Markdownは安全に書き換えられないため更新を止める。関連文書として `docs/development/coding-rules.md` を更新し、対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.193` へ上げる。

### 2026-06-13 18:04 JST

- P17の次の実装単位として、Mapサイドバーの配置可能Markdownファイルをクリックしたとき、開いているMapタブの中央付近へ固定サイズNodeを追加する処理を実装した。Map用Markdown本文を更新して既存の自動保存に乗せる形にし、Mapファイルが開かれていない場合は配置操作を無効にした。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.194` へ上げる。

### 2026-06-13 18:08 JST

- P17の次の実装単位として、Mapキャンバス上のNode移動をPointer Eventsで実装した。ドラッグ中はMap表示の一時状態だけを動かし、pointerup時にNode座標をMap用Markdownへ書き戻して既存の自動保存に乗せる。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.195` へ上げる。

### 2026-06-13 18:13 JST

- P17の次の実装単位として、MapキャンバスでNodeの接続ハンドルから別NodeへPointer操作でLineを追加する処理を実装した。自分自身へのLineと重複Lineは作らず、確定時にMap用Markdownへ書き戻して既存の自動保存に乗せる。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.196` へ上げる。

### 2026-06-13 18:17 JST

- P17の次の実装単位として、MapキャンバスでNodeとLineを選択し、DeleteまたはBackspaceで削除する処理を実装した。Node削除時は接続しているLineも一緒にMap用Markdownから削除し、Line削除時は対象Lineだけを削除する。Nodeをクリックしただけでは座標を書き戻さないようにした。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.197` へ上げる。

### 2026-06-13 18:21 JST

- P17の次の実装単位として、MapキャンバスでLineのLabelをクリック、またはLineをダブルクリックしてインライン編集する処理を実装した。Label確定時は対象Lineの `label` だけをMap用Markdownへ書き戻して既存の自動保存に乗せる。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.198` へ上げる。

### 2026-06-13 18:25 JST

- P17の次の実装単位として、Mapキャンバスのpanとzoomを画面上だけの一時状態として実装した。空白ドラッグで表示位置を動かし、ホイール操作でカーソル位置を中心に拡大縮小する。zoom中もNode移動とLine作成の座標をMap内座標へ変換して扱い、pan、zoom、選択状態はMap用Markdownへ保存しない。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.199` へ上げる。

### 2026-06-13 18:28 JST

- P17の次の実装単位として、Mapキャンバス上のNodeをダブルクリックしたとき、Nodeが参照している通常Markdownファイルを開く処理を実装した。既存のファイルオープン処理を `AppEditorWorkspace` から `PaneView`、`PaneContentSurface`、`MapCanvas` へ渡して使う形にした。対象テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.200` へ上げる。

### 2026-06-13 18:39 JST

- P17の不足対応として、Mapサイドバー上部に新規Map作成ボタンを追加した。作成時は重複しない `Map.md` 系の名前を選び、空のMap用Markdown本文を書き込んでからワークスペース状態を更新し、作成したMapファイルを開く。関連テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.201` へ上げる。

### 2026-06-13 18:45 JST

- P17の不具合対応として、Map Node内の文字と接続点が見切れないよう表示を修正し、Node全体のダブルクリックで参照ファイルへ画面遷移する処理を外した。接続点のクリックとダブルクリックはMapキャンバス内で完結するようにし、誤操作でMap画面から離れない形にした。関連テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.202` へ上げる。

### 2026-06-13 18:53 JST

- P17の不具合対応として、Map Node右端の接続点を押したときのイベントが親Nodeやキャンバスへ漏れないよう修正した。同じ接続点を押して離した場合はLine作成やMarkdown更新を行わず、Map画面内で何も起きない形にした。接続点の押下操作を含むテストを追加し、関連テスト、型チェック、全体テストが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.203` へ上げる。

### 2026-06-13 19:09 JST

- P17の不具合対応として、Map Node右端の接続点を押してわずかに動いたときに画面が白くなる問題を修正した。接続中の移動処理がReactのイベント情報を後から読んで例外になる状態を、イベント情報を先に取り出してから状態更新する形に直した。接続点はクリックだけではLine作成を開始せず、少し動かした場合だけLine作成操作として扱う。関連テスト、型チェック、全体テストが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.204` へ上げる。

### 2026-06-13 19:15 JST

- P17の不具合対応として、Mapキャンバスの空白ドラッグで視点移動できない場所が残っていた問題を修正した。Node、Line、Labelの各レイヤーの空白部分と空Map表示を背景ドラッグの開始対象に含め、Node移動やLine選択などの操作対象は従来どおり個別操作として扱う。空白レイヤーからpanできるテストを追加し、全体テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.205` へ上げる。

### 2026-06-13 19:52 JST

- P17の操作改善として、Map Node右端の連結点表示を削除し、選択済みNodeをドラッグしたときにLine作成へ入るよう変更した。未選択Nodeのドラッグは従来どおりNode移動として扱い、選択済みNodeから別Nodeへドラッグして離した場合だけLineを追加する。連結点非表示、選択済みNodeからのLine作成、未選択Nodeの移動維持、自分自身へのLine不作成をテストし、全体テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.206` へ上げる。

### 2026-06-13 19:58 JST

- P17の不具合対応として、選択済みNodeを掴んで移動できなくなっていた問題を修正した。選択済みNodeを別Node上で離した場合はLine作成、それ以外で離した場合はNode移動として確定する形にし、選択済みNodeの移動テストを追加した。全体テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.207` へ上げる。

### 2026-06-13 20:13 JST

- P17の操作改善として、選択済みMap Nodeの外周アウトラインだけをLine作成の開始対象にし、Node内側は選択中でもNode移動として扱うよう修正した。丸い連結点や修飾キー操作は使わず、外周から別Nodeへドラッグした場合だけLineを追加し、外周を空白へドラッグしてもNode移動やLine追加を行わない。外周Line作成、内側Node移動、未選択Node移動、外周空白ドロップのテストを確認し、全体テストと型チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.208` へ上げる。
