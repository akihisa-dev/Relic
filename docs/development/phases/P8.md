# P8

RelicのUI・ビジュアルデザインフェーズの正本。

このフェーズでは、Relicの根底である「Markdownに書ける情報をMarkdownファイルのまま保つ」方針を崩さず、画面構成、視認性、操作感、余白、色、タイポグラフィ、アイコン、状態表示を整理する。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 既存仕様との整合を見る: `../../features/`
- 画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲はUI・ビジュアルデザインの整理、仕様化、実装、検証に限る。
- Relicの中心であるMarkdown編集、ローカルファイル管理、ワークスペース操作の分かりやすさを優先する。
- 画面全体の印象、配色、余白、文字サイズ、アイコン、パネル構成、状態表示は、既存機能を壊さない範囲で検討する。
- ユーザーが指定した画面、場所、操作対象以外を勝手に変更しない。
- デザイン方針、画面変更、UI文言、操作導線を決める場合は、ユーザーへ具体例を添えて確認する。
- 見た目を整えるためだけに、Markdownファイルの保存形式や既存のデータ構造を変更しない。
- 大きなリデザイン、画面構成変更、操作導線追加は、実装前に対象範囲を明示してユーザー確認を得る。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。

---

## 対象範囲

- 全体の視覚階層、余白、色、文字サイズ、アイコンの整理
- 左レール、サイドバー、セカンダリサイドバー、右パネル、タブ、ステータスバーの見た目と操作感
- Markdown編集画面、プレビュー、設定画面、Cowork画面の視認性と状態表示
- ライト / ダーク / システム追従テーマの整合
- 実装済み機能を前提にしたUI文書・デザイン文書の同期
- 必要になった場合の最小限のUI実装、テスト、実アプリ確認

対象外:

- ユーザー確認なしの画面全体リデザイン
- ユーザー確認なしの新機能追加
- Markdownファイル保存形式の変更
- AI機能やファイル操作仕様そのものの再設計
- 配布ビルド確認

## 作業記録

このフェーズでは、UI・ビジュアルデザインの作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-31 16:23 JST

- ユーザー指示により、P7を終了し、P8 UI・ビジュアルデザインフェーズを開始するための正本を作成した。P8では、RelicのMarkdown正本方針を維持しながら、画面構成、視認性、余白、色、タイポグラフィ、アイコン、状態表示をユーザー確認に基づいて整理する扱いにした。今回のP8開始作業として `app/package.json` のバージョンを `0.1.199` へ上げる。

### 2026-05-31 16:46 JST

- 既存UXとレイアウトを維持したまま、全体背景レイヤー、中央作業領域の不透明サーフェスと軽い影、サイドバー系UIの限定的な半透明・ブラー、タイトルバーの軽い透過と影、border依存を抑えた区切り表現を追加した。Light/Darkのスクリーンショットを一時ハーネスで比較し、`pnpm verify` が成功した。今回のP8 UIモダナイズ作業として `app/package.json` のバージョンを `0.1.200` へ上げる。

### 2026-05-31 18:36 JST

- 既存ボタンとタブ配置を維持したまま、左レール、ファイルサイドバー、セカンダリサイドバー、右パネルを背景上に浮いたパネルとして見せるCSS調整を行い、メインエリアの強いカード境界を外した。開発版でファイル、右パネル、Coworkセカンダリサイドバーの表示を確認し、`pnpm typecheck`、関連テスト、`pnpm verify` が成功した。今回の浮遊パネル化作業として `app/package.json` のバージョンを `0.1.201` へ上げる。

### 2026-05-31 19:19 JST

- HTMLモックとの差分を踏まえ、タイトルバーのタブ開始位置をセカンダリサイドバー開閉状態に合わせて補正し、タブの高さ・白い面・影、下部ステータスバーの浮遊パネル表現、背景と浮遊パネルのコントラストを調整した。開発版でファイルサイドバー、Coworkセカンダリサイドバー、右パネル、既存ボタンの表示を確認し、`pnpm typecheck`、関連テスト、`pnpm verify` が成功した。今回のモック追従調整として `app/package.json` のバージョンを `0.1.202` へ上げる。

### 2026-05-31 19:31 JST

- 背景と浮遊パネルの色差が弱い状態を改善するため、ライトテーマの背景を青みのあるグレーへ寄せ、レール、サイドバー、セカンダリサイドバー、右パネル、ステータスバーの面・境界・影を強めた。右パネルにも他の浮遊パネルと同じ半透明背景とブラーを反映し、開発版でファイル表示とCowork表示を確認した。`pnpm typecheck`、関連テスト、`pnpm verify` が成功した。今回のメリハリ調整として `app/package.json` のバージョンを `0.1.203` へ上げる。

### 2026-05-31 19:35 JST

- メインエディタを背景と同じレイヤーに見せるため、エディタ本文面とCodeMirror本体、スクロール領域、ガターの白背景を透明化し、ステータス境界を弱めた。開発版で中央本文が白いカードとして浮かず、背景面と連続して見えることを確認した。`pnpm typecheck`、エディタ関連テスト、`pnpm verify` が成功した。今回のメインエディタレイヤー調整として `app/package.json` のバージョンを `0.1.204` へ上げる。

### 2026-05-31 19:40 JST

- 背景と浮遊パネルの色関係を逆転させ、ライトテーマでは背景レイヤーを明るい作業面へ戻し、レール、サイドバー、右パネルなどの浮遊要素に薄い青みを持たせた。開発版で中央本文が主役になり、左右パネルが浮遊面として見えることを確認した。`pnpm typecheck`、関連テスト、`pnpm verify` が成功した。今回の色関係調整として `app/package.json` のバージョンを `0.1.205` へ上げる。

### 2026-05-31 19:45 JST

- ファイル未選択時の空状態に旧レイヤーの白背景が残っていたため、空状態コンテナの背景を透明化した。開発版で未選択状態の中央面が背景レイヤーと連続して見えることを確認し、`pnpm typecheck`、エディタ関連テスト、`pnpm verify` が成功した。今回の反映漏れ修正として `app/package.json` のバージョンを `0.1.206` へ上げる。

### 2026-05-31 19:50 JST

- フロントマター/設定系のパネルタブに旧い白カード面が残っていたため、設定グループとフロントマター項目カードの背景塗りを外し、背景レイヤー上の線だけのセクション表現へ調整した。開発版でCowork併用時のフロントマター設定表示を確認し、`pnpm typecheck`、関連テスト、`pnpm verify` が成功した。今回の設定系旧レイヤー除去として `app/package.json` のバージョンを `0.1.207` へ上げる。
