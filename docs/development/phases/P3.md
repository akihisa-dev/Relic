# P3

RelicのUI/UX改善フェーズの正本。

このフェーズでは、現行機能の見た目、操作感、情報設計、分かりやすさ、操作負荷の改善に必要な範囲だけを扱う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- 対象機能の仕様を確認する: `../../features/`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲はUI/UX改善に限る。
- 機能追加、保存形式変更、既存Markdownの一括変更へ広げない。
- 画面変更では、ユーザーが指定した画面・場所・操作対象だけを変更する。
- 見た目を整える目的で、未確認の入口、ボタン、説明、操作方法を増やさない。
- 具体的な改善対象は、ユーザーに1件ずつ確認してから決める。
- デザイン変更では `../../design/system.md` の色、余白、コンポーネント方針に従う。

---

## 対象範囲

- 現行画面の見た目
- 現行操作の分かりやすさ
- 現行操作の手間や迷いの軽減
- レイアウト、余白、視線誘導、状態表示
- 文言、ラベル、エラー表示、補助表示

対象外:

- 新機能追加
- 保存形式、IPC、データモデルの仕様変更
- 既存Markdownファイルの一括変更
- 配布ビルド確認
- ユーザー確認なしの画面全体リデザイン

## 作業記録

このフェーズでは、UI/UX改善の実装単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-26 01:16 JST

- ユーザー指示により、P2マークダウンエディタとファイルビュー関連フェーズを終了し、P3 UI/UX改善フェーズを開始した。対象範囲を現行機能の見た目、操作感、情報設計、分かりやすさ、操作負荷の改善に限定し、具体的な改善対象は次回以降に1件ずつ確認して決める扱いにした。今回の作業記録分として `app/package.json` のバージョンを `0.0.87` へ上げた。

### 2026-05-26 07:21 JST

- ユーザー指示により、Markdownエディタの行番号が本文列から離れて表示される不具合を修正した。行番号表示時は本文最大幅と行番号ガターを同じ列として中央配置し、対象回帰テスト、型チェック、開発版Electronでの表示確認を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.88` へ上げた。

### 2026-05-26 08:16 JST

- ユーザー指示により、機能実態と一致していなかった `Gantt` 系の実装名を `Chart` 系へ、パネルタブ用の `Sidebar` 系コンポーネント名を `Panel` 系へ整理した。IPC名とワークスペース設定キーも `charts` 系へ更新し、旧 `ganttCharts` キーは読み込み互換を追加した。用語集とデータモデル文書を更新し、型チェックとテストを実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.89` へ上げた。

### 2026-05-26 20:18 JST

- ユーザー指示により、空だった最上位タイトルバー行へペインのタブとソース、分割、アウトライン、リンク切り替え操作を統合し、ペイン本文領域を上方向に広げた。タブ選択、タブ閉じ、右クリックメニュー、分割表示、右パネル表示のテストを追加・更新し、型チェック、全体テスト、開発版Electronでの表示確認を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.90` へ上げた。

### 2026-05-26 20:52 JST

- ユーザー指示により、最上位タイトルバー内のタブを本文面と一体化し、アクティブタブが紙のインデックスのように見える表現へ調整した。CSSのみでタブの高さ、境界、下辺マスク、ホバー表現を更新し、型チェック、関連テスト、開発版Electronで単一ペインと分割表示の表示確認を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.91` へ上げた。

### 2026-05-26 21:07 JST

- ユーザー指示により、最上位タイトルバーを青にし、タイトルバー内のタブ列とエディタ本文面の境界線を消して一枚の紙としてつながる見た目へ調整した。型チェック、全体テスト、開発版Electronでの表示確認を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.92` へ上げた。

### 2026-05-26 21:18 JST

- ユーザー指示により、Markdownエディタの行番号を本文列の近くではなくエディタ面の左端側へ戻した。本文は設定幅で中央配置するまま、行番号と本文を同じ中央列として扱う分岐を削除し、対象テスト、型チェック、全体テスト、開発版Electronでの表示確認を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.93` へ上げた。

### 2026-05-26 21:30 JST

- ユーザー指摘により、左端側へ戻したMarkdownエディタの行番号が本文行と縦に一致しない不具合を修正した。行番号ガターにも本文と同じ上下余白を設定し、左端寄りの配置を保ったまま行との対応を戻した。対象テスト、型チェック、全体テスト、開発版Electronでの表示確認を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.94` へ上げた。
