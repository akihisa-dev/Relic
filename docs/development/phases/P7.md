# P7

RelicのAIワークスペースフェーズの正本。

このフェーズでは、Relicの根底である「Markdownに書ける情報をMarkdownファイルのまま保つ」方針を崩さず、AIがワークスペース内の情報をどのように補助できるかを、ユーザー確認に基づいて小さく定義する。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 既存仕様との整合を見る: `../../features/`
- 画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲はAIワークスペースの方向性整理、仕様化、最小実装、検証に限る。
- AI補助は、Markdownファイルを正本として扱うRelicの方針を崩さない範囲で検討する。
- AIが生成した内容を、ユーザー確認なしにMarkdownファイルへ書き込む前提にしない。
- AIワークスペースの具体的な機能、画面、データ保持、外部サービス利用、API利用は、ユーザーへ1問ずつ確認し、承認された内容だけを正本化または実装する。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。
- 新しい仕様書、設計書、チェックリスト、UI、API連携を勝手に作成・更新しない。

---

## 対象範囲

- AIワークスペースの目的と境界の整理
- AIが参照してよいワークスペース情報の範囲決め
- AIがMarkdownファイルへ提案、要約、整理、生成を行う場合の扱いの整理
- 必要になった場合の最小限の仕様書、設計文書、実装、テスト

対象外:

- ユーザー確認なしのAI機能確定
- ユーザー確認なしの外部AIサービス利用
- ユーザー確認なしのAPIキー保存方式決定
- ユーザー確認なしのMarkdownファイル自動更新
- ユーザー確認なしの画面全体リデザイン
- 配布ビルド確認

## 作業記録

このフェーズでは、AIワークスペースの作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-30 14:40 JST

- ユーザー指示により、P7 AIワークスペースフェーズを最小構成で開始した。具体的なAI機能、画面、データ保持、外部サービス利用、API利用は未確定のため、次回以降に1件ずつ確認して決める扱いにした。今回のフェーズ移行作業として `app/package.json` のバージョンを `0.1.100` へ上げる。

### 2026-05-30 16:30 JST

- ユーザー確認により、AI WorkspaceをRelic内でAIとローカルMarkdownワークスペースを共同編集する体験として整理し、`docs/features/ai-workspace.md` を作成した。右パネルをAI共同作業パネルとして扱い、全Markdown対象、対話による作成・編集・削除、AI系データのローカルアプリデータ保存、codexappserverをAI処理基盤として扱う方針を仕様化した。今回の仕様整理として `app/package.json` のバージョンを `0.1.101` へ上げる。

### 2026-05-30 16:43 JST

- ユーザー指示により、AI Workspace仕様を追加整理した。CodexやCoworkで外部からRelicワークスペースを開いて行う共同編集をRelic内で完結させる体験を中核に置き、差分表示、履歴、参照ファイル表示、適用ボタンは中核ではなく補助要素として扱うよう明確化した。自然文による反映、取りやめ、部分変更、再作業を右パネル内の操作として扱う方針も追記した。今回の仕様整理として `app/package.json` のバージョンを `0.1.102` へ上げる。

### 2026-05-30 17:03 JST

- ユーザー指示により、AI Workspaceの初期実装として右パネルのAI表示、ワークスペース内Markdownのローカルインデックス、参照検索、会話履歴とAI系データのアプリデータ保存、IPC/API境界、codexappserver利用可否表示を追加した。大きすぎるMarkdownと読めないMarkdownはインデックス対象から除外し、現時点では本物のcodexappserver会話とMarkdown作成・編集・削除の実行は次の実装単位として残した。今回の初期実装として `app/package.json` のバージョンを `0.1.103` へ上げる。

### 2026-05-30 17:13 JST

- ユーザー指示により、AI Workspaceでcodexappserverを起動してturnを実行できることを確認し、右パネルの会話からcodexappserverへMarkdown変更案をJSONで生成させる経路を追加した。生成された作成・更新・削除の変更案はAI系アプリデータへ保存し、右パネルからRelic側のMarkdown操作として反映できるようにした。今回のAI変更案反映基盤として `app/package.json` のバージョンを `0.1.104` へ上げる。

### 2026-05-30 17:19 JST

- ユーザー指示により、AI Workspaceの変更反映時に未保存Markdownを上書きしない保護を追加し、右パネルと自然文の両方からAI変更案を取りやめられるようにした。未保存タブがある対象Markdownへの更新・削除は反映前に停止し、変更案の取りやめはMarkdownファイルへ影響しないことをテストで確認した。今回の安全な変更反映強化として `app/package.json` のバージョンを `0.1.105` へ上げる。

### 2026-05-30 17:24 JST

- ユーザー指示により、AI Workspaceの参照Markdownと変更対象Markdownに右パネルから開ける導線を明示し、codexappserverの実行に失敗した場合はローカル検索結果だけを表示していることと失敗理由をAIメッセージ内で分かるようにした。codexappserver失敗時にMarkdown変更案を作らないこともテストで確認した。今回のAI Workspace表示改善として `app/package.json` のバージョンを `0.1.106` へ上げる。

### 2026-05-30 17:29 JST

- ユーザー指示により、AI Workspace右パネル内に会話と変更履歴の表示切り替えを追加した。AI変更履歴では作成・編集・削除の種類、未反映・反映済み・取りやめ・失敗の状態、実施時刻、対象Markdownへの開く導線を表示し、保存済みのAI系アプリデータを右パネルで見返せるようにした。今回の履歴表示強化として `app/package.json` のバージョンを `0.1.107` へ上げる。

### 2026-05-30 17:33 JST

- ユーザー指示により、AI Workspaceで大きすぎる、または読めないためAI参照から除外されたMarkdownを右パネルで確認できるようにした。除外対象のパス、除外理由、対象Markdownを開く導線を表示し、AIが読めなかったファイルを読んだように扱わないための透明性を強化した。今回のAI参照除外表示として `app/package.json` のバージョンを `0.1.108` へ上げる。

### 2026-05-30 17:40 JST

- ユーザー指示により、AI Workspaceで外部AIへ送る前に参照予定Markdownを右パネルで確認できるようにした。通常のAI依頼では参照予定ファイル、行、プレビューを表示して送信またはキャンセルできるようにし、作業中の変更を自然文で反映・取りやめする指示は外部AI送信確認を挟まずローカル処理へ進むようにした。今回の送信前参照確認として `app/package.json` のバージョンを `0.1.109` へ上げる。

### 2026-05-30 17:43 JST

- ユーザー指示により、AI WorkspaceでAI変更案の作成時点における対象Markdownの内容指紋を保存し、反映時に現在のMarkdownと照合する保護を追加した。AI変更案の作成後に対象Markdownが変更されていた場合は上書きせず、変更履歴上で再作業が必要な状態として扱うようにした。今回の古い変更案反映防止として `app/package.json` のバージョンを `0.1.110` へ上げる。

### 2026-05-30 17:46 JST

- ユーザー指示により、AI WorkspaceのローカルMarkdown検索で日本語の自然文依頼から関連語を拾いやすくした。空白区切りだけに依存せず、日本語の連続文字から短い検索手がかりを作ることで、「認証について整理して」のような入力でも対象Markdownを参照予定に出せるようにした。今回の日本語検索改善として `app/package.json` のバージョンを `0.1.111` へ上げる。
