# P19

Relicの機能改善・修正・追加フェーズの正本。

このフェーズでは、Markdownファイルを正本として扱う前提と既存の保存形式を守りながら、ユーザーが指定した機能改善、不具合修正、必要な小規模追加を扱う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 画面・見た目の正本を確認する: `../../design/DESIGN.md`
- 機能仕様に関わる場合: `../../features/`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は、ユーザーが明示したP19のトピックに限る。
- Markdownファイルを正本として扱う前提を変えない。
- 既存のMarkdown保存形式、ローカルファイル操作仕様、画面仕様、ユーザー操作の意味をユーザー確認なしに変更しない。
- 機能改善、修正、追加はいずれも小さな単位で進め、作業前に対象、内容、除外範囲、進め方を確認する。
- 新規機能追加や仕様変更は、ユーザーの明示指示または合意がある場合だけ扱う。
- 画面全体の方針変更、保存形式変更、大規模アーキテクチャ変更は、事前に理由と選択肢を示してユーザー確認を取る。
- 仕様書、設計書、チェックリスト、詳細な作業項目は、ユーザーの明示指示または合意なしに作成しない。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。
- `../phases.md` の共通方針に従い、このフェーズでもP19のトピックを主テーマとしつつ、プロジェクト全体に関わる整理・修正・不具合修正をサブテーマとして扱う。

---

## 対象範囲

- 既存機能の使い勝手改善
- 既存機能の小規模な表示・操作改善
- ユーザーが指定した不具合修正
- ユーザーが指定したGitHub Issue対応
- ユーザー確認済みの小規模な機能追加
- 既存挙動を守るためのテスト追加・補強
- P19中に見つかった小規模な不具合修正
- プロジェクト全体に関わる整理・修正・不具合修正
- 変更内容に必要な範囲での文書更新

対象外:

- ユーザー確認なしの新規機能追加
- ユーザー確認なしのMarkdown保存形式変更
- ユーザー確認なしのローカルファイル操作仕様変更
- ユーザー確認なしの画面全体のデザイン方針変更
- ユーザー確認なしの大規模アーキテクチャ変更
- 配布ビルド確認

## 作業記録

このフェーズでは、P19の作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-06-17 13:19 JST

- ユーザー指示により、P19 機能改善・修正・追加フェーズを開始した。P19ではMarkdownファイルを正本として扱う前提と既存の保存形式を保ちながら、ユーザーが指定した機能改善、不具合修正、必要な小規模追加を扱う。今回のフェーズ移行作業として `docs/development/phases.md` の現在フェーズをP19へ変更し、P18を終了扱いにする。作業記録更新に伴い `app/package.json` のバージョンを `0.3.58` へ上げる。

### 2026-06-17 14:00 JST

- 自由図に領域図形とNodeレイヤーを追加し、判断図形から接続するLineには標準選択肢ラベルを自動入力するようにした。Line経路は同じ行き先または空ではないラベル完全一致の場合に共有できるようにし、PDF出力時の判断図形アウトラインを維持するよう修正した。`docs/features/diagrams.md` に自由図の保存形式と操作仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.59` へ上げた。

### 2026-06-17 14:29 JST

- 自由図のレイヤー表示を安全な表示階層へ変換し、背面寄りの領域図形が操作不能にならないよう修正した。領域図形の移動時は完全に内包された図形も同じ距離だけ移動するようにし、領域図形は領域名表示、その他の図形は中央揃えテキスト表示に調整した。`docs/features/diagrams.md` に領域図形の内包、領域名、レイヤー変更の扱いを反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.60` へ上げた。

### 2026-06-17 19:25 JST

- 自由図のレイヤーを、領域図形は常に `0`、領域図形以外は `1` 以上として扱うよう修正した。既存保存値は読み込み時に現在ルールへ正規化し、領域図形ではレイヤー操作を表示しないようにした。レイヤーが高い図形は位置をずらさず影を強めて浮いて見えるよう調整し、`docs/features/diagrams.md` に自由図レイヤー仕様を反映した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.61` へ上げた。

### 2026-06-17 19:37 JST

- 自由図の選択中表示でも保存された図形レイヤー順を維持するよう修正した。Lineは接続している図形のうち高い方のレイヤーと同じ表示扱いにし、Lineラベルと接続中プレビューも対応するレイヤーへそろえた。`docs/features/diagrams.md` に選択中とLineの表示レイヤー仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.62` へ上げた。

### 2026-06-17 19:53 JST

- 自由図の判断図形と入出力図形で、選択中やドラッグ中に図形外側の四角い領域枠が見えないよう修正した。非矩形図形の選択・ドラッグ時は図形本体の強調だけを表示するようにし、`docs/features/diagrams.md` に非矩形図形の領域枠非表示を反映した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.63` へ上げた。

### 2026-06-17 20:43 JST

- 自由図の判断図形について、入ってくるLineを平均方向に近い1つの角へ集約し、出ていくLineは入力角を避けて行き先方向に近い角から出すよう修正した。判断図形から出るLineは2本まで追加できるようにし、既存ファイルに3本目以降がある場合は表示・操作対象から外した。`docs/features/diagrams.md` に判断図形のLine本数と角接続仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.64` へ上げた。

### 2026-06-17 21:01 JST

- 自由図の判断図形から出る2本のLineが、入力角を避けたうえで互いに別々の角から出るように修正した。`docs/features/diagrams.md` に出力Line同士は別角から出す仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.65` へ上げた。

### 2026-06-17 21:06 JST

- 自由図の判断図形から新しく出すLineの標準ラベルを `はい` / `いいえ` から `YES` / `NO` へ変更した。既存Lineラベルの自動変換は行わず、`docs/features/diagrams.md` に標準ラベル仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.66` へ上げた。

### 2026-06-17 21:18 JST

- 自由図のレイヤー表示について、レイヤーが高い図形ほど影に加えて縁取りと下側の奥行き表現が強くなるよう調整した。判断図形と入出力図形は図形本体の疑似要素に影を当て、四角い外枠ではなく図形の形に沿って浮いて見えるようにした。`docs/features/diagrams.md` に自由図レイヤーの見た目仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.67` へ上げた。

### 2026-06-17 21:27 JST

- 自由図で領域図形以外の図形を選択している間だけ現在レイヤーを表示し、レイヤー変更直後に選択図形とレイヤー表示を短く強調するよう修正した。最背面の図形では背面へ送るボタンを無効化し、これ以上下げられないことが分かるようにした。`docs/features/diagrams.md` に選択中レイヤー表示と操作フィードバック仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.68` へ上げた。

### 2026-06-17 21:35 JST

- 自由図の領域図形以外のレイヤー上限を `8` にし、既存ファイルなどで `9` 以上が保存されている場合は `8` として扱うよう修正した。`レイヤー 8` では前面へ出すボタンを無効化し、レイヤー1段ごとの影・縁取り・下側の奥行き表現を強めた。選択図形を基準に他の図形が上か下か分かるよう、上下関係に応じた表示状態を追加した。`docs/features/diagrams.md` に自由図レイヤー上限と上下関係の表示仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.69` へ上げた。

### 2026-06-17 21:59 JST

- 自由図のレイヤーを固定3層に変更し、領域図形を `0`、領域以外の図形本体を `1`、図形に付くラベルを表示上の `2` として扱うよう修正した。Lineラベルは図形ラベルとは別扱いにし、Line専用レイヤーは保存しないまま表示時に決めるよう維持した。前面/背面レイヤー操作、レイヤー表示、レイヤー差分の見た目を削除し、PDF/HTML出力と `docs/features/diagrams.md` も固定3層仕様へ更新した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.70` へ上げた。

### 2026-06-17 22:08 JST

- 自由図の図形種別から `note` を削除し、新しい図形種別として `label` を追加した。自由図のサイドバー、接続追加メニュー、既定テキスト、画面表示、HTML/PDF出力、仕様書を `ラベル` にそろえ、`shape: note` は自由図の保存形式として受け付けないようにした。Lineに付く表示はLineラベルとして明記し、図形に付くラベルとは別扱いのまま維持した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.71` へ上げた。

### 2026-06-17 22:36 JST

- Diagramモードの新規作成を図解ファイル1種類へ統一し、Diagram判定を `type: diagram` のみに変更した。Relationship、構造ツリー、自由図の作成分岐と旧 `type` 互換を外し、サイドバー、キャンバス表示、HTML出力、関連テスト、仕様書を図解ファイル仕様へそろえた。作業記録更新に伴い `app/package.json` のバージョンを `0.3.72` へ上げた。

### 2026-06-18 07:04 JST

- Diagram型定義を `diagram` のみに整理し、未使用の配置可能Markdown文言と構造ツリー表示名パネル文言を削除した。旧 `type` は拒否確認テストと仕様上の非対応明記だけに残る状態を確認した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.73` へ上げた。

### 2026-06-18 07:19 JST

- 旧Relationshipと構造ツリーの未使用実装、Mermaid変換、HTML出力分岐、CSS、スキップ済みテストを削除し、図解キャンバスの内部名とテスト名をDiagram仕様へ整理した。`docs/engineering/data-model.md` の図形種別を実装どおり `area` に修正し、対象テストでスキップが0件になることを確認した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.74` へ上げた。

### 2026-06-18 08:02 JST

- GitHub Issue #42, #43, #46, #48, #51 に対応し、PDF/印刷HTML入力のサイズ上限、D2描画キューのタイムアウト後直列化、ワークスペース状態構築時のファイルツリー二重走査削減、設定保存IPC失敗時のUIエラー反映、チャート・暦設定保存後返却値の正規化済みデータ一致を実装した。`pnpm typecheck`、関連テスト66件、`pnpm test` で全130ファイル1107件、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.75` へ上げた。

### 2026-06-18 08:46 JST

- GitHub Issue #44, #52, #53, #55, #59, #77, #79, #82, #83, #87 に対応し、検索の遅延・キャッシュ指定・長大行スキップ表示、出力SVGサニタイズ分離、ワークスペースID変更時の旧設定削除、プレビュー埋め込みと出力図描画の同時実行制限、WikiLink生成名の安全化、終了確認タイムアウト時の待受解除確認、印刷プレビュー一時ディレクトリのプロセス単位化、Markdown拡張登録の一度きり化を実施した。`pnpm typecheck`、関連テスト115件、`pnpm test` で全134ファイル1125件、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.76` へ上げた。

### 2026-06-18 09:00 JST

- GitHub Issue #66, #67, #68, #72 に対応し、出力CSS分離、文字数・単語数カウントの不要再計算抑制、一括置換プレビューの上限と読み取り不能ファイル通知、一括置換適用結果の読み取り不能ファイル通知を実装した。`pnpm typecheck`、関連テスト46件、`pnpm test` で全135ファイル1128件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.77` へ上げた。

### 2026-06-18 09:06 JST

- GitHub Issue #64, #70, #74, #75 に対応し、内部リンク更新影響確認の読み取り不能ファイル通知、大きめMarkdown判定の不要再計算抑制、ファイル加工ツール生成Markdown見出しの安全化、PDF/印刷HTMLのmain側CSP・危険要素検証を実装した。`pnpm typecheck`、関連テスト107件、`pnpm test` で全135ファイル1135件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.78` へ上げた。

### 2026-06-18 09:12 JST

- GitHub Issue #76, #90, #92, #81 に対応し、検索インデックスキャッシュから本文行保存を削除してcontent hashによる鮮度確認を追加し、内部atomic write一時ファイル由来のworkspaceWatcher通知を抑制し、一括置換プレビュー後の対象ファイル変更を検出して適用を止めるようにした。`pnpm typecheck`、関連テスト66件、`pnpm test` で全135ファイル1142件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.79` へ上げた。

### 2026-06-18 09:18 JST

- GitHub Issue #47, #49, #63, #89 に対応し、破損したapp/workspace設定JSONを既定値へ黙って戻さず退避してエラーにし、設定更新を直列化し、Markdownファイル移動時に移動対象ファイル内のbasename-onlyリンクの意味を維持し、内部リンク更新走査を候補ファイルに絞るようにした。`pnpm typecheck`、関連テスト92件、`pnpm test` で全135ファイル1149件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.80` へ上げた。

### 2026-06-18 09:27 JST

- GitHub Issue #56, #61, #73, #80 に対応し、大規模ワークスペース処理の無制限 `Promise.all` を上限付き並列へ置き換え、App/Editorの肥大化テストを機能別ファイルへ分割し、大文字小文字を区別しない環境で同じワークスペースが重複登録されにくいID正規化と既存ID維持を追加し、大文字小文字だけのファイル/フォルダリネームを安全に扱うようにした。`pnpm typecheck`、関連テスト133件、`pnpm test` で全137ファイル1156件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.81` へ上げた。

### 2026-06-18 18:37 JST

- ユーザー指示により、P19の対象範囲にユーザーが指定したGitHub Issue対応を追加した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.82` へ上げた。

### 2026-06-18 18:54 JST

- GitHub Issue #93, #94, #95, #96, #97 に対応し、ワークスペース状態構築時の検索本文再読込抑制、Mermaid/D2描画結果の再利用、Markdownプレビュー・ハイライト・KaTeX結果の再利用、既定除外ディレクトリによるファイルツリー走査削減、rendererサイズ計測コマンド追加を実装した。`pnpm test -- src/main/files/workspaceFileIndex.test.ts src/main/files/fileTree.test.ts src/renderer/diagramPreview.test.ts src/renderer/previewMarkdown.test.ts`、`pnpm typecheck`、`pnpm build:size`、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.83` へ上げた。

### 2026-06-18 20:14 JST

- GitHub Issue #98, #99, #100, #101 に対応し、app/workspace設定とDiagram保存形式にバージョンを追加して未知の将来形式を旧形式として誤読しないようにし、WikiLink解析・走査・書き戻しを共通化し、チャート更新をpreload API契約確認付きのmain側IPCへ一本化してrenderer側ファイル読み書きfallbackを削除した。`pnpm typecheck`、`pnpm test -- src/main/settings/appSettings.test.ts src/main/settings/workspaceSettings.test.ts src/shared/links.test.ts src/main/files/linkUpdaterModel.test.ts src/renderer/previewMarkdown.test.ts src/renderer/editorLivePreviewModel.test.ts src/shared/diagramMarkdown.test.ts src/renderer/chartData.test.ts src/renderer/hooks/useWorkspaceCharts.test.tsx src/renderer/App.dateCharts.test.tsx`、`pnpm verify` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.84` へ上げた。

### 2026-06-18 20:45 JST

- 図解の新規図形について、領域図形以外の初期サイズとキャンバス上の追加位置計算を横5マス、縦2マスへ変更した。領域図形の初期サイズは変更せず、`docs/features/diagrams.md` と追加操作テストに反映した。`pnpm test -- src/shared/diagramMarkdown.test.ts src/renderer/components/DiagramCanvas.test.tsx`、`pnpm typecheck`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.85` へ上げた。

### 2026-06-18 20:54 JST

- 図解の領域図形について、図形全体を薄くせず塗りだけが半透明になるよう画面表示と出力CSSを変更した。`docs/features/diagrams.md` と表示CSSテストに反映した。`pnpm test -- src/renderer/components/DiagramCanvas.test.tsx src/renderer/outputHtml.test.ts`、`pnpm typecheck`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.86` へ上げた。

### 2026-06-18 21:11 JST

- 図解のラベル図形を他図形への注釈専用として扱い、ラベル図形を含む接続を通常Lineではなく吹き出し状の注釈連結として表示するよう変更した。注釈連結ではLineラベル、矢印、向き反転操作を表示せず、判断図形の通常Line本数にも含めないようにした。`docs/features/diagrams.md` と関連テストに反映した。`pnpm test -- src/shared/diagramMarkdown.test.ts src/renderer/components/diagram/diagramGeometry.test.ts src/renderer/components/DiagramCanvas.test.tsx src/renderer/outputHtml.test.ts`、`pnpm typecheck`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.87` へ上げた。

### 2026-06-18 21:30 JST

- 図解のラベル図形について、吹き出しとして見えるようラベル本体の角丸・背景・影を調整し、注釈線の接続先に小さなアンカーを表示するよう画面表示と出力HTML/CSSを変更した。`docs/features/diagrams.md` と関連テストに反映した。`pnpm exec vitest run src/renderer/components/DiagramCanvas.test.tsx src/renderer/outputHtml.test.ts`、`pnpm typecheck`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.88` へ上げた。

### 2026-06-18 21:42 JST

- 図解のラベル図形について、小さなアンカー表示をやめ、接続方向に応じて上下左右へ三角のしっぽを出す吹き出し形状へ変更した。画面表示、出力HTML/CSS、`docs/features/diagrams.md`、関連テストに反映した。`pnpm exec vitest run src/renderer/components/DiagramCanvas.test.tsx src/renderer/outputHtml.test.ts src/renderer/components/diagram/diagramGeometry.test.ts`、`pnpm typecheck`、`git diff --check`、開発版レンダラー `http://127.0.0.1:5173/` の読み込み確認が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.89` へ上げた。

### 2026-06-18 22:04 JST

- 図解のラベル図形を廃止し、図形一覧、接続追加候補、保存形式、画面描画、HTML出力、CSS、翻訳、関連テストから `label` 図形と注釈連結専用処理を削除した。Lineラベル機能は維持し、`shape: label` を含むDiagramは不正な形式として拒否するよう確認した。`docs/features/diagrams.md` に現行仕様を反映した。`pnpm exec vitest run src/shared/diagramMarkdown.test.ts src/renderer/components/diagram/diagramGeometry.test.ts src/renderer/components/DiagramCanvas.test.tsx src/renderer/components/DiagramSidebar.test.tsx src/renderer/outputHtml.test.ts`、`pnpm typecheck`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.90` へ上げた。

### 2026-06-18 22:17 JST

- GitHub Issue #102, #103, #104, #105, #106, #107, #108, #109, #110 に対応し、チャート旧payload互換とrenderer側Markdown再読込を削除し、設定schema移行成功時の現行schema書き戻し、リンクパネル非表示時のWikiLink/バックリンク停止、バックリンクresolver再利用と上限付き読込、aliases索引の差分再利用と上限、WikiLink走査のマスク文字列削除、アウトゴーイングリンク解析上限、renderer側resolver再利用を実装した。`pnpm exec vitest run src/renderer/chartData.test.ts src/renderer/hooks/useWorkspaceCharts.test.tsx src/renderer/App.dateCharts.test.tsx src/main/settings/appSettings.test.ts src/main/settings/workspaceSettings.test.ts src/main/files/backlinks.test.ts src/main/files/aliases.test.ts src/shared/links.test.ts src/renderer/App.searchLinks.test.tsx`、`pnpm typecheck`、`pnpm test` で全138ファイル1182件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.91` へ上げた。

### 2026-06-18 22:38 JST

- GitHub Issue #111 に対応し、Diagramキャンバスのファイル別表示状態リセット、表示操作ツールバー、Undo/Redo、拒否理由表示、複数選択、複数移動・削除・複製・コピー・貼り付け・整列・等間隔配置、キーボード操作、図形種類変更を実装した。`docs/features/diagrams.md` に操作仕様を反映し、`pnpm exec vitest run src/shared/diagramMarkdown.test.ts src/renderer/components/DiagramCanvas.test.tsx`、`pnpm typecheck`、`pnpm test` で全138ファイル1188件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.92` へ上げた。

### 2026-06-18 23:11 JST

- GitHub Issue #112, #113 に対応し、Diagram解析エラーの原因・分類・詳細・コピー・ソースモード導線表示と、選択Lineの始点/終点付け替えハンドル、接続可否表示、共有Line端点更新処理を実装した。`docs/features/diagrams.md` に無効Diagram表示とLine端点付け替え仕様を反映し、`pnpm exec vitest run src/shared/diagramMarkdown.test.ts src/renderer/components/DiagramCanvas.test.tsx src/renderer/components/PaneContentSurface.test.tsx src/renderer/components/diagram/diagramGeometry.test.ts src/renderer/outputHtml.test.ts`、`pnpm typecheck`、`pnpm test` で全138ファイル1195件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.93` へ上げる。

### 2026-06-19 06:17 JST

- GitHub Issue #114, #115, #116 に対応し、Diagram図形パレットを基本フローと構造へ再編し、クリック・キーボード・ドラッグで図形を追加できるようにした。空キャンバスには開始パネルと図形選択メニューを追加し、固定ツールバーをキャンバス操作だけへ絞り、Node、複数Node、Lineの選択時操作をコンテキストツールバーへ分離した。`docs/features/diagrams.md` に現行操作仕様を反映し、`pnpm exec vitest run src/renderer/components/DiagramSidebar.test.tsx src/renderer/components/DiagramCanvas.test.tsx`、`pnpm typecheck`、`pnpm test` で全138ファイル1201件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.94` へ上げる。

### 2026-06-19 06:38 JST

- GitHub Issue #117, #118 に対応し、Diagramファイルの大見出しをコンパクトヘッダーへ置き換え、既存ソースモードアイコンでキャンバス/ソース切替を維持した。Diagramステータスバーを選択状態別の操作ガイドへ変更し、図形/線の件数表示、通知読み上げ、キャンバスと図形の読み上げ情報を画面表示名に合わせた。`docs/features/diagrams.md` に現行表示・操作・読み上げ仕様を反映し、`pnpm exec vitest run src/renderer/components/PaneContentSurface.test.tsx src/renderer/components/DiagramCanvas.test.tsx src/renderer/components/PaneView.test.tsx`、`pnpm exec vitest run src/renderer/App.fileTabs.test.tsx`、`pnpm typecheck`、`pnpm test` で全138ファイル1205件、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.95` へ上げる。

### 2026-06-19 07:30 JST

- GitHub Issue #119, #120, #121, #122, #123, #124, #125 に対応し、DiagramのNode移動・リサイズ・テキスト直接編集・接続ポート・Line端点ハンドルを視覚化し、選択時フローティング文字ツールバーを右クリックまたは `Shift+F10` の明示的メニューへ集約した。固定ツールバーは標準的なアイコン中心にし、操作説明文をステータス表示から除外して状態表示と拒否理由表示へ分離した。`docs/features/diagrams.md` に現行操作仕様を反映し、`pnpm exec vitest run src/renderer/components/DiagramCanvas.test.tsx`、`pnpm typecheck`、`pnpm verify`、`git diff --check` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.96` へ上げる。
