# P19

Relicの機能改善・修正・追加フェーズの正本。

このフェーズでは、Markdownファイルを正本として扱う前提と既存の保存形式を守りながら、ユーザーが指定した機能改善、不具合修正、必要な小規模追加を扱う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 画面・見た目の正本を確認する: `../../design/DESIGN.md`
- 機能仕様に関わる場合: `../../features/`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は、ユーザーが明示したP19のトピックに限る。
- Markdownファイルを正本として扱う前提を変えない。
- 既存のMarkdown保存形式、ローカルファイル操作仕様、画面仕様、ユーザー操作の意味をユーザー確認なしに変更しない。
- 機能改善、修正、追加はいずれも小さな単位で進め、作業前に対象、内容、除外範囲、進め方を確認する。
- 新規機能追加や仕様変更は、ユーザーの明示指示または合意がある場合だけ扱う。
- 画面全体の方針変更、保存形式変更、大規模アーキテクチャ変更は、事前に理由と選択肢を示してユーザー確認を取る。
- 仕様書、設計書、チェックリスト、詳細な作業項目は、ユーザーの明示指示または合意なしに作成しない。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。
- `../phases.md` の共通方針に従い、このフェーズでもP19のトピックを主テーマとしつつ、プロジェクト全体に関わる整理・修正・不具合修正をサブテーマとして扱う。

---

## 対象範囲

- 既存機能の使い勝手改善
- 既存機能の小規模な表示・操作改善
- ユーザーが指定した不具合修正
- ユーザー確認済みの小規模な機能追加
- 既存挙動を守るためのテスト追加・補強
- P19中に見つかった小規模な不具合修正
- プロジェクト全体に関わる整理・修正・不具合修正
- 変更内容に必要な範囲での文書更新

対象外:

- ユーザー確認なしの新規機能追加
- ユーザー確認なしのMarkdown保存形式変更
- ユーザー確認なしのローカルファイル操作仕様変更
- ユーザー確認なしの画面全体のデザイン方針変更
- ユーザー確認なしの大規模アーキテクチャ変更
- 配布ビルド確認

## 作業記録

このフェーズでは、P19の作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-06-17 13:19 JST

- ユーザー指示により、P19 機能改善・修正・追加フェーズを開始した。P19ではMarkdownファイルを正本として扱う前提と既存の保存形式を保ちながら、ユーザーが指定した機能改善、不具合修正、必要な小規模追加を扱う。今回のフェーズ移行作業として `docs/development/phases.md` の現在フェーズをP19へ変更し、P18を終了扱いにする。作業記録更新に伴い `app/package.json` のバージョンを `0.3.58` へ上げる。

### 2026-06-17 14:00 JST

- 自由図に領域図形とNodeレイヤーを追加し、判断図形から接続するLineには標準選択肢ラベルを自動入力するようにした。Line経路は同じ行き先または空ではないラベル完全一致の場合に共有できるようにし、PDF出力時の判断図形アウトラインを維持するよう修正した。`docs/features/diagrams.md` に自由図の保存形式と操作仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.59` へ上げた。

### 2026-06-17 14:29 JST

- 自由図のレイヤー表示を安全な表示階層へ変換し、背面寄りの領域図形が操作不能にならないよう修正した。領域図形の移動時は完全に内包された図形も同じ距離だけ移動するようにし、領域図形は領域名表示、その他の図形は中央揃えテキスト表示に調整した。`docs/features/diagrams.md` に領域図形の内包、領域名、レイヤー変更の扱いを反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.60` へ上げた。

### 2026-06-17 19:25 JST

- 自由図のレイヤーを、領域図形は常に `0`、領域図形以外は `1` 以上として扱うよう修正した。既存保存値は読み込み時に現在ルールへ正規化し、領域図形ではレイヤー操作を表示しないようにした。レイヤーが高い図形は位置をずらさず影を強めて浮いて見えるよう調整し、`docs/features/diagrams.md` に自由図レイヤー仕様を反映した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.61` へ上げた。

### 2026-06-17 19:37 JST

- 自由図の選択中表示でも保存された図形レイヤー順を維持するよう修正した。Lineは接続している図形のうち高い方のレイヤーと同じ表示扱いにし、Lineラベルと接続中プレビューも対応するレイヤーへそろえた。`docs/features/diagrams.md` に選択中とLineの表示レイヤー仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.62` へ上げた。

### 2026-06-17 19:53 JST

- 自由図の判断図形と入出力図形で、選択中やドラッグ中に図形外側の四角い領域枠が見えないよう修正した。非矩形図形の選択・ドラッグ時は図形本体の強調だけを表示するようにし、`docs/features/diagrams.md` に非矩形図形の領域枠非表示を反映した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.63` へ上げた。

### 2026-06-17 20:43 JST

- 自由図の判断図形について、入ってくるLineを平均方向に近い1つの角へ集約し、出ていくLineは入力角を避けて行き先方向に近い角から出すよう修正した。判断図形から出るLineは2本まで追加できるようにし、既存ファイルに3本目以降がある場合は表示・操作対象から外した。`docs/features/diagrams.md` に判断図形のLine本数と角接続仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.64` へ上げた。

### 2026-06-17 21:01 JST

- 自由図の判断図形から出る2本のLineが、入力角を避けたうえで互いに別々の角から出るように修正した。`docs/features/diagrams.md` に出力Line同士は別角から出す仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.65` へ上げた。

### 2026-06-17 21:06 JST

- 自由図の判断図形から新しく出すLineの標準ラベルを `はい` / `いいえ` から `YES` / `NO` へ変更した。既存Lineラベルの自動変換は行わず、`docs/features/diagrams.md` に標準ラベル仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.66` へ上げた。

### 2026-06-17 21:18 JST

- 自由図のレイヤー表示について、レイヤーが高い図形ほど影に加えて縁取りと下側の奥行き表現が強くなるよう調整した。判断図形と入出力図形は図形本体の疑似要素に影を当て、四角い外枠ではなく図形の形に沿って浮いて見えるようにした。`docs/features/diagrams.md` に自由図レイヤーの見た目仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.67` へ上げた。

### 2026-06-17 21:27 JST

- 自由図で領域図形以外の図形を選択している間だけ現在レイヤーを表示し、レイヤー変更直後に選択図形とレイヤー表示を短く強調するよう修正した。最背面の図形では背面へ送るボタンを無効化し、これ以上下げられないことが分かるようにした。`docs/features/diagrams.md` に選択中レイヤー表示と操作フィードバック仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.68` へ上げた。

### 2026-06-17 21:35 JST

- 自由図の領域図形以外のレイヤー上限を `8` にし、既存ファイルなどで `9` 以上が保存されている場合は `8` として扱うよう修正した。`レイヤー 8` では前面へ出すボタンを無効化し、レイヤー1段ごとの影・縁取り・下側の奥行き表現を強めた。選択図形を基準に他の図形が上か下か分かるよう、上下関係に応じた表示状態を追加した。`docs/features/diagrams.md` に自由図レイヤー上限と上下関係の表示仕様を反映し、作業記録更新に伴い `app/package.json` のバージョンを `0.3.69` へ上げた。

### 2026-06-17 21:59 JST

- 自由図のレイヤーを固定3層に変更し、領域図形を `0`、領域以外の図形本体を `1`、図形に付くラベルを表示上の `2` として扱うよう修正した。Lineラベルは図形ラベルとは別扱いにし、Line専用レイヤーは保存しないまま表示時に決めるよう維持した。前面/背面レイヤー操作、レイヤー表示、レイヤー差分の見た目を削除し、PDF/HTML出力と `docs/features/diagrams.md` も固定3層仕様へ更新した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.70` へ上げた。

### 2026-06-17 22:08 JST

- 自由図の図形種別から `note` を削除し、新しい図形種別として `label` を追加した。自由図のサイドバー、接続追加メニュー、既定テキスト、画面表示、HTML/PDF出力、仕様書を `ラベル` にそろえ、`shape: note` は自由図の保存形式として受け付けないようにした。Lineに付く表示はLineラベルとして明記し、図形に付くラベルとは別扱いのまま維持した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.71` へ上げた。

### 2026-06-17 22:36 JST

- Diagramモードの新規作成を図解ファイル1種類へ統一し、Diagram判定を `type: diagram` のみに変更した。Relationship、構造ツリー、自由図の作成分岐と旧 `type` 互換を外し、サイドバー、キャンバス表示、HTML出力、関連テスト、仕様書を図解ファイル仕様へそろえた。作業記録更新に伴い `app/package.json` のバージョンを `0.3.72` へ上げた。

### 2026-06-18 07:04 JST

- Diagram型定義を `diagram` のみに整理し、未使用の配置可能Markdown文言と構造ツリー表示名パネル文言を削除した。旧 `type` は拒否確認テストと仕様上の非対応明記だけに残る状態を確認した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.73` へ上げた。

### 2026-06-18 07:19 JST

- 旧Relationshipと構造ツリーの未使用実装、Mermaid変換、HTML出力分岐、CSS、スキップ済みテストを削除し、図解キャンバスの内部名とテスト名をDiagram仕様へ整理した。`docs/engineering/data-model.md` の図形種別を実装どおり `area` に修正し、対象テストでスキップが0件になることを確認した。作業記録更新に伴い `app/package.json` のバージョンを `0.3.74` へ上げた。
