# P15

Relicのカレンダーと年表機能の全般フェーズの正本。

このフェーズでは、既存のMarkdown正本の扱いを保ちながら、カレンダーと年表機能について、小さな単位で機能改善・不具合修正・UI調整・テスト補強を行う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 画面・見た目の正本を確認する: `../../design/DESIGN.md`
- 機能仕様に関わる場合: `../../features/`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は、ユーザーが明示したカレンダーと年表機能の全般に限る。
- Markdownファイルを正本として扱う前提を変えない。
- Markdown保存形式、日付・暦・年表データの意味、ローカルファイル操作仕様をユーザー確認なしに変更しない。
- カレンダーと年表の表示・操作・データ読み取り・設定・テストは、既存仕様と現在の実装を確認してから小さな単位で変更する。
- 大きな設計変更、新規大型機能追加、画面全体の方針変更は、事前に理由と選択肢を示してユーザー確認を取る。
- 仕様書、設計書、チェックリスト、詳細な作業項目は、ユーザーの明示指示または合意なしに作成しない。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。

---

## 対象範囲

- カレンダー機能の機能改善
- カレンダー機能の不具合修正
- 年表機能の機能改善
- 年表機能の不具合修正
- カレンダー・年表に関わるUI・エラー表示の小規模改善
- カレンダー・年表に関わる日付・暦・表示設定の小規模改善
- カレンダー・年表に関わるテストの追加・補強
- 文書の最小限の整合性更新

対象外:

- ユーザー確認なしの新規大型機能追加
- ユーザー確認なしのMarkdown保存形式変更
- ユーザー確認なしの日付・暦・年表データ意味変更
- ユーザー確認なしのローカルファイル操作仕様変更
- ユーザー確認なしの画面全体のデザイン方針変更
- 配布ビルド確認

## 作業記録

このフェーズでは、カレンダーと年表機能の全般に関わる作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-06-07 22:30 JST

- ユーザー指示により、P15 カレンダーと年表機能の全般フェーズを開始した。P15では既存のMarkdown正本の扱いを保ちながら、カレンダーと年表機能について、小さな単位で機能改善・不具合修正・UI調整・テスト補強を行う。今回のフェーズ移行作業として `docs/development/phases.md` の現在フェーズをP15へ変更し、P14を終了扱いにする。作業記録更新に伴い `app/package.json` のバージョンを `0.2.150` へ上げる。

### 2026-06-07 22:45 JST

- ユーザー指示により、年表とカレンダー機能を確認しやすくするため、`/Users/akihisa/RelicTestWorkspaces` の日本語ワークスペースと英語ワークスペースへ、計画のみ、計画と実績の差分、複数年の年表期間、実績のみ単日を確認できるMarkdownサンプルを各4件追加した。追加ファイルの主要な `chronicle0`、`plannedDate`、`actualDate` を確認した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.151` へ上げる。

### 2026-06-08 16:01 JST

- ユーザー指示により、年表表示だけを対象に検索入力、並び替え選択、並び順更新ボタンを表示しないようにした。カレンダー表示の検索、並び替え、ステータス絞り込み、今日ボタンは残した。年表表示では隠れた検索語や並び替え状態が行表示に影響しないようにし、対象テスト、`pnpm typecheck`、差分チェックが成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.152` へ上げる。

### 2026-06-08 16:25 JST

- ユーザー指示により、年表表示では検索・並び替え削除後の空ツールバー領域を表示せず、横ミニマップと縦ミニマップ用パネルも表示しないようにした。カレンダー表示のツールバー、横ミニマップ、縦ミニマップは残した。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.153` へ上げる。

### 2026-06-08 16:41 JST

- ユーザー指示により、年表表示だけを1行の横長タイムラインにし、ファイル名列と年数表示列を表示しないようにした。年表バーは同じ帯に重ねて表示し、カレンダー表示の左列と複数行表示は残した。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.154` へ上げる。

### 2026-06-08 16:45 JST

- ユーザー指示により、年表表示だけを対象に、1行タイムラインの表示帯と年表バーの高さを従来行高の8倍にした。カレンダー表示の行高は変更していない。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.155` へ上げる。

### 2026-06-08 16:53 JST

- ユーザー指示により、年表表示だけを対象に、表示帯を12行相当にし、バーの開始・終了境界で区間分割して、各区間の重なり本数に応じて縦幅を均等分割する表示にした。重なりなし区間は12行ぶん全体を使い、カレンダー表示は変更していない。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.156` へ上げる。

### 2026-06-08 17:00 JST

- ユーザー指示により、年表表示だけを対象に、既存の青系トーンに合わせた12色の固定色をファイルパスから自動割り当てするようにした。同じファイルの分割バーは同じ色になり、12色を超える場合は再利用される。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.157` へ上げる。

### 2026-06-08 17:03 JST

- ユーザー指摘により、年表バーの固定12色を青寄りの連続色から、明度・彩度・透明度を揃えた多色パレットへ変更した。同じファイルの分割バーは同じ色のまま、色相は青、緑、黄、橙、赤紫、紫などへ広げた。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.158` へ上げる。

### 2026-06-08 17:15 JST

- ユーザー指示により、年表表示だけを対象に、固定12行・固定縦幅をやめ、同時に重なる最大本数から行数と縦幅を動的に決めるようにした。同じファイルの隣接する分割区間は内部の角丸とラベル重複を抑え、連続した塊として見えるようにした。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.159` へ上げる。

### 2026-06-08 17:57 JST

- ユーザー指摘により、年表表示だけを対象に、重なり区間で同じファイルのバーが上下へ散らばりにくいよう、ファイルごとの縦位置を優先して重なり順を決めるようにした。重なりなし区間は表示領域全体を使う挙動を維持した。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.160` へ上げる。

### 2026-06-08 18:06 JST

- ユーザー指示により、年表表示だけを対象に、縦方向の行グリッドと重なり区間ごとの行分割をやめ、横方向の年グリッドだけを残した。年表バーは1ファイル1本の塊として同じ表示面に重ねて描画し、同じファイルのバーが区間ごとに散らばらないようにした。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.161` へ上げる。

### 2026-06-08 18:12 JST

- ユーザー指示により、年表表示だけを対象に、縦方向の行グリッドは出さないまま、バー同士が重なる年区間だけ表示面内で縦分割するようにした。重なっていない区間は表示面全体を使い、同じファイルは重なり区間で同じ縦位置に寄せる。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.162` へ上げる。

### 2026-06-08 18:29 JST

- ユーザー指示により、年表表示だけを対象に、バー本体をHTMLボタン描画からSVG描画へ移し、クリック・ドラッグ・リサイズ操作をSVG要素に接続した。年表の縦幅は固定値ではなく最大重なり本数から動的に決め、横方向の年グリッドと重なり区間だけ縦分割する表示を維持した。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.163` へ上げる。

### 2026-06-08 18:42 JST

- ユーザー指示により、年表表示だけを対象に、重なり区間ごとのSVG矩形を同じファイルごとにまとめ、1ファイル1つのSVGグループと1つのpath図形として描画するようにした。重なり区間のクリック・ドラッグ範囲は透明pathで維持し、ラベルとリサイズ操作を同じグループに残した。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.164` へ上げる。

### 2026-06-08 18:59 JST

- ユーザー指示により、年表表示だけを対象に、SVG描画は維持したまま、1ファイル1本の連続バーを重なり回避レーンへ自動配置する方式に変更した。重ならないバーは同じレーンを再利用し、必要なレーン数に応じて年表の縦幅を伸ばす。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.165` へ上げる。

### 2026-06-08 19:07 JST

- ユーザー指示により、年表表示だけを対象に、チャート表示領域の利用可能な高さを年表SVGへ渡し、重なり回避レーンを表示領域いっぱいに均等配分するようにした。必要レーン数が多い場合は最小レーン高を維持して縦方向に伸びる。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.166` へ上げる。

### 2026-06-08 19:14 JST

- ユーザー指示により、年表表示だけを対象に、SVGバー内の年数ラベルの上にファイル名ラベルを表示するようにした。ファイル名ラベルは年数ラベルと同じバー内に配置し、背景と文字色を分けて見分けやすくした。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.167` へ上げる。

### 2026-06-08 19:25 JST

- ユーザー指示により、年表表示だけを対象に、SVGバーのファイル名ラベルと年数ラベルをバー内の表示幅でクリップし、隣接するバーのラベルへはみ出さないようにした。長さ変更中のドラッグpreviewには編集種別を持たせ、resize中は元の範囲でレーン判定を固定し、バーの横幅だけをpreviewで更新するようにした。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.168` へ上げる。

### 2026-06-08 19:34 JST

- ユーザー指示により、年表表示だけを対象に、SVGバーのラベルを見切らず全文表示する方式へ変更した。バー内に収まり、既存ラベルと重ならない場合はバー内へ表示し、狭いバーや他ラベルと重なる場合はリーダー線を引いて外側へ離して表示するようにした。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.169` へ上げる。

### 2026-06-08 19:51 JST

- ユーザー指示により、年表表示だけを対象に、リーダー線付き外側ラベルを短いバーでラベル幅が収まらない場合だけ使う条件へ変更した。長いバーはバー内表示へ戻し、外側ラベルは既存バーと既存ラベルを避ける候補位置へ配置し、リーダー線はバー中央から出すようにした。ラベル背景のSVG矩形と残存CSS背景指定も削除した。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.170` へ上げる。

### 2026-06-08 20:13 JST

- ユーザー指示により、年表表示だけを対象に、リードライン導入前のバー内ラベル表示へ戻した。リーダー線と外側ラベル配置の仕組みを取り除き、ファイル名と年数はバー内でクリップ付きラベルとして表示する状態に戻した。横向き年表、ファイル名と年数の表示、長さ変更中のレーン判定固定は維持した。対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.171` へ上げる。

### 2026-06-08 20:22 JST

- ユーザー指示により、年表表示だけを対象に、ドラッグ中の重なり判定とレーン適用・解除を移動、開始変更、終了変更のすべてで停止するようにした。ドラッグ中はバーの横位置や長さだけpreviewで更新し、レーン再配置は編集確定後の更新結果で行う。移動中にもレーン判定が動かないテストを追加し、対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.172` へ上げる。

### 2026-06-08 20:33 JST

- ユーザー指示により、年表表示だけを対象に、SVGバー内ラベルを全文幅で表示し、ラベル同士が重なる場合は同じバーの縦範囲内で上下候補へずらすようにした。リードラインと外側ラベルは使わず、ファイル名と年数ラベルの全文幅を維持する。短い隣接バーのラベルが見切れず上下にずれるテストを追加し、対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.173` へ上げる。

### 2026-06-08 20:44 JST

- ユーザー指示により、年表表示だけを対象に、SVGバーのホバーまたはクリック選択で詳細カードを表示するようにした。詳細カードにはファイルアイコン、ファイル名、年情報を表示し、ファイルアイコンから対象ファイルを開ける。何もない年表領域のクリックで固定表示カードを閉じる。ホバー表示、選択表示、ファイルを開く操作、空白クリックで閉じる動作のテストを追加し、対象テストと `pnpm typecheck` が成功した。作業記録更新に伴い `app/package.json` のバージョンを `0.2.174` へ上げる。
