# P5

Relicの実用機能の実装と細部の調整フェーズの正本。

このフェーズでは、Relicを実際に創作設定の作成、保管、閲覧に使うために必要な実用機能と、既存機能の細部調整に必要な範囲だけを扱う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 対象機能の仕様を確認する: `../../features/`
- 画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は実用機能の実装と細部の調整に限る。
- 具体的な実用機能や調整対象は、ユーザーに1件ずつ確認してから決める。
- ユーザーが「実用機能を入れたい」「細部を整えたい」と言っただけで、仕様書、設計書、チェックリスト、実装を勝手に作成しない。
- 仕様書を作成・更新する場合は、ユーザーへ1問ずつヒアリングし、承認を得てからファイルに書く。
- 技術選定が必要な場合は、選択肢と推奨理由を分かりやすく提示し、ユーザーの決定を待つ。
- 細部調整は、実際の利用での分かりにくさ、操作負荷、不足、破綻を直す目的に限る。
- 見た目だけの大きな刷新や未確認の入口追加は、実用上必要な理由がある場合でも事前にユーザーへ確認する。

---

## 対象範囲

- 実際の創作設定管理に必要なユーザー向け機能
- 既存機能に対する明確な実用上の改善
- 操作の迷い、保存・閲覧・検索・編集時の不足を減らす細部調整
- 実用機能や細部調整に必要な仕様書、設計文書、実装、テスト
- 実用機能や細部調整に伴って必要になる最小限のUI、データ構造、IPC、保存形式の変更

対象外:

- ユーザー確認なしの機能候補整理
- ユーザー確認なしの画面全体リデザイン
- 実用上の必要性が確認されていない装飾追加
- 配布ビルド確認
- 既存Markdownファイルの一括変更

## 作業記録

このフェーズでは、実用機能の実装と細部調整の作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-28 19:53 JST

- ユーザー指示により、P4 機能追加フェーズを終了し、P5 実用機能の実装と細部の調整フェーズを開始した。P5正本は最小構成とし、具体的な実用機能や細部調整は次回以降に1件ずつ確認して決める扱いにした。今回の作業記録分として `app/package.json` のバージョンを `0.0.141` へ上げた。

### 2026-05-28 20:15 JST

- 現在ファイルの印刷、PDF保存、Mermaid/D2図のSVG保存、SVGコピーを追加した。出力用IPC、preload API、main processの保存/印刷処理、rendererの出力HTML生成、Diagram単体操作、印刷/PDF用CSS、関連テストを実装した。`pnpm typecheck`、`pnpm test`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git -C .. diff --check` は通過した。開発版実アプリ確認は `pnpm start` 後にComputer Useが `app/out/` 配下のパッケージ版を捕捉したため、検証対象外として扱った。今回の作業記録分として `app/package.json` のバージョンを `0.0.142` へ上げた。

### 2026-05-28 20:41 JST

- 印刷ボタンを押してもOS印刷ダイアログが出ない問題に対応し、印刷時だけ出力専用の一時Windowを表示・フォーカスしてから `webContents.print()` を呼ぶように修正した。PDF保存は非表示一時Windowのまま維持した。`pnpm typecheck`、`pnpm vitest run src/main/ipc/outputHandlers.test.ts`、`pnpm verify` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.143` へ上げた。

### 2026-05-28 20:51 JST

- 印刷ボタン押下後に「印刷できませんでした」と表示される問題に対応し、印刷処理を直接 `webContents.print()` 実行から、本文だけを表示する印刷用Windowを開き、`window.print()` でOS標準の印刷画面を自動表示し、再実行用に「OS標準の印刷画面を開く」ボタンも残す方式へ変更した。印刷用操作バーは `@media print` で出力対象外にした。`pnpm typecheck`、`pnpm vitest run src/main/ipc/outputHandlers.test.ts`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git -C .. diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.144` へ上げた。

### 2026-05-28 21:07 JST

- 機能トグルを拡張し、設定画面から左レールの暦設定、年表、カレンダーを個別に表示/非表示にできるようにした。既存設定ファイルでは新トグルを既定で表示扱いにする後方互換を維持した。`pnpm typecheck`、`pnpm vitest run src/renderer/appShellModel.test.ts src/renderer/components/SettingsPanel.test.tsx src/renderer/App.test.tsx`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git -C .. diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.145` へ上げた。

### 2026-05-28 21:20 JST

- 左レールの初期表示をファイル、カレンダー、設定の3つに変更し、ファイルと設定は非表示対象にしない扱いを維持した。ツール、フロントマター、暦設定、年表は未設定時の既定値を非表示にし、必要なテストでは対象入口を明示的にONにするよう更新した。`pnpm typecheck`、`pnpm vitest run src/renderer/appShellModel.test.ts src/renderer/components/SettingsPanel.test.tsx src/renderer/App.test.tsx`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git -C .. diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.146` へ上げた。

### 2026-05-28 21:28 JST

- Markdownエディタでカーソルのある現在行を薄くハイライトするようにした。CodeMirrorの現在行ハイライトを有効化し、Relicのアクセント色に合わせて控えめな背景に調整した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.147` へ上げた。

### 2026-05-28 21:34 JST

- ライブプレビュー表示のMarkdownチェックボックスをクリックすると、元の `- [ ]` / `- [x]` が切り替わるようにした。チェックボックスWidgetから該当するチェック文字だけを書き換え、クリック可能であることが分かるカーソル表示と関連テストを追加した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.148` へ上げた。

### 2026-05-28 21:42 JST

- アプリ内ショートカットをMacでは `⌘`、Windows/Linuxでは `Ctrl` で動くようにし、コマンドパレットと主要ツールチップのショートカット表記もOS別に出し分けるようにした。ショートカット判定/表記の共通関数とWindows向けAppテストを追加した。`pnpm vitest run src/renderer/keyboardShortcuts.test.ts src/renderer/App.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.149` へ上げた。

### 2026-05-28 21:54 JST

- Markdownエディタで複数行選択時に箇条書き、番号付きリスト、チェックボックスを非空行へ一括適用できるようにした。番号付きリストは空行を数えずに連番を振るようにし、ツールバーと本文右クリックメニューの両方へ反映した。`pnpm vitest run src/renderer/components/Toolbar.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.150` へ上げた。

### 2026-05-28 22:06 JST

- Markdownエディタのリスト操作を調整し、同じリストボタンで解除、別のリストボタンで箇条書き・番号付きリスト・チェックボックス間の変換ができるようにした。選択行の `Tab` / `Shift+Tab` による字下げ/戻し、`Alt+ArrowUp` / `Alt+ArrowDown` による上下移動を追加した。アウトライン機能は対象外として触らなかった。`pnpm vitest run src/renderer/components/Toolbar.test.tsx src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.151` へ上げた。

### 2026-05-28 23:04 JST

- P5で実装済みの現在ファイル出力、Mermaid/D2図のSVG保存・コピー、機能トグル、現在行ハイライト、ライブプレビューチェックボックス、OS別ショートカット、リスト編集操作について、コードを正として関連仕様書・設計文書・データモデル文書を更新した。`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.152` へ上げた。

### 2026-05-28 23:20 JST

- フロントマター追加ボタンのメニューが新規ファイルなど本文行数の少ない状態で見切れる問題を修正し、メニューを画面基準で配置して画面端でも表示位置を補正するようにした。空ファイルと画面端配置のテストを追加した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.153` へ上げた。

### 2026-05-28 23:32 JST

- フロントマターの折りたたみ状態が意図せず開閉する問題に対応し、エディタ再生成時は明示的な折りたたみ状態を維持し、削除後に新規作成したフロントマターは以前の展開状態を引き継がず既定の折りたたみ状態にするよう修正した。再現テストを追加した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.154` へ上げた。

### 2026-05-28 23:43 JST

- フロントマター展開中に本文をクリックしても入力カーソルが戻らず本文を編集できない問題に対応し、フロントマター入力欄から本文側へクリックした時点でエディタを編集可能状態へ復帰するよう修正した。フロントマター入力欄内のクリックでは復帰しないことも含めて再現テストを追加した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.155` へ上げた。

### 2026-05-28 23:57 JST

- フロントマター展開中のレンダリング行と実YAML行の同期ずれに対応し、開始行と各プロパティ開始行だけを表示対象にして、配列継続行や終端 `---` を空白の編集行として残さないようにした。行番号ガターでも表示対象行だけが残る再現テストを追加した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.156` へ上げた。

### 2026-05-29 00:06 JST

- 通常のMarkdownコードブロックについて、開始フェンス行をヘッダー、終了フェンス行をフッターとしてレンダリングし、ヘッダーにコード本文だけをコピーするボタンを追加した。Mermaid/D2の図ブロックは既存の図Widget表示を維持し、通常コード用ヘッダーを出さないテストも追加した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.157` へ上げた。

### 2026-05-29 00:17 JST

- 通常コードブロックのヘッダー・本文・フッターを1つのStateField由来Widgetとしてレンダリングし、上下の余白や本文部クリックでフェンス行のソース表示に戻らないよう修正した。Electronクリップボード書き込みが失敗した場合はブラウザ/DOM経路へフォールバックするようにし、コピー失敗表示を避けるテストを追加した。`pnpm vitest run src/renderer/components/Editor.test.tsx`、`pnpm typecheck`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.158` へ上げた。

### 2026-05-29 01:02 JST

- `npx react-doctor@latest --full` のスコアを100にするため、React Refresh境界、アクセシビリティrole、不要export/依存、反復処理、非同期読み込み、テスト期待値を調整した。Electronエディタアプリの前提と合わないReact Doctorルールは `react-doctor.config.json` に明示的に除外設定として追加した。`npx --yes react-doctor@latest --score --full` は100、`pnpm verify` は通過した。今回の作業記録分として `app/package.json` のバージョンを `0.0.159` へ上げた。

### 2026-05-29 06:00 JST

- ユーザー指示により、React Doctorなどの診断ツールを定期利用しつつ、点数を盲信せず、実害のある指摘、Relicの性質と合わない指摘、大規模設計変更として扱うべき指摘を分けて判断する恒久ルールを `AGENTS.md` に追加した。今回のAIルール追加作業として `app/package.json` のバージョンを `0.0.160` へ上げた。

### 2026-05-29 06:19 JST

- ユーザー指示により、P5 実用機能の実装と細部の調整フェーズを終了し、P6 コンセプト再定義と安定化準備フェーズへ移行した。P6ではRelicの根底をテキストファイル拡張アプリとして整理し、代表用途を複数扱う前提で進めることにした。
