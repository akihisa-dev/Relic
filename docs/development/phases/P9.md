# P9

Relicの全リファクタリングフェーズの正本。

このフェーズでは、Relicの外部仕様、Markdown保存形式、既存機能、画面上の操作導線を維持したまま、内部実装の見通し、責務分離、重複削減、型安全性、テストしやすさを整理する。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 既存仕様との整合を見る: `../../features/`
- 画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は、既存機能を壊さないための内部整理、分割、重複削減、型整理、テスト補強に限る。
- リファクタリングは小さい単位に分け、変更前後で同じ挙動を保っていることを検証する。
- 外部仕様、Markdown保存形式、IPCの公開的な意味、ファイル操作仕様、画面の操作導線、UI文言は、ユーザー確認なしに変更しない。
- 大規模な設計変更、状態管理の入れ替え、保存処理やファイル操作の再設計は、実装前に対象範囲と理由をユーザーへ確認する。
- React Doctorなどの診断ツールは判断材料として使ってよいが、点数を上げるためだけに構造変更しない。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。

---

## 対象範囲

- Reactコンポーネント、hooks、状態管理、CSS構成の整理
- main / preload / renderer / shared の責務境界の整理
- IPCハンドラー、ファイル処理、検索、AIワークスペース関連処理の見通し改善
- 重複コード、長すぎる関数、テストしにくい処理の分割
- 型定義、バリデーション、エラー処理の一貫性整理
- 既存テストの補強、関連テストの整理、`pnpm verify` が通る状態の維持

対象外:

- ユーザー確認なしの新機能追加
- ユーザー確認なしの画面全体リデザイン
- Markdownファイル保存形式の変更
- ローカルファイル操作仕様の変更
- AI機能や外部サービス利用方針の変更
- 配布ビルド確認

## 作業記録

このフェーズでは、全リファクタリングの作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-31 20:02 JST

- ユーザー指示により、P8 UI・ビジュアルデザインフェーズを終了し、P9 全リファクタリングフェーズを開始した。P9では既存機能、Markdown保存形式、画面の操作導線を維持したまま、内部実装の見通し、責務分離、重複削減、型安全性、テストしやすさを整理する扱いにした。今回のフェーズ移行作業として `app/package.json` のバージョンを `0.1.209` へ上げる。

### 2026-05-31 20:23 JST

- ユーザー添付テキストの最優先実施として、`app/src` の行数上位と危険な型逃げ・抑制コメントを調査し、`aiWorkspaceService.ts` をCowork公開操作の接続役へ縮小して、チャットモデル、参照収集、変更案検証・適用、メッセージ整形、状態変換、中断判定を責務別ファイルへ分割した。`App.tsx` から終了前保存、外部変更反映、印刷/PDF出力、サイドバー幅、AI操作ハンドラなどをhookへ分離し、App / Editorテストの共通ヘルパーも分離した。`docs/development/refactor-notes.md`、`docs/development/coding-rules.md`、`docs/engineering/architecture.md` を更新し、今回のP9リファクタリング作業として `app/package.json` のバージョンを `0.1.210` へ上げる。

### 2026-05-31 20:35 JST

- 添付テキストの継続実施として、`App.tsx` に残っていたレイアウト描画とAppLayout向けprops組み立てを `AppLayout.tsx` と `appLayoutProps.ts` へ分離し、`App.tsx` を685行まで削減した。`pnpm typecheck` と `pnpm exec vitest run src/renderer/App.test.tsx` が成功した。今回のP9追加リファクタリング作業として `app/package.json` のバージョンを `0.1.211` へ上げる。

### 2026-05-31 20:39 JST

- 添付テキストの継続実施として、`aiWorkspaceService.test.ts` のpreview系テストを `aiWorkspaceService.preview.test.ts` へ分離し、共通の一時ディレクトリ作成・モック初期化・テストデータ生成を `aiWorkspaceServiceTestHelpers.ts` へ移した。`aiWorkspaceService.test.ts` は628行まで削減し、`pnpm typecheck` と `pnpm exec vitest run src/main/ai/aiWorkspaceService.test.ts src/main/ai/aiWorkspaceService.preview.test.ts` が成功した。今回のP9追加リファクタリング作業として `app/package.json` のバージョンを `0.1.212` へ上げる。

### 2026-05-31 20:46 JST

- 添付テキストの継続実施として、`diagramPreview.test.ts` のpan/zoom系テストを `diagramPreview.panZoom.test.ts` へ分離し、Mermaid/D2モックとDOM生成・イベント生成ヘルパーを `diagramPreviewTestHelpers.ts` へ移した。`diagramPreview.test.ts` は492行まで削減し、`pnpm typecheck` と `pnpm exec vitest run src/renderer/diagramPreview.test.ts src/renderer/diagramPreview.panZoom.test.ts` が成功した。今回のP9追加リファクタリング作業として `app/package.json` のバージョンを `0.1.213` へ上げる。

### 2026-05-31 20:51 JST

- 添付テキストの継続実施として、`Editor.test.tsx` のライブプレビュー・図Widget・通常コードブロック・チェックボックス系テストを `Editor.livePreview.test.tsx` へ分離した。`Editor.test.tsx` は2107行まで削減し、`pnpm typecheck` と `pnpm exec vitest run src/renderer/components/Editor.test.tsx src/renderer/components/Editor.livePreview.test.tsx` が成功した。今回のP9追加リファクタリング作業として `app/package.json` のバージョンを `0.1.214` へ上げる。

### 2026-05-31 20:59 JST

- 添付テキストの継続実施として、`Editor.test.tsx` からフロントマター、フロントマター型別入力、表プレビューのテストを専用ファイルへ分離し、`Editor.test.tsx` を618行まで削減した。`App.test.tsx` はファイルタブ、外部変更、サイドバー、Cowork、チャート、ワークスペース、ファイル操作、検索/リンクなどの機能別テストファイルへ分離し、95行まで削減した。追加分割後、`app/src` の `ts` / `tsx` に1000行超と700行超のファイルが残っていないことを確認し、`pnpm typecheck`、分割対象のEditor系テスト、分割対象のApp系テスト、`pnpm verify:full` が成功した。今回のP9追加リファクタリング作業として `app/package.json` のバージョンを `0.1.215` へ上げる。

### 2026-05-31 21:04 JST

- 添付テキストのビルド系検証として、`pnpm build:mac:safe` を実行した。通常権限では `github.com` の名前解決で失敗したためネットワーク許可付きで再実行し、macOS向けunpacked appと禁止成果物不在の確認が成功した。Windows向け安全ビルドはこのMac環境で実行可能と判断できないため実施しなかった。今回のP9追加検証記録として `app/package.json` のバージョンを `0.1.216` へ上げる。

### 2026-05-31 21:19 JST

- ユーザー指示により、直近リファクタリング後の堅牢性・保守性懸念を広めに点検し、未使用コード・未使用引数・未使用テスト変数を削除または意図が分かる形へ整理した。`tsconfig.json` に `noUnusedLocals` と `noUnusedParameters` を追加し、同種の残骸を通常の型チェックで検出できるようにした。危険な型逃げ・抑制コメント、行数上位、差分空白を再確認し、対象テスト、`pnpm typecheck`、`pnpm verify:full` が成功した。今回のP9追加保守性改善として `app/package.json` のバージョンを `0.1.217` へ上げる。

### 2026-05-31 21:22 JST

- ユーザー指示により、実装を正として仕様書と設計文書の整合を確認し、Coworkの左レール常設入口、プライマリサイドバー、セカンダリサイドバー、AI変更案の即時検証反映、AI使用量表示、OpenAI APIキーの `safeStorage` 暗号化専用ファイル保存を、機能仕様、画面設計、ナビゲーション設計、アーキテクチャ、データモデル、設計判断、索引へ反映した。今回のP9文書整合作業として `app/package.json` のバージョンを `0.1.218` へ上げる。

### 2026-05-31 21:28 JST

- ユーザー指示により、堅牢性・保守性懸念の追加点検として `noImplicitReturns`、`noFallthroughCasesInSwitch`、`noImplicitOverride` を確認し、D2レンダラーの型逃げを公開型に沿うコンパイル要求へ整理した。CodeMirror Widgetのoverride明示とMarkdownプレビュー拡張の未一致分岐を明示し、同チェックを通常の型チェックへ追加した。対象テストと `pnpm typecheck` が成功した。今回のP9追加保守性改善として `app/package.json` のバージョンを `0.1.219` へ上げる。

### 2026-05-31 21:32 JST

- ユーザー指示により、追加の型厳格化候補として `noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`、`noPropertyAccessFromIndexSignature` を調査し、一括導入は修正規模が大きいため見送った。安全に直せる範囲として、検索・置換・リンク更新・Markdownリンク/タグ・図・見出し・コードフェンス解析で、正規表現や配列分割結果の未定義を明示的に扱うよう整理した。対象テストと `pnpm typecheck` が成功した。今回のP9追加保守性改善として `app/package.json` のバージョンを `0.1.220` へ上げる。

### 2026-06-01 07:11 JST

- ユーザー指示により、タイトルバーのタブ右クリックメニューが背景レイヤーの裏に回る不具合を修正し、タブメニューを `document.body` へポータル描画して `z-index` を上げた。`AppTitleBar.test.tsx` にポータル描画とレイヤー確認を追加し、`pnpm typecheck` と `pnpm exec vitest run src/renderer/components/AppTitleBar.test.tsx src/renderer/App.fileTabs.test.tsx` が成功した。今回のP9表示不具合修正として `app/package.json` のバージョンを `0.1.221` へ上げる。

### 2026-06-01 08:19 JST

- ユーザー指示により、本文エディタで範囲選択後に右クリックメニューからMarkdown装飾を適用すると対象範囲の直前側へ装飾が付く不具合を修正した。右クリック1回で発生する複数イベントが最初に記録した選択範囲を上書きしないようにし、`Editor.test.tsx` に右クリック中に選択が動いても最初の選択範囲へ装飾する回帰テストを追加した。`pnpm typecheck` と `pnpm exec vitest run src/renderer/components/Editor.test.tsx` が成功した。今回のP9本文装飾不具合修正として `app/package.json` のバージョンを `0.1.222` へ上げる。

### 2026-06-01 09:21 JST

- ユーザー指示により、ファイルツリーのファイル・フォルダリネームができない不具合を修正した。リネーム入力欄が `<button>` 内に入る不正なHTML構造をやめ、リネーム中だけ同じ見た目の非button行に入力欄を描画するよう変更した。`FileTree.test.tsx` に入力欄がbutton内へ入らない確認を追加し、`App.fileRename.test.tsx` にフォルダの右クリックリネーム確認を追加した。`pnpm typecheck`、`pnpm exec vitest run src/renderer/components/FileTree.test.tsx src/renderer/App.fileRename.test.tsx`、`pnpm exec vitest run src/main/files/markdownFiles.test.ts src/main/files/folders.test.ts src/main/ipc/fileHandlerValidators.test.ts` が成功した。今回のP9ファイルツリーリネーム不具合修正として `app/package.json` のバージョンを `0.1.223` へ上げる。

### 2026-06-01 20:01 JST

- ユーザー報告により、Macでは直ったがWindowsビルドではファイル未オープン状態でもファイル・フォルダリネームが直っていない件を調査し、ワークスペース相対パス生成にOS依存の `node:path` 区切りが混ざる箇所を修正した。Markdown正本として扱う相対パスは常に `/` 区切りになるよう、リネーム・移動・コピー名生成の相対パス処理をPOSIX扱いへ寄せた。`pnpm typecheck`、`pnpm exec vitest run src/main/files/paths.test.ts src/main/files/markdownFilePaths.test.ts src/main/files/markdownFiles.test.ts src/main/files/folders.test.ts src/main/ipc/fileHandlerValidators.test.ts`、`pnpm exec vitest run src/renderer/components/FileTree.test.tsx src/renderer/App.fileRename.test.tsx` が成功した。今回のP9 Windowsリネーム不具合修正として `app/package.json` のバージョンを `0.1.224` へ上げる。

### 2026-06-01 20:05 JST

- ユーザー指示により、本文エディタで範囲選択後にコードブロックを付与しても選択範囲へ適用されず挿入になる不具合を修正した。コードブロック専用コマンドを追加し、選択範囲がある場合は範囲を ``` で囲み、未選択時だけ従来どおり空のコードブロックを挿入するようにした。右クリックメニューとツールバーの両方で同じコマンドを使うよう整理し、`pnpm typecheck` と `pnpm exec vitest run src/renderer/components/Toolbar.test.tsx src/renderer/components/Editor.test.tsx` が成功した。今回のP9本文装飾不具合修正として `app/package.json` のバージョンを `0.1.225` へ上げる。

### 2026-06-01 20:34 JST

- ユーザー指示により、RelicのOSS公開準備として `LICENSE` にAGPL-3.0正式本文とSPDX `AGPL-3.0-or-later` を追加し、README、CONTRIBUTING、SECURITY、AGENTS、docs索引・概要を公開リポジトリ前提へ更新した。現行実装を確認してREADMEの主機能へ検索置換、印刷/PDF保存、図表SVG出力、Coworkを反映し、旧フェーズ記録の個人環境絶対パスを一般化した。秘密情報・認証情報・生成物・公開不要ファイルの監査、LICENSE本文のGNU公式本文との一致確認、`git diff --check`、`pnpm verify` が成功した。今回のP9 OSS公開準備として `app/package.json` のバージョンを `0.1.226` へ上げる。

### 2026-06-01 20:38 JST

- ユーザー指示により、AIルールからフェーズ正本更新時の自動push義務を外し、pushはユーザーが明示的に指示した場合だけ実行する扱いへ変更した。`AGENTS.md` と `docs/development/phases.md` の該当ルールを更新し、今回のP9 AIルール修正として `app/package.json` のバージョンを `0.1.227` へ上げる。

### 2026-06-01 20:46 JST

- ユーザー指示により、GitHub Public化直前の最終セキュリティ監査として、現在ファイル、Git管理対象、`.env` 系、秘密情報パターン、Git履歴、Electron設定、IPC境界、Actions、依存関係、README/SECURITY/CONTRIBUTING/AGENTS/docsを確認した。現在ファイルには実値の秘密情報や`.env`系追跡ファイルは見つからず、READMEのCowork説明だけ現行実装に合わせてCodex App Server方式とOpenAI API方式の併記へ直した。Git履歴には旧日誌と旧AI入口文書が残るため、Public化前の履歴整理判断が必要と扱う。オンライン依存監査は通常権限で名前解決に失敗し、外部送信を伴う権限付き実行は未実施。今回のP9 Public化前監査として `app/package.json` のバージョンを `0.1.228` へ上げる。

### 2026-06-01 21:39 JST

- ユーザー指示により、READMEを英語セクション先行・日本語セクション後続の構成へ整理し、スクリーンショットを冒頭集約から対応する機能説明の近くへ移した。`git diff --check` が成功した。今回のP9 README英語対応として `app/package.json` のバージョンを `0.1.229` へ上げる。

### 2026-06-01 22:11 JST

- ユーザー指示により、P9全範囲リファクタリングとして、`codexAppServerClient.ts` を公開API入口に縮小し、Codex App Serverの通信、プロンプト構築、レスポンス解析、使用量変換、型定義を責務別ファイルへ分離した。`appLayoutProps.ts` はAppへ責務を戻さず、表示領域ごとのprops組み立てへ分割した。CoworkはAI Provider境界を `aiWorkspaceProviders.ts` へ切り出し、Provider共通の変更案ID生成とpending operation整形を共通helper化した。`App.test.tsx` と `Editor.test.tsx` は責務別テストへ追加分割し、Editorの共通レンダリング処理をhelper化した。`chronicleTimelineAxis.ts` は年表軸としてまとまった責務と判断し、無理な分割は行わなかった。`pnpm typecheck`、`pnpm test`、`pnpm verify`、`pnpm verify:full` が成功した。今回のP9追加リファクタリング作業として `app/package.json` のバージョンを `0.1.230` へ上げる。

### 2026-06-01 22:33 JST

- ユーザー指示により、P9で提案した保守性改善をまとめて実施した。テスト内の `window.relic` 直接モックを既存の `makeRelicApi` へ寄せ、`as unknown as typeof window.relic` の散在を解消した。Windows風区切りが混ざるリンク更新・コピー名生成の回帰テストを追加し、リンク更新処理では入力パスをワークスペース相対パスの `/` 区切りへ正規化して扱うようにした。App / Editorは行数と責務を点検し、追加分割は不要と判断した。React Doctorは通常権限で `registry.npmjs.org` 名前解決に失敗し、ローカルにも未導入だったため実行不可として扱った。`pnpm typecheck` と対象テストが成功した。今回のP9追加保守性改善として `app/package.json` のバージョンを `0.1.231` へ上げる。

### 2026-06-01 23:09 JST

- ユーザー指示により、ローカライズ漏れを確認し、AI設定、AIチャット履歴、Cowork入力、図プレビュー、通常コードブロック、印刷/PDF/SVG出力、編集コンテキストメニューの表示文言を辞書経由へ整理した。`pnpm typecheck`、`pnpm test`、`git diff --check` が成功した。今回のP9ローカライズ修正として `app/package.json` のバージョンを `0.1.233` へ上げる。

### 2026-06-01 23:10 JST

- ユーザー指示により、タイトルバー右上の操作ボタン周辺で、ボタンではない余白部分をウィンドウ移動用に掴めるよう修正した。操作ボタン自体は `no-drag` のまま維持し、`AppTitleBar.test.tsx` にCSS指定の回帰確認を追加した。`pnpm typecheck` と `pnpm exec vitest run src/renderer/components/AppTitleBar.test.tsx` が成功した。今回のP9タイトルバー操作性修正として `app/package.json` のバージョンを `0.1.233` へ上げる。

### 2026-06-01 23:14 JST

- ユーザー指摘により、タイトルバー右上ボタンのホバー説明が背景レイヤーの下に隠れる不具合を修正した。タイトルバーを前面レイヤーとして扱い、ホバー説明がタイトルバー外へ出ても表示されるようにし、右上ボタン周辺の余白を掴める挙動は維持した。`pnpm typecheck` と `pnpm exec vitest run src/renderer/components/AppTitleBar.test.tsx` が成功した。今回のP9タイトルバーホバー表示修正として `app/package.json` のバージョンを `0.1.234` へ上げる。
