# P6

Relicのコンセプト再定義と安定化準備フェーズの正本。

このフェーズでは、Relicの根底を「Markdownに書ける情報をMarkdownファイルのまま保ち、その情報をもとに編集・閲覧・検索・可視化・出力を拡張するローカルアプリ」として整理し、創作設定を代表用途のひとつに位置づけながら、複数用途を前提にした安定化準備を扱う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 既存仕様との整合を見る: `../../features/`
- 画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲はコンセプト再定義、代表用途の整理、安定化準備に限る。
- Relicの根底は、創作設定専用アプリではなく、Markdownに書ける情報をMarkdownファイルのまま活かして拡張するアプリとして扱う。
- 創作設定は重要な代表用途だが、唯一の主要用途として固定しない。
- 用途例や説明文を決める場合は、ユーザーへ1問ずつ確認し、承認された内容だけを正本化する。
- コンセプト整理を理由に、仕様書、設計書、UI文書、機能一覧、チェックリストを勝手に作成・更新しない。
- 安定化準備は、コンセプト整理後に確認された範囲でのみ扱う。
- 新機能追加、画面全体の刷新、用途別テンプレート作成、配布ビルド確認は、ユーザーが明示した場合だけ扱う。

---

## 対象範囲

- Relicの根底コンセプトの言語化
- 創作設定、研究、学習、個人Wiki、プロジェクト資料など、複数の代表用途の整理
- 既存ドキュメントに書かれている「創作設定用」という表現の扱いの見直し
- コンセプト整理に基づく安定化準備の範囲決め
- 安定化準備に必要な最小限の仕様書、設計文書、実装、テスト

対象外:

- ユーザー確認なしのコンセプト確定
- ユーザー確認なしの用途一覧作成
- ユーザー確認なしの新機能追加
- ユーザー確認なしの画面全体リデザイン
- 配布ビルド確認
- 既存Markdownファイルの一括変更

## 作業記録

このフェーズでは、コンセプト再定義と安定化準備の作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-29 06:19 JST

- ユーザー指示により、P6 コンセプト再定義と安定化準備フェーズを開始した。Relicの根底をテキストファイル拡張アプリとして整理し、創作設定は代表用途のひとつであり唯一の主要用途には固定しない前提にした。具体的なコンセプト文言や用途整理は次回以降に1件ずつ確認して決める扱いにした。今回のフェーズ移行作業として `app/package.json` のバージョンを `0.0.161` へ上げた。

### 2026-05-29 06:32 JST

- ユーザー確認により、Relicのコンセプトを「Markdownに書ける情報をMarkdownファイルのまま保ち、その情報をもとに編集・閲覧・検索・可視化・出力を拡張するローカルアプリ」として整理し、`docs/project/overview.md`、`README.md`、`docs/features/markdown.md`、`app/package.json` の説明を更新した。MermaidやD2はMarkdownコードブロックとして書ける情報だから扱う、という判断軸も明記した。今回のコンセプト再定義作業として `app/package.json` のバージョンを `0.0.162` へ上げた。

### 2026-05-29 06:35 JST

- ユーザー指示により、Relicのコンセプト再定義を区切りとして `app/package.json` のバージョンを `0.1.0` へ上げた。

### 2026-05-29 06:52 JST

- ユーザー指示により、安定化準備を安全優先で開始し、ファイル操作のうちリネーム・移動・リンク更新・ワークスペース外拒否を確認した。別フォルダへ移動したファイルへの basename-only 内部リンクがパス付きリンクへ更新されない不具合を修正し、ファイル・フォルダ移動時のリンク更新、フロントマター内リンク更新、ワークスペース外移動先拒否、外部実体シンボリックリンク拒否の回帰テストを追加した。`pnpm test -- src/main/files/linkUpdater.test.ts src/main/files/markdownFiles.test.ts src/main/files/folders.test.ts` 実行時に全体テストが走り `79` ファイル `671` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.1` へ上げた。

### 2026-05-29 06:56 JST

- 安定化準備の続きとして、保存・自動保存・保存失敗時のエラー表示を確認した。保存中に本文が変わったあと外部変更と衝突した場合、保存待ち本文が衝突後に追加保存され得る問題を修正し、衝突中は保存成功扱いにせず保存待ち本文を破棄する回帰テストを追加した。`pnpm test -- src/renderer/hooks/useEditorAutoSave.test.ts` 実行時に全体テストが走り `79` ファイル `672` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.2` へ上げた。

### 2026-05-29 06:59 JST

- 安定化準備の続きとして、Mermaid / D2 の描画失敗時挙動を確認した。図表レンダラーが文字列を返してもサニタイズ後にSVGが残らない場合、描画成功扱いにせずエラーUIへ倒すよう修正し、空の図表示やSVG保存・コピー操作が出ないことを確認する回帰テストを追加した。`pnpm test -- src/renderer/diagramPreview.test.ts` 実行時に全体テストが走り `79` ファイル `673` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.3` へ上げた。

### 2026-05-29 07:04 JST

- 安定化準備の続きとして、UI状態復元まわりのうちワークスペース変更時のタブ状態を確認した。ワークスペース切替・削除ではタブを閉じる一方、既存ワークスペースを開く操作と新規ワークスペース作成操作では前ワークスペースのタブが残り得る問題を修正し、両操作後にタブが閉じられる回帰テストを追加した。`pnpm test -- src/renderer/App.test.tsx` 実行時に全体テストが走り `79` ファイル `675` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.4` へ上げた。

### 2026-05-29 07:09 JST

- 安定化準備の続きとして、機能トグルOFF時のUI状態を確認した。右パネルを開いた状態で右パネル機能をOFFにすると操作ボタンだけが消え、右パネル表示が残り得る問題を修正し、OFF中はショートカットでも右パネルを表示しない回帰テストを追加した。`pnpm test -- src/renderer/App.test.tsx` 実行時に全体テストが走り `79` ファイル `676` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.5` へ上げた。

### 2026-05-29 07:16 JST

- 安定化準備の続きとして、ファイル加工ツールの入出力パスを確認した。出力先フォルダ、目次生成対象、分割元ファイルがワークスペース内のシンボリックリンクでも、実体がワークスペース外なら読み書きしないよう修正し、外部実体シンボリックリンクを拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/toolActions.test.ts src/main/files/paths.test.ts` 実行時に全体テストが走り `80` ファイル `679` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.6` へ上げた。

### 2026-05-29 07:19 JST

- 安定化準備の続きとして、ワークスペース検索の大きいMarkdownファイル判定を確認した。2MiBを超えるMarkdownを検索対象外にする判定を本文読み込み前のファイルサイズ確認へ変更し、上限ちょうどのMarkdownは検索対象に含める境界テストを追加した。`pnpm test -- src/main/files/search.test.ts` 実行時に全体テストが走り `80` ファイル `680` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.7` へ上げた。

### 2026-05-29 07:23 JST

- 安定化準備の続きとして、ゴミ箱移動時のパス検証を確認した。ゴミ箱移動でも実体がワークスペース外のシンボリックリンクを拒否するよう修正し、ファイル・フォルダの外部実体シンボリックリンクをゴミ箱に移動しない回帰テストを追加した。`pnpm test -- src/main/files/trash.test.ts src/main/files/paths.test.ts` 実行時に全体テストが走り `80` ファイル `681` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.8` へ上げた。

### 2026-05-29 07:28 JST

- 安定化準備の続きとして、アプリ設定とワークスペース設定の保存処理を確認した。設定JSON保存を一時ファイル経由の安全書き込みへそろえ、通常保存後に一時ファイルが残らない回帰テストを追加した。`pnpm test -- src/main/settings/appSettings.test.ts src/main/settings/workspaceSettings.test.ts src/main/files/atomicWrite.test.ts` 実行時に全体テストが走り `81` ファイル `682` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.9` へ上げた。

### 2026-05-29 07:32 JST

- 安定化準備の続きとして、検索置換のMarkdown書き換え処理を確認した。単体置換と一括置換を一時ファイル経由の安全書き込みへそろえ、置換対象の実体がワークスペース外のシンボリックリンクである場合は拒否するよう修正した。外部実体シンボリックリンクを置換しない回帰テストと、置換後に一時ファイルが残らない確認を追加した。`pnpm test -- src/main/files/replace.test.ts src/main/files/paths.test.ts src/main/files/atomicWrite.test.ts` 実行時に全体テストが走り `81` ファイル `683` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.10` へ上げた。

### 2026-05-29 07:35 JST

- 安定化準備の続きとして、チャート項目編集時のMarkdown書き戻し処理を確認した。チャート書き戻しを一時ファイル経由の安全書き込みへそろえ、対象の実体がワークスペース外のシンボリックリンクである場合は拒否するよう修正した。外部実体シンボリックリンクへチャート書き戻しをしない回帰テストと、通常書き戻し後に一時ファイルが残らない確認を追加した。`pnpm test -- src/main/files/charts.test.ts src/main/files/paths.test.ts src/main/files/atomicWrite.test.ts` 実行時に全体テストが走り `81` ファイル `684` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.11` へ上げた。

### 2026-05-29 07:38 JST

- 安定化準備の続きとして、ファイル加工ツールのMarkdown生成処理を確認した。結合、見出し分割、タイトル一覧生成、目次生成の出力保存を一時ファイル経由の安全書き込みへそろえ、タイトル一覧生成後に一時ファイルが残らない回帰テストを追加した。`pnpm test -- src/main/ipc/toolActions.test.ts src/main/files/atomicWrite.test.ts src/main/files/paths.test.ts` 実行時に全体テストが走り `81` ファイル `685` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.12` へ上げた。

### 2026-05-29 07:42 JST

- 安定化準備の続きとして、PDF/SVGエクスポート保存処理を確認した。安全書き込み処理をバイナリにも使える形へ広げ、PDF保存とSVG保存を一時ファイル経由の安全書き込みへそろえた。PDF/SVG保存時に最終パスへ直接書き込まず一時ファイルからリネームする回帰テストを追加した。`pnpm test -- src/main/ipc/outputHandlers.test.ts src/main/files/atomicWrite.test.ts` 実行時に全体テストが走り `81` ファイル `687` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.13` へ上げた。

### 2026-05-29 07:46 JST

- 安定化準備の続きとして、Markdownファイルの新規作成と複製処理を確認した。新規作成専用の安全書き込みを追加し、同名ファイルがある場合は上書きせず、作成・複製後に一時ファイルが残らないようにした。`pnpm test -- src/main/files/atomicWrite.test.ts src/main/files/markdownFiles.test.ts src/main/files/paths.test.ts` 実行時に全体テストが走り `81` ファイル `688` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.14` へ上げた。

### 2026-05-29 07:50 JST

- 安定化準備の続きとして、ワークスペース名変更処理を確認した。大文字小文字だけの名前変更で使う一時フォルダ名が既存フォルダと重なった場合は別名へ避け、二段階リネームの後半で失敗した場合は元のフォルダ名へ戻すよう修正した。一時フォルダ名衝突時も名前変更できる回帰テストを追加した。`pnpm test -- src/main/workspace/workspaceService.test.ts` 実行時に全体テストが走り `81` ファイル `689` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.15` へ上げた。

### 2026-05-29 07:54 JST

- 安定化準備の続きとして、アプリ設定とワークスペース設定の読み込み処理を確認した。設定JSONが壊れている場合やオブジェクトではない場合でも起動処理を止めず、初期設定として読み込めるよう修正した。壊れた設定ファイルと配列形式の設定ファイルを初期設定で扱う回帰テストを追加した。`pnpm test -- src/main/settings/appSettings.test.ts src/main/settings/workspaceSettings.test.ts` 実行時に全体テストが走り `81` ファイル `693` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.16` へ上げた。

### 2026-05-29 07:57 JST

- 安定化準備の続きとして、起動時のワークスペース状態復元を確認した。登録済みのアクティブワークスペースフォルダが削除・移動済みの場合でも状態取得自体は成功し、ファイルツリーだけ空として扱うよう修正した。フォルダが存在しないアクティブワークスペースでも起動時状態を返す回帰テストを追加した。`pnpm test -- src/main/ipc/workspaceHandlers.test.ts` 実行時に全体テストが走り `81` ファイル `694` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.17` へ上げた。

### 2026-05-29 08:01 JST

- 安定化準備の続きとして、起動時のワークスペース別設定読み込みを確認した。アクティブワークスペースの補助設定ファイルが読めない場合でも状態取得自体は成功し、ピン留めなしとして扱うよう修正した。ワークスペース別設定のパスがファイルではなくフォルダになっている場合でも、起動時状態を返す回帰テストを追加した。`pnpm test -- src/main/ipc/workspaceHandlers.test.ts` 実行時に全体テストが走り `81` ファイル `695` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.18` へ上げた。

### 2026-05-29 08:05 JST

- 安定化準備の続きとして、ファイルツリー読み込み処理を確認した。ワークスペース直下は読めるが子フォルダだけ読めない場合、ファイルツリー全体を失敗させず、その子フォルダを空として扱うよう修正した。読めない子フォルダと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/fileTree.test.ts` 実行時に全体テストが走り `81` ファイル `696` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.19` へ上げた。

### 2026-05-29 08:08 JST

- 安定化準備の続きとして、ワークスペース検索のMarkdown読み込み処理を確認した。検索中に一部のMarkdownファイルのサイズ確認や本文読み込みが失敗しても検索全体を失敗させず、そのファイルだけをスキップして続行するよう修正した。読めないMarkdownファイルと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/search.test.ts` 実行時に全体テストが走り `81` ファイル `697` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.20` へ上げた。

### 2026-05-29 08:12 JST

- 安定化準備の続きとして、ワークスペースタグ一覧のMarkdown読み込み処理を確認した。タグ集計中に一部のMarkdownファイルの本文読み込みが失敗してもタグ一覧全体を失敗させず、そのファイルだけをスキップして続行するよう修正した。読めないMarkdownファイルと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/tags.test.ts` 実行時に全体テストが走り `81` ファイル `698` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.21` へ上げた。

### 2026-05-29 08:15 JST

- 安定化準備の続きとして、ワークスペース別名一覧のMarkdown読み込み処理を確認した。別名集計中に一部のMarkdownファイルの本文読み込みが失敗しても別名一覧全体を失敗させず、そのファイルだけをスキップして続行するよう修正した。読めないMarkdownファイルと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/aliases.test.ts` 実行時に全体テストが走り `81` ファイル `699` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.22` へ上げた。

### 2026-05-29 08:19 JST

- 安定化準備の続きとして、バックリンク集計の参照元Markdown読み込み処理を確認した。バックリンク集計中に一部の参照元Markdownファイルの本文読み込みが失敗してもバックリンク一覧全体を失敗させず、そのファイルだけをスキップして続行するよう修正した。読めない参照元Markdownファイルと読める参照元Markdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/backlinks.test.ts` 実行時に全体テストが走り `81` ファイル `700` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.23` へ上げた。

### 2026-05-29 08:24 JST

- 安定化準備の続きとして、フロントマター候補集計のMarkdown読み込み処理を確認した。候補集計中に一部のMarkdownファイルの本文読み込みが失敗しても候補一覧全体を失敗させず、そのファイルだけをスキップして続行するよう修正した。読めないMarkdownファイルと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/frontmatterCandidates.test.ts` 実行時に全体テストが走り `81` ファイル `701` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.24` へ上げた。

### 2026-05-29 08:28 JST

- 安定化準備の続きとして、チャート読み込み時のMarkdown集計処理を確認した。チャート読み込み中に一部のMarkdownファイルの本文読み込みが失敗してもチャート全体を失敗させず、そのファイルだけをスキップして続行するよう修正した。読めないMarkdownファイルと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/charts.test.ts` 実行時に全体テストが走り `81` ファイル `702` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.25` へ上げた。

### 2026-05-29 08:31 JST

- 安定化準備の続きとして、検索置換プレビュー時のMarkdown読み込み処理を確認した。置換プレビュー生成中に一部のMarkdownファイルの本文読み込みが失敗してもプレビュー全体を失敗させず、そのファイルだけをスキップして続行するよう修正した。実際に書き換える一括置換は安全側で止める挙動を維持し、読めないMarkdownファイルと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/replace.test.ts` 実行時に全体テストが走り `81` ファイル `703` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.26` へ上げた。

### 2026-05-29 08:36 JST

- 安定化準備の続きとして、ファイル結合前のタグ・フロントマター絞り込み時のMarkdown読み込み処理を確認した。絞り込み中に一部のMarkdownファイルの本文読み込みが失敗しても結合処理全体を失敗させず、その候補だけを除外して続行するよう修正した。結合対象として確定したファイル本文の読み込み失敗は安全側で止める挙動を維持し、タグ絞り込みとフロントマター絞り込みそれぞれの回帰テストを追加した。`pnpm test -- src/main/ipc/toolActions.test.ts` 実行時に全体テストが走り `81` ファイル `705` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.27` へ上げた。

### 2026-05-29 08:39 JST

- 安定化準備の続きとして、タイトル一覧生成とファイル結合の候補作成時に行うMarkdownファイル情報取得処理を確認した。候補作成中に一部のMarkdownファイルの情報取得が失敗しても処理全体を失敗させず、その候補だけを除外して続行するよう修正した。出力保存や結合対象として確定したファイル本文の読み込み失敗は安全側で止める挙動を維持し、タイトル一覧生成とファイル結合それぞれの回帰テストを追加した。`pnpm test -- src/main/ipc/toolActions.test.ts` 実行時に全体テストが走り `81` ファイル `707` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.28` へ上げた。

### 2026-05-29 08:43 JST

- 安定化準備の続きとして、目次生成時のサブフォルダ再帰読み込み処理を確認した。目次生成中に一部の子フォルダの読み込みが失敗しても目次生成全体を失敗させず、その子フォルダだけを除外して続行するよう修正した。対象フォルダ自体が読めない場合は従来通り安全側で失敗させ、読めない子フォルダと読める子フォルダが混在する場合の回帰テストを追加した。`pnpm test -- src/main/ipc/toolActions.test.ts` 実行時に全体テストが走り `81` ファイル `708` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.29` へ上げた。

### 2026-05-29 08:47 JST

- 安定化準備の続きとして、リンク更新の影響確認プレビュー時のMarkdown読み込み処理を確認した。影響確認中に一部のMarkdownファイルの本文読み込みが失敗しても確認全体を失敗させず、そのファイルだけを除外して件数を返すよう修正した。実際にリンクを書き換える処理は読めないMarkdownファイルがある場合に安全側で失敗させる挙動を維持し、読めないMarkdownファイルと読めるMarkdownファイルが混在する場合の回帰テストを追加した。`pnpm test -- src/main/files/linkUpdater.test.ts` 実行時に全体テストが走り `81` ファイル `709` テストが通過し、`pnpm typecheck` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.30` へ上げた。

### 2026-05-29 08:52 JST

- 安定化準備の続きとして、Markdownファイル複製時のコピー名候補探索を確認した。コピー名候補が大量に埋まっている場合に探索が無制限に続かないよう上限を設け、候補が多すぎる場合は処理を停止するよう修正した。コピー名候補が上限まで埋まっている場合の回帰テストを追加した。`pnpm test -- src/main/files/markdownFilePaths.test.ts src/main/files/markdownFiles.test.ts` 実行時に全体テストが走り `82` ファイル `710` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.31` へ上げた。

### 2026-05-29 08:56 JST

- 安定化準備の続きとして、ファイル加工ツールの出力ファイル名候補生成を確認した。出力名に `.md` が含まれる場合もMarkdownファイルとして保存し、同名候補が大量に埋まっている場合は候補探索を無制限に続けず処理を停止するよう修正した。出力名に `.md` が含まれる場合と、出力ファイル名候補が上限まで埋まっている場合の回帰テストを追加した。`pnpm test -- src/main/ipc/toolActions.test.ts` 実行時に全体テストが走り `82` ファイル `712` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.32` へ上げた。

### 2026-05-29 09:00 JST

- 安定化準備の続きとして、ワークスペース名変更時に使う一時フォルダ名候補生成を確認した。大文字小文字だけの名前変更で一時フォルダ名候補が大量に埋まっている場合、候補探索を無制限に続けず処理を停止するよう修正した。一時フォルダ名候補が上限まで埋まっている場合の回帰テストを追加した。`pnpm test -- src/main/workspace/workspaceService.test.ts` 実行時に全体テストが走り `82` ファイル `713` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.33` へ上げた。

### 2026-05-29 09:04 JST

- 安定化準備の続きとして、新規保存先パスの親フォルダ実体確認を確認した。存在しない親フォルダは既存の親までたどる挙動を維持し、権限エラーなど存在しない以外の理由で親フォルダの実体確認に失敗した場合は安全側で拒否するよう修正した。存在しない親フォルダを許可する場合と、権限エラー時に拒否する場合の回帰テストを追加した。`pnpm test -- src/main/files/paths.test.ts src/main/files/markdownFiles.test.ts src/main/ipc/toolActions.test.ts` 実行時に全体テストが走り `82` ファイル `715` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.34` へ上げた。

### 2026-05-29 09:08 JST

- 安定化準備の続きとして、ワークスペース別設定のピン留めパス復元とピン留め操作を確認した。保存済み設定のピン留めパスはワークスペース相対パスだけに正規化し、IPCから不正なピン留めパスが渡された場合は保存前に拒否するよう修正した。設定読み込み時のピン留めパス正規化と、不正なピン留めパスを保存しない回帰テストを追加した。`pnpm test -- src/main/settings/workspaceSettings.test.ts src/main/ipc/workspaceHandlers.test.ts` 実行時に全体テストが走り `82` ファイル `717` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.35` へ上げた。

### 2026-05-29 09:12 JST

- 安定化準備の続きとして、機能トグル保存時のIPC入力検証を確認した。機能トグル保存では全項目が真偽値でそろっている入力だけを保存し、壊れた形式の入力は保存前に拒否するよう修正した。不正な型や不足した項目を拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/workspaceHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `718` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.36` へ上げた。

### 2026-05-29 09:16 JST

- 安定化準備の続きとして、チャート設定の対象ファイルパス復元と保存時検証を確認した。保存済み設定のチャート対象パスはワークスペース相対パスだけに正規化し、IPCからワークスペース外を指すチャート対象パスが渡された場合は保存前に拒否するよう修正した。設定読み込み時のチャート対象パス正規化と、不正なチャート対象パスを拒否する回帰テストを追加した。`pnpm test -- src/main/settings/workspaceSettings.test.ts src/main/ipc/workspaceHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `719` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.37` へ上げた。

### 2026-05-29 09:19 JST

- 安定化準備の続きとして、フォルダ作成時のIPC入力検証を確認した。フォルダ作成では任意項目の親フォルダが省略または文字列の場合だけ受け付け、壊れた形式の親フォルダ指定は処理前に拒否するよう修正した。親フォルダ指定の有無と不正な型を確認する回帰テストを追加した。`pnpm test -- src/main/ipc/fileHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `720` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.38` へ上げた。

### 2026-05-29 09:23 JST

- 安定化準備の続きとして、リンク更新影響確認時のIPC入力検証を確認した。リンク更新の影響確認では種別がファイルまたはフォルダで、旧パスと新パスがワークスペース相対パスの場合だけ受け付け、ワークスペース外を指す形式や空白付きの壊れた形式は処理前に拒否するよう修正した。正しいファイル・フォルダ指定と、不正なパス・種別を拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/fileHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `721` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.39` へ上げた。

### 2026-05-29 09:27 JST

- 安定化準備の続きとして、ワークスペース相対パス判定の重複を確認した。保存済み設定向けの正規化とIPC入力向けの正規化済みパス判定を共通化し、チャート設定とリンク更新影響確認で同じ安全基準を使うよう修正した。共通パス判定の回帰テストと、チャート設定IPCで未正規化パスを拒否する回帰テストを追加した。`pnpm test -- src/main/files/paths.test.ts src/main/ipc/fileHandlerValidators.test.ts src/main/ipc/workspaceHandlerValidators.test.ts src/main/settings/workspaceSettings.test.ts` 実行時に全体テストが走り `82` ファイル `723` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.40` へ上げた。

### 2026-05-29 09:31 JST

- 安定化準備の続きとして、ファイル系IPCの単一パス入力検証を確認した。ファイル読み込み、バックリンク、複製、場所表示などで使う単一パス入力と、単一ファイル置換の対象パスを、正規化済みのワークスペース相対パスだけ受け付けるよう修正した。不正な外部パス、絶対パス、未正規化パスを拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/fileHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `724` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.41` へ上げた。

### 2026-05-29 09:37 JST

- 安定化準備の続きとして、ファイル移動・フォルダ移動・リネーム・ゴミ箱移動・フォルダ作成時のIPC入力検証を確認した。元パスは正規化済みのワークスペース相対パスだけを受け付け、移動先フォルダと親フォルダは空文字または正規化済みのワークスペース相対パスだけを受け付けるよう修正した。不正な外部パス、未正規化パス、壊れた移動先を拒否する回帰テストを追加した。`pnpm test -- src/main/files/paths.test.ts src/main/ipc/fileHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `726` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.42` へ上げた。

### 2026-05-29 09:40 JST

- 安定化準備の続きとして、エディタ保存時のIPC入力検証を確認した。保存対象パスは正規化済みのワークスペース相対パスだけを受け付け、外部参照、絶対パス、未正規化パスを保存処理前に拒否するよう修正した。保存入力検証を共通validatorへ移し、不正な保存対象パスと壊れた本文入力を拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/fileHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `727` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.43` へ上げた。

### 2026-05-29 09:44 JST

- 安定化準備の続きとして、ファイル加工ツールのIPC入力検証を確認した。タイトル一覧、目次生成、ファイル結合、見出し分割で使う出力先フォルダ、対象フォルダ、分割元ファイル、フォルダ絞り込みを、空文字または正規化済みのワークスペース相対パスだけ受け付けるよう修正した。画面側のルート直下指定も空文字にそろえ、不正な外部パス、未正規化パス、ドット指定を拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/toolHandlerValidators.test.ts src/renderer/toolsPanelModel.test.ts` 実行時に全体テストが走り `82` ファイル `727` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.44` へ上げた。

### 2026-05-29 09:48 JST

- 安定化準備の続きとして、SVG保存時の初期ファイル名生成を確認した。SVG保存の初期ファイル名がドットだけでサニタイズ後に空になる場合、PDF用の既定名ではなくSVG用の既定名へ戻るよう修正した。SVG保存ダイアログの初期ファイル名が `relic-diagram.svg` になる回帰テストを追加した。`pnpm test -- src/main/ipc/outputHandlers.test.ts` 実行時に全体テストが走り `82` ファイル `728` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.45` へ上げた。

### 2026-05-29 09:52 JST

- 安定化準備の続きとして、チャート項目編集時のIPC入力検証を確認した。チャート項目編集の対象ファイルパスを正規化済みのワークスペース相対パスだけ受け付けるよう修正し、外部参照や未正規化パスを処理前に拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/workspaceHandlerValidators.test.ts` 実行時に全体テストが走り `82` ファイル `728` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.46` へ上げた。

### 2026-05-29 09:56 JST

- 安定化準備の続きとして、保存済みアプリ設定からのワークスペース登録復元を確認した。壊れた設定ファイルに空のID、空の名前、相対パス、重複IDのワークスペース登録が含まれる場合、それらを読み込み時に除外するよう修正した。有効なワークスペース登録だけを残す回帰テストを追加した。`pnpm test -- src/main/settings/appSettings.test.ts` 実行時に全体テストが走り `82` ファイル `729` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.47` へ上げた。

### 2026-05-29 10:01 JST

- 安定化準備の続きとして、エディタ設定保存時のIPC入力検証を確認した。保存時に `theme` と `language` が壊れている場合や、フォントサイズ・行間が正の有限数ではない場合は保存前に拒否するよう修正した。エディタ設定保存用のvalidatorを分離し、壊れた設定を拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/editorHandlerValidators.test.ts` 実行時に全体テストが走り `83` ファイル `730` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.48` へ上げた。

### 2026-05-29 10:04 JST

- 安定化準備の続きとして、ワークスペース切替・削除・名前変更時のIPC入力検証を確認した。ワークスペースIDが空文字または空白だけの場合は処理前に拒否するよう修正し、ハンドラ側の入力型も実行時検証前提の `unknown` にそろえた。空のワークスペースIDを拒否する回帰テストを追加した。`pnpm test -- src/main/ipc/workspaceHandlerValidators.test.ts` 実行時に全体テストが走り `83` ファイル `731` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.49` へ上げた。

### 2026-05-29 10:08 JST

- 安定化準備の続きとして、保存済みアプリ設定からの最後のワークスペースID復元を確認した。`lastWorkspaceId` が空文字、または正規化後のワークスペース登録一覧に存在しないIDの場合は、読み込み時に `null` として扱うよう修正した。登録一覧に存在しない最後のワークスペースIDを除外する回帰テストを追加した。`pnpm test -- src/main/settings/appSettings.test.ts` 実行時に全体テストが走り `83` ファイル `732` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.50` へ上げた。

### 2026-05-29 10:11 JST

- 安定化準備の続きとして、保存済みアプリ設定からのワークスペースID復元を確認した。ワークスペースIDはワークスペース別設定ファイル名にも使われるため、英数字・ハイフン・アンダースコアだけの安全なID以外は読み込み時に除外するよう修正した。パス移動を含む壊れたワークスペースIDを除外する回帰テストを追加した。`pnpm test -- src/main/settings/appSettings.test.ts` 実行時に全体テストが走り `83` ファイル `732` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.51` へ上げた。

### 2026-05-29 10:15 JST

- 安定化準備の続きとして、ワークスペース別設定ファイルのパス生成を確認した。`workspaceId` にパス移動や区切り文字を含む場合は設定ファイルのパスを作らず、読み込み時は初期設定へ戻し、書き込み時は拒否するよう修正した。危険な `workspaceId` でワークスペース別設定ファイルのパスを作らない回帰テストを追加した。`pnpm test -- src/main/settings/workspaceSettings.test.ts` 実行時に全体テストが走り `83` ファイル `733` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.52` へ上げた。

### 2026-05-29 10:21 JST

- 安定化準備の続きとして、保存済みアプリ設定からのエディタ数値設定復元を確認した。`fontSize` と `lineHeight` は正の有限数だけを採用し、極端な数値やゼロの場合は既定値へ戻すよう修正した。壊れたエディタ設定の数値を読み込み時に既定値へ戻す回帰テストを追加した。`pnpm test -- src/main/settings/appSettings.test.ts` 実行時に全体テストが走り `83` ファイル `734` テストが通過し、`pnpm typecheck` と `pnpm verify:full` も通過した。今回の安定化作業として `app/package.json` のバージョンを `0.1.53` へ上げた。
