# P1

Relicの暦設定の実装フェーズの正本。

このフェーズでは、暦設定の実装に必要な範囲だけを扱う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- フロントマター仕様を確認する: `../../features/frontmatter.md`
- チャートビューの画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- アーキテクチャとデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- P1では「目的」ではなく「対象範囲」を正本にする。
- 対象範囲はチャートビューとフロントマターに限る。
- チャートビューとフロントマター以外のUI、仕様、データ構造へ変更を広げない。
- 暦設定の詳細仕様は、ユーザーに1問ずつ確認してから決める。
- 仕様が決まっていない暦単位、保存形式、表示形式、既存日付との関係をAIが推定で実装しない。
- 既存Markdownファイルの本文やフロントマターを書き換える処理は、影響範囲と戻し方を確認してから実装する。

---

## 対象範囲

- チャートビュー
- フロントマター

対象外:

- アプリ全体の思想変更
- チャートビューとフロントマター以外の画面変更
- 暦設定と直接関係しない新機能
- ユーザー確認なしの既存Markdown一括変更

---

## 修正管理

P1では、暦設定の仕様を最初から大きく固定しない。
ユーザーへのヒアリングで合意した単位ごとに、仕様、実装、検証を小さく進める。
作業記録を書く場合は、その時点のすべての差分を作業記録の単位でコミットし、コミット後にpushする。
作業記録を書く場合は、`app/package.json` のPATCHバージョンを1つ上げ、同じコミットに含める。
MAJORとMINORは、ユーザーが明示的に指示した場合だけ変更する。
「自分が直接編集したファイルだけ」を選別してコミットしてはならない。

完了条件:

- 暦設定の対象範囲がチャートビューとフロントマター内で実装されている
- 合意した仕様が、必要な `docs/features/`, `docs/design/`, `docs/engineering/` に反映されている
- `pnpm typecheck`、`pnpm test`、`git diff --check` の必要範囲を確認している
- 実アプリ確認が必要な項目は、確認結果または未確認理由を記録している
- 次フェーズへ移る場合は、ユーザーが明示している

---

## 作業記録

このフェーズでは、暦設定の実装単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-24 22:22 JST

- ユーザー指示により、P1暦設定の実装フェーズを開始した。P1では目的ではなく対象範囲を正本にし、対象範囲をチャートビューとフロントマターに限定した。フェーズ管理、索引、AI開始例をP1へ更新し、今回の作業記録分として `app/package.json` のバージョンを `0.0.53` へ上げた。
