# P4

Relicの機能追加フェーズの正本。

このフェーズでは、Relicの目的に沿って、創作設定の作成、保管、閲覧を支える機能追加に必要な範囲だけを扱う。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- 対象機能の仕様を確認する: `../../features/`
- 画面・遷移・見た目を確認する: `../../design/screens.md`, `../../design/navigation.md`, `../../design/system.md`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は機能追加に限る。
- 具体的な追加機能は、ユーザーに1件ずつ確認してから決める。
- ユーザーが「機能を追加したい」と言っただけで、仕様書、設計書、チェックリスト、実装を勝手に作成しない。
- 仕様書を作成・更新する場合は、ユーザーへ1問ずつヒアリングし、承認を得てからファイルに書く。
- 技術選定が必要な場合は、選択肢と推奨理由を分かりやすく提示し、ユーザーの決定を待つ。
- 既存機能のUI/UX改善だけを目的とする変更は、機能追加と混ぜずに扱う。

---

## 対象範囲

- 新しいユーザー向け機能
- 既存機能に対する明確な機能追加
- 機能追加に必要な仕様書、設計文書、実装、テスト
- 機能追加に伴って必要になる最小限のUI、データ構造、IPC、保存形式の変更

対象外:

- ユーザー確認なしの機能候補整理
- ユーザー確認なしの画面全体リデザイン
- 機能追加と関係しない見た目だけの調整
- 配布ビルド確認
- 既存Markdownファイルの一括変更

## 作業記録

このフェーズでは、機能追加の実装単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-05-27 00:00 JST

- ユーザー指示により、P4 機能追加フェーズを開始した。P4正本は最小構成とし、具体的な追加機能は次回以降に1件ずつ確認して決める扱いにした。今回の作業記録分として `app/package.json` のバージョンを `0.0.106` へ上げた。

### 2026-05-27 00:11 JST

- ユーザー指示により、Markdownの `mermaid` コードブロックをライブプレビューで図として表示する機能を追加した。Mermaidは遅延読み込みし、生成SVGを安全化して表示し、カーソルがブロック内にある場合は編集用にソース表示を維持する扱いにした。型チェック、全体テスト、差分空白チェック、rendererビルドを実施し、開発版Electron起動は `ERR_CONNECTION_REFUSED` で画面確認未達だった。今回の作業記録分として `app/package.json` のバージョンを `0.0.107` へ上げた。

### 2026-05-27 07:06 JST

- ユーザー指示により、開発版Electron起動時にVite dev serverへの初回接続が `ERR_CONNECTION_REFUSED` になる場合へ短時間リトライする処理を追加した。開発時の `loadURL` だけを対象にし、本番の `loadFile` は変更しない扱いにした。確認中に見つかった `write EIO` とMermaidライブプレビューのCodeMirror装飾クラッシュも修正し、開発版Electronで `101_mermaid_表示テスト.md` が画面表示され、Mermaid図のラベルが表示されることを確認した。対象テストと型チェックを実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.108` へ上げた。

### 2026-05-27 07:54 JST

- ユーザー指示により、Mermaidコードブロック対応の仕上げとして、通常プレビューHTMLへ `data-mermaid-source` を安全に保持し、描画時は属性値を優先して使う回帰テストを追加した。Mermaid描画失敗時は `console.warn` を出しつつ画面にはエラー内容を出さずソース表示へ戻すようにし、既存テーマに合わせてMermaidの `theme` を `default` / `dark` で初期化し直す処理を追加した。対象指定テスト実行時に全体テスト相当が通過し、`pnpm typecheck`、renderer build、`pnpm verify:full` を実施した。実アプリでのライト/ダーク切り替え目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.109` へ上げた。

### 2026-05-27 08:18 JST

- ユーザー指示により、`restoreMermaidSourceAttributes()` を削除し、通常プレビューHTML生成時に `data-mermaid-source` を安全な属性値として持たせ、DOMPurify の `ADD_ATTR` 許可で残す設計へ一本化した。Mermaidソース属性は `encodeURIComponent` 形式で保持し、描画時に復元して `dataset.mermaidSource` 優先の扱いを維持した。`pnpm test -- previewMarkdown mermaidPreview`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.110` へ上げた。

### 2026-05-27 13:52 JST

- ユーザー指示により、Mermaidソース属性の `uri:` 付き値がURI decodeできない場合は空文字列にし、壊れた `data-mermaid-source` をMermaid描画へ渡さない扱いへ変更した。prefixなし値をそのまま返す後方互換、正常なencoded値の復元、`dataset.mermaidSource` 優先と `code.textContent` fallback、安全設定とSVGサニタイズは維持した。`pnpm test -- mermaidSourceAttribute mermaidPreview previewMarkdown`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。未確認事項はない。今回の作業記録分として `app/package.json` のバージョンを `0.0.111` へ上げた。

### 2026-05-27 14:17 JST

- ユーザー指示により、大きなMermaid図が本文幅に強制縮小されないようSVGの自然幅を維持し、本文内で横スクロールできる表示へ調整した。Mermaid SVG描画成功時だけ拡大ボタンを追加し、SVGクリックまたはボタン押下で開く拡大ビュー、Esc/閉じるボタンでの終了、ズームイン・ズームアウト・リセット、ドラッグ移動を実装した。`securityLevel: "strict"`、`htmlLabels: false`、`flowchart.htmlLabels: false`、DOMPurifyサニタイズ、`data-mermaid-source` 優先と `code.textContent` fallback、描画失敗時フォールバックは維持した。`pnpm test -- mermaidPreview mermaidSourceAttribute previewMarkdown`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。未確認事項はない。今回の作業記録分として `app/package.json` のバージョンを `0.0.112` へ上げた。

### 2026-05-27 14:30 JST

- ユーザー指示により、Mermaid図の本文表示・拡大表示・編集状態の責務を仕上げた。本文内 `.preview-mermaid` は横スクロールとSVG強制縮小回避を維持したまま最大高さと縦スクロールを追加し、拡大ボタンは通常時に控えめ、hover/focus時に見やすくした。拡大ビューはズーム率表示の補助ラベルを追加し、背景クリックで閉じ、図・ツールバー操作では閉じない挙動、Ctrl+ホイール、ドラッグ移動、フォーカス復帰をテストで補強した。Mermaid安全設定、DOMPurifyサニタイズ、`data-mermaid-source` のencode/decode、壊れたソース属性を描画に渡さない挙動、編集中はソース表示を維持する挙動は維持した。`pnpm test -- mermaidPreview mermaidSourceAttribute previewMarkdown Editor`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.113` へ上げた。

### 2026-05-27 14:40 JST

- ユーザー指示により、Mermaid拡大ビューのviewport内ではCtrlなしの通常ホイールでズームし、wheelイベントを `preventDefault()` して背後やviewport自体のスクロールと競合しないようにした。ホイール位置を中心にズームするoffset調整、grab/grabbingカーソル、ドラッグ中の選択・dragstart抑制、pointerup/pointercancelでのdragging解除を追加し、本文内Mermaidのスクロール挙動とズームボタン・リセット・Esc/閉じる操作、Mermaid安全設定、DOMPurifyサニタイズ、`data-mermaid-source` encode/decode、壊れたソース属性を描画に渡さない挙動は維持した。`pnpm test -- mermaidPreview mermaidSourceAttribute previewMarkdown`、`pnpm test -- mermaidPreview`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.114` へ上げた。

### 2026-05-27 14:54 JST

- ユーザー指示により、Mermaid拡大ビューをGoogleマップ型のホイールズームと掴んで移動するパン操作へ整理した。拡大ビューviewportは `overflow: hidden` とし、スクロールバー移動ではなく `transform: translate(...) scale(...)` で視点を制御する扱いにした。クリックズームと本文内SVGクリック起動は使わず、ズームイン・ズームアウトボタンも削除し、入口は拡大ボタン、拡大ビュー内操作はホイールズーム・ドラッグ移動・リセット・閉じるに限定した。Mermaid安全設定、DOMPurifyサニタイズ、`data-mermaid-source` encode/decode、壊れたソース属性を描画に渡さない挙動、描画失敗時フォールバックは維持した。`pnpm test -- mermaidPreview mermaidSourceAttribute previewMarkdown`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.115` へ上げた。

### 2026-05-27 15:31 JST

- ユーザー指示により、Mermaid図のGoogleマップ型パン・ズーム操作をオーバーレイ内ではなく本文内表示領域そのものへ移した。Mermaid描画成功時は `.preview-mermaid-panzoom-viewport` と `.preview-mermaid-panzoom-content` を生成し、本文内viewportの通常ホイールでズーム、左ドラッグで `translate(...)` を更新する扱いにした。拡大ボタンとオーバーレイは削除し、本文内Mermaid領域は `overflow: hidden` としてスクロールバー移動を使わない扱いにした。Mermaid安全設定、DOMPurifyサニタイズ、`data-mermaid-source` encode/decode、壊れたソース属性を描画に渡さない挙動、描画失敗時フォールバックは維持した。`pnpm test -- mermaidPreview mermaidSourceAttribute previewMarkdown`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.116` へ上げた。

### 2026-05-27 17:59 JST

- ユーザー指示により、Mermaidライブプレビューはカーソルや選択範囲がブロックに触れただけではソース表示へ戻らず、Mermaid Widget上の「ソースを編集」ボタンでだけ明示的な編集状態へ入るようにした。編集状態はCodeMirrorのStateFieldで対象ブロック範囲を管理し、カーソルがブロック外へ出たら解除して図表示へ戻す扱いにした。Mermaidのクリック、ホイール、ドラッグなどのpan/zoom操作ではレンダリング表示を維持するようイベント伝播も抑制した。Mermaid安全設定、DOMPurifyサニタイズ、`data-mermaid-source` encode/decode、壊れたソース属性を描画に渡さない挙動、描画失敗時フォールバックは維持した。`pnpm test -- Editor mermaidPreview mermaidSourceAttribute previewMarkdown`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.117` へ上げた。

### 2026-05-27 18:54 JST

- ユーザー指示により、既存Mermaidレンダリングを活かして図ビュー操作を仕上げた。描画成功時に `MermaidRenderHandle` を返し、初回表示とライブプレビューの「全体表示」ボタンで同じ `fitToViewport()` を使う扱いにした。ホイールズームは倍率式に変更し、ドラッグ移動、イベント伝播抑制、専用エラーUI、危険なHTMLをDOM化しないテストを追加した。Mermaid安全設定、DOMPurifyサニタイズ、`data-mermaid-source` 優先、`previewMarkdown.ts` にUI操作ロジックを入れない責務分離は維持した。`pnpm test -- mermaidPreview Editor`、`pnpm typecheck`、`pnpm verify:full`、`pnpm vite build --config vite.renderer.config.ts` を実施した。`lint` は `app/package.json` に script がないため未実施。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.118` へ上げた。

### 2026-05-27 19:57 JST

- ユーザー指示により、Mermaid pan/zoom viewport をキーボード操作可能にし、`+`/`-` ズーム、`0`/`f` 全体表示、矢印キー移動、Shift+矢印キー大きめ移動、操作説明の `aria-label` を追加した。ResizeObserver による初回・未操作時の自動全体表示、手動操作後に勝手に戻さない制御、ホイールズームとドラッグ移動の `requestAnimationFrame` 集約、古い非同期描画結果とDOM外containerへの反映防止を実装した。Mermaid安全設定、DOMPurifyサニタイズ、`data-mermaid-source` 優先、描画失敗時エラーUI、ライブプレビューの「ソースを編集」責務は維持した。`pnpm test -- mermaidPreview`、`pnpm test -- Editor mermaidPreview mermaidSourceAttribute previewMarkdown`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.119` へ上げた。

### 2026-05-27 20:16 JST

- ユーザー指示により、Mermaidレンダリング状態の内部管理を整理し、`rendering`、`rendered`、`error`、`stale` を明示的な状態として扱うようにした。描画成功、描画失敗、DOM外containerへの古い描画結果破棄、後続描画による上書き防止の既存挙動は維持し、状態を `data-mermaid-render-status` にも反映して回帰テストで確認できるようにした。`pnpm test -- mermaidPreview`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.120` へ上げた。

### 2026-05-27 21:30 JST

- ユーザー指示により、左レールにキャンバスビューを追加し、試作用の `CanvasPanel` をパネルタブとして開けるようにした。キャンバス上で四角・ひし形・円のノード追加、ノード選択とドラッグ、ラベル編集、形変更、接続追加、削除、方向切り替え、生成Mermaidソース表示をローカル状態で試せるようにした。既存Markdownの解析・保存書き戻しは未実装の試作扱いにした。`pnpm test -- CanvasPanel appShellModel paneViewModel App`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.121` へ上げた。

### 2026-05-27 22:00 JST

- ユーザー指示により、試作用 `CanvasPanel` をMarkdown内の `mermaid` コードブロックを正本として編集するキャンバス機能へ変更した。対応対象は `flowchart TD` / `flowchart LR`、四角・ひし形・円、`id --> id` に限定し、ノード追加、選択、ラベル編集、形状変更、ノード削除、接続追加、接続削除、方向変更、Markdownタブ内容への書き戻しを実装した。Mermaidブロック0個では末尾に新規ブロック作成、1個では自動選択、2個以上では対象選択、未対応構文では読み取り専用にしてブロック内容を壊さない扱いにした。`pnpm test -- canvasMermaid CanvasPanel App`、`pnpm typecheck`、`pnpm test`、`pnpm verify:full`、`pnpm vite build --config vite.renderer.config.ts` を実施した。開発版Electronの目視確認は、ユーザーの既存ワークスペースを開く可能性があるため未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.122` へ上げた。

### 2026-05-27 22:27 JST

- ユーザー指示により、Mermaidキャンバス機能を独立タブ主導からMarkdown内 `mermaid` コードブロック起点のポップ型編集へ再設計した。ライブプレビューWidgetに「キャンバスで編集」を追加し、対象ブロックの `filePath`、`blockRange`、`source` だけを `CanvasPopover` へ渡して、`MermaidCanvasEditor` が編集済みsourceを返す扱いにした。独立 `CanvasPanel` は案内表示へ縮小し、左レールのキャンバス入口と複数ブロック選択UIを外した。ノード座標は `%% relic:canvas ...` metadata としてMermaidブロック内へ保存・復元するようにした。`pnpm test -- App Editor CanvasPanel canvasMermaid appShellModel`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.123` へ上げた。

### 2026-05-27 22:36 JST

- ユーザー指示により、キャンバス編集を開いた時点で既存Mermaid図がキャンバス上に表示されるよう、`graph TD; A-->B` の短縮表記、`;` 区切り、暗黙ノード、接続行内の `id[ラベル]` / `id{ラベル}` / `id((ラベル))` をキャンバスデータへ変換できるようにした。`MermaidCanvasEditor` は初期描画時から変換済みdiagramを持つようにし、既存ノードと接続が表示される回帰テストを追加した。`pnpm test -- canvasMermaid CanvasPanel Editor App`、`pnpm typecheck`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.124` へ上げた。

### 2026-05-27 22:53 JST

- ユーザー指示により、Mermaidキャンバス編集で `D -->|はい| E` のようなラベル付き接続を `CanvasEdge.label` として保持し、インラインノード定義付き接続を含む既存flowchartを読み取り専用にせずキャンバス表示できるようにした。`flowchart TD/LR` と `graph TD/LR`、暗黙ノード、行末セミコロン、同一ノード再定義の安全判定、HTML風ラベルの未対応扱いをテストで補強し、再生成時もラベル付き接続を保持するようにした。`pnpm test -- canvasMermaid`、`pnpm test -- CanvasPanel`、`pnpm verify` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.125` へ上げた。

### 2026-05-27 23:36 JST

- ユーザー指示により、Mermaidキャンバス編集を自由配置UIではなくMermaid記法の補助編集UIとして作り直した。座標、ノードドラッグ移動、グリッド、`%% relic:canvas` 出力、Canvas系の主要名称と画面文言を削除し、`MermaidVisualEditor`、`mermaidFlowchart.ts`、`MermaidEditorPopover` へ整理した。編集画面は通常MermaidレンダラーのSVGプレビューをクリック選択対象にし、ノード追加、削除、ID変更、ラベル変更、形状変更、接続追加、削除、接続ラベル変更、TD/LR切り替え、ソース編集、非対応構文時のビジュアル編集無効化を実装した。`pnpm typecheck`、`pnpm test`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.126` へ上げた。

### 2026-05-28 00:01 JST

- ユーザー指示により、Mermaid図編集の残課題として `TD/LR/TB/BT/RL` と `graph` 読み取り・`flowchart` 保存への正規化、Mermaidブロック編集セッションの `blockIndex` / `sourceHash` ベース再特定、対象ブロック削除時の安全停止、保存成功後だけの反映済み表示、ポップオーバー内イベント伝播抑制、レンダリングのdebounceとrequestId破棄を追加した。Markdown更新は保存済み範囲を直接信用せず、更新直前に現在文書からMermaidブロックを再スキャンして対象を決める扱いにした。複数Mermaidブロック、対象外本文変更、対象ブロック削除、パン/ズーム非保存、方向切り替え、旧メタデータ非出力の回帰テストを追加・更新した。`pnpm test -- mermaidFlowchart MermaidVisualEditor Editor`、`pnpm typecheck`、`pnpm test`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。`lint` は `app/package.json` に script がないため未実施。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.127` へ上げた。

### 2026-05-28 00:32 JST

- ユーザー指示により、Mermaid図のビジュアル編集UIを廃止し、Markdown内の図ブロックは表示とソース編集だけを扱う構成へ戻した。`MermaidVisualEditor`、`MermaidEditorPopover`、`mermaidFlowchart.ts`、Mermaid編集パネル、関連イベント、専用CSS、編集用テストを削除し、ライブプレビューの「Mermaidを図で編集」入口と左レールのMermaid編集パネルを外した。既存Mermaid表示は維持しつつ、`d2` コードブロックを `@terrastruct/d2` でSVG描画できるようにし、D2依存は専用の遅延chunkへ分離し、WebAssembly worker用のCSPを追加した。`previewMarkdown`、ライブプレビュー、通常プレビュー、Mermaid/D2共通レンダラー、関連テストを更新し、`pnpm typecheck`、`pnpm test`、`pnpm vite build --config vite.renderer.config.ts`、`pnpm verify:full` を実施した。`lint` は `app/package.json` に script がないため未実施。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.128` へ上げた。

### 2026-05-28 07:07 JST

- ユーザー指示により、既存のテスト用ファイル置き場である `app/src/test/` にD2描画確認用Markdown `d2-sample.md` を追加した。2つの `d2` コードブロックを含め、RelicでD2図の通常表示を確認できる内容にした。`pnpm verify:full` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.129` へ上げた。

### 2026-05-28 07:13 JST

- ユーザー指示により、誤って `app/src/test/` に追加したD2描画確認用Markdown `d2-sample.md` を削除した。今回の作業記録分として `app/package.json` のバージョンを `0.0.130` へ上げた。

### 2026-05-28 07:15 JST

- ユーザー指摘により、D2描画確認用Markdownの置き場所を既存のRelicテスト用ワークスペース `` と確認し、`102_d2_表示テスト.md` を同ディレクトリに作成した。今回の作業記録分として `app/package.json` のバージョンを `0.0.131` へ上げた。

### 2026-05-28 07:20 JST

- ユーザー指摘により、図表レンダリングエラー時に元ソースと詳細エラーをコピーできるボタンを追加し、エラー表示とフォールバックコード表示をテキスト選択可能にした。D2ブラウザ版が内部で `new Function()` を使うため、D2表示に必要なCSPとして `script-src` に `'unsafe-eval'` を追加した。今回の作業記録分として `app/package.json` のバージョンを `0.0.132` へ上げた。

### 2026-05-28 07:33 JST

- ユーザー指摘により、複数D2コードブロックを同時に描画するとD2 workerの応答が混ざり、片方が `rendering` のまま残る問題を確認した。D2描画だけを直列キュー化し、D2 rendererがSVG文字列を返さない場合はエラー表示へ逃がすようにした。`102_d2_表示テスト.md` の2つのD2ブロックが非表示Electron確認でどちらもSVG化されることを確認し、`pnpm test -- mermaidPreview`、`pnpm typecheck`、renderer build、`pnpm verify:full` を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.133` へ上げた。

### 2026-05-28 08:33 JST

- ユーザー指示により、D2 / Mermaid の共通処理を Diagram として整理し、`mermaidPreview`、`editorMermaidEditState`、`mermaidSourceAttribute` を Diagram 名へ改名した。`data-diagram-source` を共通ソース属性の正とし、共通UIのCSSを `preview-diagram-*` / `cm-live-diagram-*` へ統一した。D2の「ソースを編集」が無効化される原因だった編集範囲検証を `diagramLanguageFor()` ベースへ変更し、D2 / Mermaid 両方を許可するよう修正した。ライブプレビューのコードフェンス判定は開始・終了バッククォート数を合わせ、4連バッククォート内の3連diagramを図として誤検出しないようにした。`pnpm typecheck`、`pnpm test`、`pnpm verify`、`git diff --check` を実施した。実アプリ目視は未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.134` へ上げた。

### 2026-05-28 13:11 JST

- ユーザー指摘により、開発版起動後に白い画面が残る問題を調査し、開発版Electronの再起動・停止時にウィンドウ閉じる保護が働いて古いElectronプロセスが残る可能性を確認した。開発版のアプリ終了中だけ閉じる保護を通さず終了できるようにし、`pnpm start` 停止後に `5173` の待ち受けが残らないことを確認した。`pnpm typecheck`、`pnpm test`、`pnpm verify`、renderer build、`git diff --check` を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.135` へ上げた。

### 2026-05-28 13:35 JST

- ユーザー指摘により、Windows用の `Relicを起動.bat` と `Relicをビルド.bat` が `node_modules` の有無だけで依存関係更新を判断していたため、新規追加されたD2依存が入らない問題を修正した。起動・ビルド前に毎回 `pnpm install` を実行し、追加依存が自動で反映されるようにした。`pnpm verify`、`git diff --check` を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.136` へ上げた。

### 2026-05-28 15:41 JST

- ユーザー指摘により、Windows用batの `pnpm install` が `@terrastruct/d2` 取得時に `ETIMEDOUT` で失敗する状況へ対応した。起動・ビルドbatの依存関係更新に `--fetch-timeout 300000`、`--fetch-retries 5`、`--network-concurrency 2` を指定し、遅い回線でも待ち時間と再試行回数を増やすようにした。失敗時はネットワーク、VPN、プロキシ確認を促す文言を表示するようにした。`pnpm verify`、`git diff --check` を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.137` へ上げた。

### 2026-05-28 18:19 JST

- ユーザー指示により、Diagram / Mermaid / D2 周辺コードの挙動を変えずに責務分離した。Diagram言語判定、Mermaidレンダラー、D2レンダラーと直列キュー、pan/zoom、エラー表示、ライブプレビューDiagram Widgetを個別ファイルへ分け、通常プレビューとライブプレビューの `data-diagram-source`、`preview-diagram-*` / `cm-live-diagram-*`、D2直列描画、SVGサニタイズ、ソース編集導線は維持した。`pnpm typecheck`、`pnpm test`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git diff --check` を実施した。`lint` は `app/package.json` に script がないため未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.138` へ上げた。

### 2026-05-28 18:44 JST

- ユーザー指示により、`diagramPreview.ts` に残っていたDiagram描画状態管理を新規 `diagramRenderState.ts` へ分離した。非同期描画token、現在描画判定、detached / superseded のstale化、rendered / error 記録、`data-diagram-render-status` 更新を移し、SVG描画、DOMPurifyサニタイズ、pan/zoom、エラーUI生成、D2直列キュー、Mermaid初期化設定は変更しない扱いにした。`pnpm test -- diagramPreview`、`pnpm typecheck`、`pnpm test`、`pnpm verify`、`pnpm vite build --config vite.renderer.config.ts`、`git -C .. diff --check` を実施した。`lint` は `app/package.json` に script がないため未実施。今回の作業記録分として `app/package.json` のバージョンを `0.0.139` へ上げた。

### 2026-05-28 19:49 JST

- ユーザー指示により、コードを正として `docs/` 全体を棚卸しし、現行仕様文書を更新した。Markdown図表仕様、エディタ挙動、エディタエンジン、技術スタック、アーキテクチャ、データモデル、設計判断、画面・ナビゲーション、用語を、現行の Diagram / Mermaid / D2 共通実装、D2直列描画、`data-diagram-source`、図表pan/zoom、明示的な「ソースを編集」導線、暦設定パネルに合わせた。`git diff --check` と旧記述検索を実施した。今回の作業記録分として `app/package.json` のバージョンを `0.0.140` へ上げた。

### 2026-05-28 19:53 JST

- ユーザー指示により、P4 機能追加フェーズを終了し、P5 実用機能の実装と細部の調整フェーズへ移行した。P5は最小構成の正本として開始し、具体的な実用機能や細部調整は次回以降に1件ずつ確認して決める扱いにした。今回の作業記録分として `app/package.json` のバージョンを `0.0.141` へ上げた。
