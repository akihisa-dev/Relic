# P11

Relicのマークダウンエディタ改善フェーズの正本。

このフェーズでは、RelicのMarkdown正本という前提を保ちながら、Markdown編集体験、ライブプレビュー、フロントマター編集、図表・表・リンクなどエディタ内操作の改善を進める。

---

## フェーズ固有ルール・制約

AIはこのフェーズを前提にユーザーへ接する。

### 読み込みルール

最初に読む:

- この文書

必要になったときだけ読む:

- プロダクト意図を確認する: `../../project/overview.md`
- Markdownエディタ仕様との整合を見る: `../../features/markdown.md`, `../../features/frontmatter.md`
- 図表やプレビュー仕様を見る: `../../features/editor.md`, `../../features/markdown.md`, `../../features/navigation.md`
- 画面・遷移・見た目を確認する: `../../design/DESIGN.md`
- アーキテクチャやデータ構造に触れる場合: `../../engineering/architecture.md`, `../../engineering/data-model.md`, `../../engineering/decisions.md`
- 実装規約とテスト方針を確認する: `../coding-rules.md`, `../testing-rules.md`
- 未決定事項を扱う場合: `../questions.md`
- バージョンを切る場合: `../versioning-rules.md`

注意:

- 対象範囲は、ユーザーが明示したMarkdownエディタ改善に限る。
- Markdown保存形式、ローカルファイル操作仕様、AI機能や外部サービス利用方針、画面全体の設計変更は、ユーザー確認なしに変更しない。
- ライブプレビューや装飾表示を改善する場合も、Markdown本文そのものを勝手に変換しない。
- CodeMirror、エディタ状態管理、自動保存、外部変更検知に触れる場合は、既存の未保存本文保護と衝突検知を壊さない。
- 仕様書、設計書、チェックリスト、詳細な作業項目は、ユーザーの明示指示または合意なしに作成しない。
- 小規模修正でも、関連ファイルの更新がユーザー指示範囲を越える場合は、理由と対象を説明してから確認する。
- React Doctorなどの診断ツールは判断材料として使ってよいが、点数を上げるためだけにエディタ本体や状態管理を大きく作り替えない。
- 未決定の問いは仕様書や設計書に書かず、必要になった場合だけ `../questions.md` に一元管理する。

---

## 対象範囲

- Markdown本文編集の操作性改善
- ライブプレビューと通常プレビューの表示・操作改善
- フロントマター編集UIの改善
- 表、リンク、図表、コードブロックなどエディタ内要素の操作改善
- キーボード操作、選択範囲操作、コンテキストメニューの改善
- エディタ周辺の小規模な不具合修正
- 既存仕様に沿ったテスト追加・補強
- 変更に必要な最小限の関連文書更新

対象外:

- ユーザー確認なしのMarkdown保存形式変更
- ユーザー確認なしのエディタ全面刷新
- ユーザー確認なしの画面全体リデザイン
- ユーザー確認なしのローカルファイル操作仕様変更
- ユーザー確認なしのAI機能や外部サービス利用方針変更
- 配布ビルド確認

## 作業記録

このフェーズでは、Markdownエディタ改善の作業単位ごとに次回作業時の判断に必要な状態だけを記録する。
作業記録は `YYYY-MM-DD HH:mm JST` 形式の日時付きで記録する。

### 2026-06-04 20:52 JST

- ユーザー指示により、P10 機能改善・追加・小規模修正フェーズを終了し、P11 マークダウンエディタ改善フェーズを開始した。P11ではMarkdown正本を保ちながら、Markdown本文編集、ライブプレビュー、フロントマター編集、表・リンク・図表・コードブロックなどエディタ内操作の改善を対象にする。今回のフェーズ移行作業として `app/package.json` のバージョンを `0.2.2` へ上げる。

### 2026-06-04 21:23 JST

- Markdownエディタ監査の修正候補として、プロトコル相対Markdownリンクをワークスペース内ファイル解決から除外し、SVG保存・コピーIPCでmain側でも危険なSVG要素・属性を除去するよう修正した。関連テストを追加し、`pnpm verify` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.3` へ上げる。

### 2026-06-04 21:55 JST

- Markdownエディタ改善として、CodeMirror Compartmentで設定・表示モードを差し替えてEditorViewを維持し、ライブプレビューのコードフェンス・表検出を構文木中心に見直し、Wikiリンク補完のランキング・重複名・aliases候補、巨大Markdownのfallback回帰テスト、数式・脚注ライブプレビューを追加した。関連仕様・設計文書とテストを更新し、`pnpm verify` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.4` へ上げる。

### 2026-06-04 22:10 JST

- Markdownエディタの追加安定化として、コードブロック・表ライブプレビューの通常入力時はDecorationSetをmapして再走査を抑え、Wikiリンク補完の正規化済み検索語と候補数制限を追加し、IME変換中判定、設定変更後のスクロール・フォーカス維持、大量補完の回帰テストを補強した。関連設計文書とテストを更新し、`pnpm verify` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.5` へ上げる。

### 2026-06-04 22:39 JST

- Markdownエディタの追加改善として、巨大Markdown fallbackをPaneからApp通知へ接続し、1MiB超・1行80,000文字超で一時的にソース表示にしてライブプレビュー拡張を外す挙動をテストで補強した。Wikiリンク補完は完全一致・前方一致・部分一致の索引化を行い、通常コードブロックは選択変更時の不要な再走査を抑えた。関連仕様・設計文書とテストを更新し、`pnpm --dir app verify` と `git diff --check` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.6` へ上げる。

### 2026-06-04 22:44 JST

- ライブプレビューの残存全文走査削減として、通常コードブロックと表のブロックDecorationはStateField提供のまま、ViewPluginから可視範囲をStateEffectで同期し、初期生成と再構築を可視範囲中心にした。関連設計文書を更新し、`pnpm --dir app verify` と `git diff --check` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.7` へ上げる。

### 2026-06-04 23:27 JST

- Markdownエディタの打ち心地を守る品質ゲートとして、EditorView identity、undo/redo、IME composition、selection、scroll、CodeBlock/Table decoration再構築抑制、Wiki補完大量候補、巨大Markdown fallback、大規模文書smokeのテストを補強した。IME中のEnterがCodeMirror既定キー処理へ流れないようイベント優先度とcomposition終了管理を補強し、`pnpm --dir app verify` と `git diff --check` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.8` へ上げる。

### 2026-06-04 23:53 JST

- エディタ品質改善のためのデザイン文書整理として、ご提示の方針をもとに `docs/design/DESIGN.md` を作成し、構造化・可読性・集中体験を重視するビジュアルデザイン方針を記録した。今回は実装変更を行わず、文書整理として `app/package.json` のバージョンを `0.2.9` へ上げる。

### 2026-06-05 00:05 JST

- ユーザー指示により、デザイン系文書を `docs/design/DESIGN.md` に完全統合し、旧 `docs/design/system.md`、`docs/design/screens.md`、`docs/design/navigation.md` を削除した。関連文書の参照先を `docs/design/DESIGN.md` へ更新し、文書整理として `app/package.json` のバージョンを `0.2.10` へ上げる。

### 2026-06-05 00:06 JST

- ユーザー指示により、`docs/design/DESIGN.md` から旧 `Monochrome Blue` 系の既存トークン参照と旧デザイン制約を削除し、添付画像と新しいDesign System本文に沿う構造化・青系・幾何学的な方針だけを正として残した。文書整理として `app/package.json` のバージョンを `0.2.11` へ上げる。

### 2026-06-05 00:24 JST

- 新しい `docs/design/DESIGN.md` に沿って、レンダラー全体へ構造化された青系・境界線中心のビジュアルトークンを追加し、シェル、レール、サイドバー、タブ、エディタ、右アウトライン、ステータスバーを新デザインへ寄せた。右アウトラインを初期表示に変更し、番号付きアウトラインとパネル見出しを追加した。開発版ブラウザで初期画面の右アウトライン・装飾表示を確認し、`pnpm --dir app verify` と `git diff --check` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.12` へ上げる。

### 2026-06-05 00:37 JST

- 添付画像により近づける追加調整として、周辺UIのフォント指定を控えめにし、タブ、ステータスバー、右パネル見出し、表見出し、設定系見出しのウェイト・字間・uppercase表現を抑えた。大見出しとアウトライン番号の強さは維持し、開発版ブラウザの横長表示で確認した。`pnpm --dir app verify` と `git diff --check` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.13` へ上げる。

### 2026-06-05 07:17 JST

- ユーザー指定の `PROJEKT KOSMOS COLOR PALETTE` に合わせて、`docs/design/DESIGN.md` のカラーパレットとレンダラーのデザイントークンを更新した。Primary Blue `#3261A1`、Sub Blue `#6C8BB7`、Background `#F2F1EE`、Surface `#E7E7E7`、Surface Alt `#DDDEDF`、Border `#C3C8CE`、Border Strong `#A4AEBD`、Primary Text `#232B36`、Secondary Text `#89909A` をアプリへ反映し、開発版ブラウザで主要CSS変数と初期画面表示を確認した。`pnpm --dir app verify` と `git diff --check` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.14` へ上げる。

### 2026-06-05 07:27 JST

- ユーザー指示により、`ローカルのカラーパレットHTML` をたたき台として、`docs/design/DESIGN.md` の内容を視覚化した `docs/design/DESIGN.html` を作成した。カラーパレット、デザイン原則、ビジュアル言語、タイポグラフィ、画面構造、ナビゲーション、コンポーネント方針を静的HTML文書として整理し、ローカルHTTP経由のブラウザ表示と `git diff --check` で確認した。今回の修正として `app/package.json` のバージョンを `0.2.15` へ上げる。

### 2026-06-05 18:14 JST

- Markdownエディタのマーク適用品質改善として、ツールバー・右クリックメニュー共通のMarkdown書式処理で、太字、斜体、打ち消し線、ハイライト、下線、インラインコード、見出し、引用、コードブロック、Markdownリンク、内部リンクの既存記法解除と見出しレベル置き換えを追加し、回帰テストを補強した。対象テストと型チェックで確認した。今回の修正として `app/package.json` のバージョンを `0.2.16` へ上げる。

### 2026-06-05 19:12 JST

- Markdownエディタの地味な品質向上の1件目として、ツールバー経由のマーク適用、Markdownリンク挿入、表挿入がUndo/Redoで1操作として戻ることと、太字化・リンク化・見出し化後に本文側の選択範囲を保持することを実エディタテストで補強した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.17` へ上げる。

### 2026-06-05 19:15 JST

- Markdownエディタの地味な品質向上の2件目として、空選択時の太字・Markdownリンク・コードブロック挿入のプレースホルダーとカーソル位置を実エディタテストで補強し、空のコードブロック挿入後は本文を書ける空行へカーソルを置くよう修正した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.18` へ上げる。

### 2026-06-05 19:18 JST

- Markdownエディタの地味な品質向上の3件目として、IME入力中の安全性を補強し、compositionstart中、`event.isComposing`、`keyCode 229` の各状態でTab段下げとAlt+上下の行移動ショートカットが実行されないことを実エディタテストで確認した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.19` へ上げる。

### 2026-06-05 19:22 JST

- Markdownエディタの地味な品質向上の4件目として、Markdownリンク内のURLだけを選択した場合とリンク内にカーソルがある場合はURLだけを更新し、リンク本文を選択状態に戻すよう修正した。既存リンク本文選択とリンク全体選択の解除挙動は維持し、対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.20` へ上げる。

### 2026-06-05 19:25 JST

- Markdownエディタの地味な品質向上の5件目として、リスト系ボタンで途中だけ選択した場合に対象行だけを変換することをテストで補強し、番号付きリスト変換ではインデント階層ごとに番号を振り直すよう修正した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.21` へ上げる。

### 2026-06-05 19:29 JST

- Markdownエディタの地味な品質向上の6件目として、フェンスコードブロック内ではWikiリンク補完、EnterによるMarkdownリスト補完、ツールバーのマーク適用・リスト化が本文を書き換えないよう抑止した。関連テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.22` へ上げる。

### 2026-06-05 19:33 JST

- Markdownエディタの地味な品質向上の7件目として、ライブプレビューで装飾文字にカーソルが触れた場合は編集用Markdown記法を隠さず、カーソル外では読みやすい置換表示を維持することをテストで補強した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.23` へ上げる。

### 2026-06-05 19:58 JST

- Markdownエディタの保存・復元信頼性の1件目として、未保存の開きタブをファイル移動する前に即時保存し、保存失敗時は移動せず本文とパスを維持することをテストで補強した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.24` へ上げる。

### 2026-06-05 20:02 JST

- Markdownエディタのカーソル・スクロール安定化の1件目として、アウトラインやリンク先見出しへのジャンプ時にスクロールだけでなくカーソルも対象見出し行へ移すよう修正し、Pane単位のテストで補強した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.25` へ上げる。

### 2026-06-05 20:05 JST

- Markdownエディタのコピー・ペースト品質の1件目として、本文への複数行日本語・コード混在テキスト貼り付けと、ライブプレビュー表セルへのTSV貼り付けが1回のUndo/Redoで戻せることをテストで補強した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.26` へ上げる。

### 2026-06-05 20:08 JST

- Markdownエディタの検索・置換安全性の1件目として、通常文字列置換では置換後テキスト内の `$&` や `$1` を特殊記号として扱わず文字どおり保存・プレビュー・一括置換するよう修正した。正規表現置換のキャプチャ参照は維持し、関連テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.27` へ上げる。

### 2026-06-05 20:12 JST

- Markdownエディタの大きい文書での打ち心地改善の1件目として、ライブプレビュー表が120行または720セルを超える場合は表Widget化せず、Markdown本文表示のままにして大量の入力欄生成を避けるよう修正した。通常サイズ表の挙動維持と大きすぎる表の回避を対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.28` へ上げる。

### 2026-06-05 20:17 JST

- MarkdownエディタのMarkdown構文境界品質の1件目として、MarkdownリンクURL内の括弧をリンク範囲として扱い、閉じていないリンクはライブプレビュー装飾しないようリンク解析を補強した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.29` へ上げる。

### 2026-06-05 20:21 JST

- MarkdownエディタのMarkdown構文境界品質の2件目として、複数バッククォートのインラインコードで外側の境界を使い、コード内のMarkdownリンク記法をライブプレビュー装飾しないようコード解析を補強した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.30` へ上げる。

### 2026-06-05 20:24 JST

- MarkdownエディタのMarkdown構文境界品質の3件目として、HTMLタグ属性内のMarkdownリンク風テキストを本文リンクとしてライブプレビュー装飾しないよう、HTMLタグ範囲を保護してインライン装飾を抑止した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.31` へ上げる。

### 2026-06-05 20:29 JST

- Markdownエディタのアクセシビリティ・キーボード操作の1件目として、ツールバーなしでも `Mod+B` と `Mod+I` で選択範囲へ太字・斜体のMarkdown記法を適用し、選択範囲を維持できるようショートカットを追加した。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.32` へ上げる。

### 2026-06-05 20:34 JST

- Markdownエディタのアクセシビリティ・キーボード操作の2件目として、CodeMirror本文入力欄へ翻訳済みの `aria-label` を付け、スクリーンリーダーやテスト上で名前付きtextboxとして認識できるようにした。対象テストで確認した。今回の修正として `app/package.json` のバージョンを `0.2.33` へ上げる。

### 2026-06-05 20:39 JST

- Markdownエディタの検索・置換安全性の2件目として、一括置換の適用時もプレビュー時と同じく読めないMarkdownファイルをスキップし、読めるファイルだけを置換対象として処理を続行するよう修正した。対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.34` へ上げる。

### 2026-06-05 20:43 JST

- Markdownエディタの大きい文書での打ち心地改善の2件目として、見出し折りたたみ範囲の探索に2,000行の上限を設け、巨大な章では折りたたみ範囲を作らず入力時の長距離走査を避けるよう修正した。対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.35` へ上げる。

### 2026-06-05 20:48 JST

- Markdownエディタの保存・復元信頼性の2件目として、ウィンドウクローズ要求中の保存確認が例外になった場合でも未処理にせず、閉じない応答を返して本文保護を優先するよう修正した。対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.36` へ上げる。

### 2026-06-05 20:55 JST

- Markdownエディタのカーソル・スクロール安定化の2件目として、アウトライン見出しに本文内の開始位置を持たせ、同じ見出し名が複数ある場合もクリックした項目の位置へカーソルとスクロールを移せるよう修正した。Wikiリンクなど文字列指定の見出しジャンプは従来どおり維持し、対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.37` へ上げる。

### 2026-06-05 20:58 JST

- Markdownエディタのコピー・ペースト品質の2件目として、右クリックメニューのペースト時にElectron側のクリップボード読み取りが例外になってもブラウザ側のクリップボード読み取りへフォールバックし、本文への貼り付けを継続できるよう修正した。対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.38` へ上げる。

### 2026-06-05 21:02 JST

- Markdownエディタの検索・置換安全性の3件目として、正規表現置換で空文字に一致する条件を受け付けず、本文の境界へ大量に置換が入る事故を防ぐよう修正した。単一ファイル置換、置換プレビュー、一括置換の対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.39` へ上げる。

### 2026-06-05 21:05 JST

- Markdownエディタの大きい文書での打ち心地改善の3件目として、通常サイズでも見出しが極端に多いMarkdownで右アウトラインが大量項目を生成しないよう、アウトライン抽出を1,000件で止める上限を追加した。対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.40` へ上げる。

### 2026-06-05 21:08 JST

- MarkdownエディタのMarkdown構文境界品質の4件目として、単語内のアンダースコアを斜体記法としてライブプレビュー装飾しないよう修正し、`foo_bar_baz` のような識別子やファイル名を書いている途中でも誤変換しないようにした。通常の `_italic_` 装飾は維持し、対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.41` へ上げる。

### 2026-06-05 21:12 JST

- Markdownエディタの保存・復元信頼性の3件目として、外部更新でファイル一覧から消えた開きタブでも、未保存本文または外部変更衝突を持つ場合はタブを閉じず、本文を開いたまま保持して通知するよう修正した。保存済みタブは従来どおり閉じる。対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.42` へ上げる。

### 2026-06-05 21:17 JST

- Markdownエディタのカーソル・スクロール安定化の3件目として、検索結果の行番号をファイルオープン処理へ渡し、検索結果クリック後に対象行へカーソルとスクロールを移すよう修正した。見出しジャンプの既存挙動は維持し、対象テストと全体検証で確認した。今回の修正として `app/package.json` のバージョンを `0.2.43` へ上げる。
