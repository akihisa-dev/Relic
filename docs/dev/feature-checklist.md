# feature-checklist.md

Relicアプリの全機能・UI確認チェックリスト。
コード確認・自動テスト・ビルド・実アプリ操作のいずれかで確認できたら `[x]` にする。
実アプリ操作でしか判断できない項目は、ユーザーが明示的に求めるまで保留してよい。

現在の開発位置・セッション開始時に読む文書は [`phases.md`](phases.md) を参照する。

---

## P9 安定化メモ

- [x] `App.test.tsx` の React `act(...)` 警告を解消する

---

## 1. ローカライズ基盤

- [x] アプリのデフォルト表示言語を英語にする
- [x] UI文言を `t()` 経由にし、コンポーネント内の固定文言を翻訳キーへ置き換える
- [x] `en.json` に英語文言を定義する
- [x] `ja.json` に日本語文言を定義する
- [x] 設定画面で言語を「System / English / 日本語」から選択できる
- [x] 言語設定を保存してアプリ再起動後も反映される
- [x] OS言語または設定に応じて英語・日本語表示が切り替わる
- [x] 英語表示でも主要UIのラベル・ボタン文言が収まる
- [x] 日本語表示でも既存の意味・操作名が維持されている

---

## 2. ワークスペース管理

- [x] 既存フォルダをワークスペースとして開く（「ワークスペースを開く」ボタン）
- [x] 新規フォルダを作成してワークスペースとして登録する
- [x] 複数ワークスペースを登録し、一覧から切り替えられる
- [x] アプリ再起動後に前回のワークスペースが復元される
- [x] ワークスペースを開いたとき `attachments/` と `templates/` フォルダが自動作成される

---

## 3. ファイル・フォルダ操作

- [x] Markdownファイルを新規作成できる（ファイル名入力→作成）
- [x] フォルダを新規作成できる
- [x] ファイルをリネームできる（RenameBarのインライン入力）
- [x] フォルダをリネームできる
- [x] ファイルを複製できる（コマンドパレット「ファイルを複製」）
- [x] ファイルをゴミ箱に移動できる（コマンドパレット「ファイルを削除」）
- [x] ファイルをドラッグ&ドロップで別フォルダに移動できる
- [x] フォルダをドラッグ&ドロップで別フォルダに移動できる
- [x] ファイル・フォルダをピン留めできる（ピンボタン表示）
- [x] ピン留めしたファイル・フォルダがツリー上部に表示される
- [x] ピン留めを解除できる
- [x] `[[ファイル名]]` のリンクから新規ファイルを作成できる（リンク先不存在時）

---

## 4. ファイルツリービュー

- [x] ワークスペース内の `.md` ファイルとフォルダがツリー表示される
- [x] `.md` 拡張子が非表示になっている
- [x] 非Markdownファイルがツリーに表示されない
- [x] フォルダを開閉できる
- [x] アクティブなファイルがツリーでハイライトされる
- [x] サイドバー幅をドラッグで変更できる（最小180px・最大500px）

---

## 5. エディタ（編集機能）

- [x] ファイルをクリックするとエディタで開く
- [x] テキストを入力・編集できる
- [x] 入力停止後に自動保存される
- [x] Undo / Redo が動作する（⌘Z / ⌘⇧Z）
- [x] `[[` を入力するとファイル名補完が表示される
- [x] ライブプレビュー：カーソルのない行の見出し記号（`#`）が非表示になる
- [x] ライブプレビュー：カーソルのない行の **太字**・*斜体*・~~打消し~~ が装飾表示される
- [x] ライブプレビュー：インラインコードが装飾表示される
- [x] ライブプレビュー：カーソルがある行はMarkdown記法のまま表示される

---

## 6. ツールバー（書式挿入）

- [x] 太字（**B**）ボタンで `**テキスト**` が挿入される
- [x] 斜体（*I*）ボタンで `*テキスト*` が挿入される
- [x] 打ち消し線（S）ボタンで `~~テキスト~~` が挿入される
- [x] ハイライト（H）ボタンで `==テキスト==` が挿入される
- [x] 下線（U）ボタンで `<u>テキスト</u>` が挿入される
- [x] インラインコード（`）ボタンで `` `code` `` が挿入される
- [x] 見出しドロップダウンから H1〜H6 が挿入される
- [x] 引用（>）ボタンで `> 引用` が挿入される
- [x] コードブロックボタンで ` ``` ` が挿入される
- [x] 水平線（—）ボタンで `---` が挿入される
- [x] 箇条書きボタンで `- ` が挿入される
- [x] 番号付きリストボタンで `1. ` が挿入される
- [x] チェックボックスボタンで `- [ ] ` が挿入される
- [x] リンクボタンでダイアログが開き `[テキスト](URL)` が挿入される
- [x] 内部リンクボタンで `[[]]` が挿入される
- [x] 表ボタンでダイアログが開き、行数・列数を指定してMarkdown表が挿入される

---

## 7. プレビュー

- [x] ソースモード⇔プレビューモードを切り替えられる
- [x] 見出し・太字・斜体・リスト・表・引用が正しくレンダリングされる
- [x] コードブロックに構文ハイライトが適用される
- [x] `$...$` / `$$...$$` で数式（KaTeX）がレンダリングされる
- [x] `==ハイライト==` が黄色背景で表示される
- [x] `<u>テキスト</u>` が下線テキストで表示される
- [x] `[[ファイル名]]` の内部リンクをクリックするとそのファイルが開く
- [x] `[[ファイル名#見出し]]` で特定の見出し位置にジャンプする
- [x] `![[ファイル名]]` で他のMarkdownファイルの内容が埋め込まれる
- [x] `![[画像ファイル名]]` で attachments フォルダの画像が表示される
- [x] `[^1]` / `[^1]: 注釈` の脚注が末尾にレンダリングされる
- [x] プレビュー上のチェックボックスをクリックするとソースが更新される
- [x] `#tag` タグをクリックするとタグ検索が起動する

---

## 8. 表示モード・レイアウト

- [x] 分割表示（⌘\）で左右2ペインになる
- [x] 分割解除すると右ペインのタブが左ペインに移動する
- [x] フォーカスモードでサイドバーと右パネルが非表示になる
- [x] タイプライターモードでカーソル行が常に画面中央に保たれる
- [x] フォーカスモードとタイプライターモードを同時に使用できる
- [x] 右パネル（アウトライン）を開閉できる（⌘⇧B）
- [x] 右パネル（リンク）を開閉できる

---

## 9. タブ管理

- [x] 複数ファイルをタブで同時に開ける
- [x] タブをクリックして切り替えられる
- [x] タブを閉じられる（⌘W）
- [x] タブを閉じた後、履歴の直前のタブが表示される
- [x] 分割表示中、左右ペインでそれぞれ独立したタブ列を持てる
- [x] ファイルがない状態で新規ノート作成フォームが表示される

---

## 10. アウトライン・リンクパネル（右パネル）

- [x] アウトラインパネルに現在のファイルの見出し一覧が表示される
- [x] 見出しをクリックすると対応箇所にジャンプする
- [x] リンクパネルにバックリンク（このファイルを参照するファイル）が表示される
- [x] リンクパネルにアウトゴーイングリンク（このファイルが参照するファイル）が表示される
- [x] リンクパネルの各リンクをクリックするとそのファイルが開く

---

## 11. 検索・置換

- [x] 検索サイドバーでワークスペース全体を全文検索できる
- [x] ファイル名検索モードが動作する
- [x] タグ検索モードが動作する
- [x] 正規表現検索モードが動作する
- [x] フロントマター検索モードが動作する
- [x] 正規表現が無効な場合にエラー表示される
- [x] 検索結果をクリックするとファイルが開く
- [x] 置換タブでアクティブファイル内の文字列を置換できる
- [x] 置換タブで一括置換（全ファイル）の変更プレビューを確認できる
- [x] 一括置換を実行できる
- [x] よく使う正規表現パターンを選択して入力欄に挿入できる
- [x] タグ一覧が表示され、クリックするとタグ検索が起動する

---

## 12. フロントマター

- [x] ファイル先頭に `---` で囲まれたYAMLがある場合にフロントマターフォームが表示される
- [x] `tags` フィールドをピル形式で追加・削除できる
- [x] `aliases` フィールドを追加・削除できる
- [x] `date`, `status`, `publish`, `url`, `author` フィールドを編集できる
- [x] フロントマターテンプレート（`frontmatter.md`）を作成できる
- [x] テンプレートに基づいた候補値が入力補完で表示される
- [x] フロントマターセクションを折りたたみ・展開できる
- [x] 機能トグルでフロントマターを非表示にできる

---

## 13. Git 連携

- [x] Gitリポジトリが未初期化の場合に「初期化」ボタンが表示される
- [x] `git init` でリポジトリが初期化される
- [x] 現在のブランチ名が表示される
- [x] 未コミット変更ファイルがステータス付きで一覧表示される（added/modified/deleted/untracked）
- [x] コミットメッセージ・作者名・メールを入力してコミットできる
- [x] ブランチを新規作成できる
- [x] ブランチを切り替えられる
- [x] 未コミット変更がある状態でブランチ切り替えを試みると警告が表示される
- [x] コミット履歴が一覧表示される
- [x] コミットをクリックして差分を左右対比で確認できる
- [x] Gitタグを作成できる（軽量・注釈付き）
- [x] Gitタグを削除できる
- [x] リモートリポジトリを接続できる（URL入力）
- [x] Push のプレビュー（送信予定の変更）を確認してから実行できる
- [x] Pull のプレビュー（受信予定のコミット）を確認してから実行できる
- [x] Gitタグをリモートにプッシュできる
- [x] GitHubアカウントと OAuth 連携できる
- [x] GitHubリポジトリをクローンしてワークスペースとして開ける
- [x] マージコンフリクトのファイルが表示される
- [x] コンフリクトを "ours" または "theirs" で解決できる
- [x] 自動同期（Auto pull/push）の有効化・間隔を設定できる
- [x] 機能トグルでGit機能を非表示にできる

---

## 14. ファイル加工ツール

- [x] タイトル一覧生成：フォルダ・ソート順を指定して `[[ファイル名]]` の箇条書きファイルを生成できる
- [x] 目次生成：フォルダ構造をMarkdown形式で出力できる（サブフォルダ含むオプション付き）
- [x] 条件指定マージ：「すべて」「フォルダ指定」「タグ指定」でファイルを絞り込んで1ファイルにマージできる
- [x] 条件指定マージ：ソート順（ファイル名/更新日時/作成日時）を指定できる
- [x] 条件指定マージ：ファイル名を見出しとして挿入するオプションが機能する
- [x] 条件指定マージ：既存の同名ファイルがある場合に別名で保存される
- [x] 見出しで分割：H1/H2/H3 を指定してファイルを複数ファイルに分割できる
- [x] 見出しで分割：生成されるファイル名がファイル名に使えない文字を変換している
- [x] 各ツールの実行結果（完了/エラー）がステータス表示される
- [x] 機能トグルでツールを非表示にできる

---

## 15. コマンドパレット

- [x] ⌘⇧P でコマンドパレットが開く
- [x] 入力に応じてコマンドが絞り込まれる
- [x] 矢印キーでコマンドを選択、Enter で実行できる
- [x] Escape で閉じる
- [x] 「新規ノートを作成」（⌘N）が実行できる
- [x] 「検索を開く」（⌘F）が実行できる
- [x] 「クイックスイッチャーを開く」（⌘P）が実行できる
- [x] 「サイドバーを開閉」（⌘B）が実行できる
- [x] 「分割表示を切り替え」（⌘\）が実行できる
- [x] 「右パネルを切り替え」（⌘⇧B）が実行できる
- [x] 「フォーカスモードを切り替え」が実行できる
- [x] 「タイプライターモードを切り替え」（⌘⇧T）が実行できる
- [x] 「Git ビューを開く」が実行できる
- [x] 「Git: 手動プッシュ」が実行できる
- [x] 「Git: 手動プル」が実行できる
- [x] 「ブランチを切り替え: {名前}」が動的に表示・実行できる
- [x] 「ファイル名を変更: {名前}」が表示される（アクティブファイルあり時）
- [x] 「ファイルを複製: {名前}」が実行できる（アクティブファイルあり時）
- [x] 「ファイルを削除: {名前}」が実行できる（アクティブファイルあり時）
- [x] 「設定を開く」が実行できる

---

## 16. クイックスイッチャー

- [x] ⌘P でクイックスイッチャーが開く
- [x] ファイル名入力で候補が絞り込まれる
- [x] 候補を Enter で開ける
- [x] 同名ファイルがある場合にフォルダパスが表示される
- [x] 既に開いているファイルを選択するとそのタブがアクティブになる
- [x] Escape で閉じる

---

## 17. キーボードショートカット

- [x] ⌘B — サイドバー開閉
- [x] ⌘F — 検索サイドバーを開く
- [x] ⌘⇧F — ワークスペース全体検索（検索サイドバーを開く）
- [x] ⌘N — 新規ノート作成（ファイルサイドバーを開いて入力フォームを表示）
- [x] ⌘P — クイックスイッチャーを開く
- [x] ⌘⇧P — コマンドパレットを開く
- [x] ⌘W — アクティブタブを閉じる
- [x] ⌘\ — 分割表示の切り替え
- [x] ⌘⇧B — 右パネルの開閉
- [x] ⌘⇧T — タイプライターモードの切り替え
- [x] Escape — モーダル（コマンドパレット・クイックスイッチャー）を閉じる

---

## 18. 設定

- [x] テーマを「システム追従」「ライト」「ダーク」から選べる
- [x] ダークテーマで全UIが正しく表示される
- [x] フォントを「システム」「明朝」「等幅」から選べる
- [x] 文字サイズを変更するとエディタに反映される
- [x] 行間を変更するとエディタに反映される
- [x] 最大幅を変更するとエディタに反映される
- [x] 行番号の表示/非表示を切り替えられる
- [x] スペルチェックの有効/無効を切り替えられる
- [x] 設定を保存してアプリ再起動後も反映される

---

## 19. 機能トグル

- [x] Git 機能をオフにするとレールのGitボタンが非表示になる
- [x] ツール機能をオフにするとレールのツールボタンが非表示になる
- [x] フロントマターをオフにするとフロントマターフォームが非表示になる
- [x] 右パネルをオフにすると右パネルボタンが非表示になる
- [x] フォーカスモードをオフにするとタイプライターモードが無効になる
- [x] トグル設定がアプリ再起動後も保持される

---

## 20. 自動同期

- [x] 自動同期を有効にして設定を保存できる
- [x] 設定した間隔（分）で自動的に Push / Pull が実行される

---

## 21. その他 UI

- [x] ステータスバーに現在のファイルの文字数・単語数が表示される
- [x] ステータスバーにGitブランチ名が表示される（Git有効時）
- [x] エラー発生時にトースト通知が画面下部に表示される
- [x] トーストが4秒後に自動消去される
- [x] トーストをクリックすると即座に消える
- [x] ファイルが開かれていないペインに「新規ノートを作成」フォームが表示される
- [x] アプリ情報（バージョン等）が設定画面で確認できる
