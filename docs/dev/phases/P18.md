# P18

RelicのUX/UIデザイン修正フェーズの正本。

このフェーズでは、AIが先に全体方針・チェックリスト・修正案一覧を作らない。
ユーザーの「ここが気になる」「こう見える」「この画面を直したい」という具体的な違和感を出発点にし、対象を確認してから必要最小限の修正を行う。

---

## 読み込みルール

RelicのUX/UIデザインを、ユーザーが指定した画面・状態・違和感を起点に修正するフェーズ。

AIはこのフェーズを前提にユーザーへ接する。
AIが先に全体チェックリスト・修正案一覧・デザイン方針を作らず、ユーザーが気になっている画面や状態を聞いてから作業範囲を決める。

最初に読む:

- この文書

必要になったときだけ読む:

- UI構成を確認する: `../../ui/screens-macos.md`, `../../ui/navigation.md`
- デザイン原則を確認する: `../../ui/DESIGN.md`
- 挙動の正解を確認する: 対応する `../../spec/*.md`
- 実装規約とテスト方針を確認する: `../conventions.md`, `../testing.md`
- 過去の経緯を確認する: `../../journal/` の該当日

注意:

- ユーザーが指定した画面・場所・操作対象だけを変更する
- 見た目の修正に機能追加・導線追加・仕様変更を混ぜない
- 実装したUX/UI判断は、実施したことだけをチェックリストとして記録する
- `../../ui/DESIGN.md` は、ユーザーがこのファイル自体の原則変更を明示した場合だけ編集する

---

## フェーズの目的

Relicを、書く道具として見やすく、迷わず、日常的に使いやすいUIへ整える。

特に、以下を重視する。

- ユーザー本人が気になっている画面・状態を最優先する
- 見た目の修正に機能追加・導線追加・仕様変更を混ぜない
- 変更範囲を、ユーザーが指定した画面・場所・操作対象に限定する
- デザイン原則は `../../ui/DESIGN.md` を正とし、変更が必要な場合もユーザーの明示判断を待つ

---

## UX/UI判断と実施

このフェーズでは、実施したことだけをチェックリストとして記録する。
方向性・確認・残りなどを表で分ける構造化はしない。

- [x] ファイルモードのサイドバーで、フォルダ行にフォルダアイコンを追加した
- [x] フォルダ右クリックメニューに「このフォルダ以下を開く」「このフォルダ以下を閉じる」を追加した
- [x] フォルダ右クリックメニューに「すべてのフォルダを開く」「すべてのフォルダを閉じる」を追加した
- [x] P18のチェックリスト記録ルールを、実施したことだけを書く形に変更した
- [x] チャートモードのサイドバーで、表示できるファイル一覧がサイドバーの縦幅を使うようにした
- [x] dateチャートのスケールを日・月・年の3種類にした
- [x] dateチャートの月スケールのグリッド幅を広げた
- [x] dateチャートのバー日付表示を、日スケールは日、月スケールは月日、年スケールは年月日にした
- [x] dateチャートの日スケールで、日付軸に年・月・日が分かる表示を追加した
- [x] dateチャートの軸・グリッド・バー位置が同じ日付境界で揃うようにした
- [x] dateチャートの単日バーは、chronicleの視認性用最低幅とは分けて、実日付の1日分だけ塗るようにした
- [x] dateチャートのバー日付表示をスケールに合わせて短くした
- [x] チャートモードのサイドバーのフォルダ・ファイル表示を、ファイルモードと同じアイコン表示に揃えた
- [x] レールにグラフビューを追加した
- [x] グラフビューをメインエリアのタブとして開けるようにした
- [x] グラフビューの検索・フィルタ・表示操作をサイドバー側に配置した
- [x] グラフビューにフォルダ・タグ・リンク状態・検索・最小リンク数フィルタを追加した
- [x] グラフビューでMarkdownファイルをノード、Wikiリンクを線として表示するようにした
- [x] グラフビューで通常のMarkdownリンクも線として表示するようにした
- [x] グラフビューにズーム・ラベル表示・再読み込み・リセットを追加した
- [x] グラフビューでノード選択時に参照先・参照元を確認して開けるようにした
- [x] グラフビューの描画密度・線・ノード・ラベル・詳細表示をObsidianのグラフビューに近い控えめな見た目へ調整した
- [x] グラフビューでノードホバー時に接続関係をハイライトし、マウス/タッチの背景ドラッグでパン、ホイール・トラックパッドでズームできるようにした
- [x] グラフビュー下部のノード詳細欄を撤去し、設定をグラフタブ上のホバー表示に移し、ノードを掴んで移動できるようにした
- [x] グラフビューの設定ホバーをObsidian風の小さい折りたたみパネルにし、最小化できるようにした
- [x] グラフビューの選択時に配置が寄る挙動をやめ、ノード・線・ホバー強調をObsidianに近い薄い表示へ調整した
- [x] グラフビューでノードを掴んだ時、掴んだノードだけの固定移動ではなく、周囲のノードが力学的に追従し、離すと配置が自然に落ち着くようにした
- [x] グラフビューの力学シミュレーション描画をブラウザの描画タイミングごとに更新し、ノードドラッグ中の追従を滑らかにした
- [x] グラフビューのSVGフォーカス枠とキャンバス枠を消し、ノード数表示を重ね表示にして描画面を広く見せた
- [x] チャートモードをサイドバーではなくレールから開くメインタブにし、chronicle/date切替・検索/フィルタ・スケール操作をチャート上のホバー表示に移した
- [x] チャートは保存済みの個別選択ではなく、chronicle/dateプロパティを持つファイルを全件表示するようにした
- [x] ファイルモードのサイドバーでは、ファイル・フォルダを掴んでドロップ移動する操作を無効にした
- [x] レールの区切りを、上段はビュー系、下段は設定・管理系として整理し、グラフビューを上段に移した
- [x] グラフビューとチャートモードのホバーメニューに掴む部分を追加し、ドラッグで位置を動かせるようにした
- [x] チャートモードのチャート面に、日付・年代境界と行境界に揃う補助線を追加した
- [x] 検索モードをレールから外し、ファイルモード内の新規ファイル作成ボタン上に検索方法候補つきフォームとして統合した
- [x] レールのサイドバー開閉専用ボタンをなくし、ファイルボタン内の矢印表示でファイルサイドバーを開閉するようにした
- [x] アプリ再起動後も、アクティブなワークスペースのファイルツリーとピン留めをファイルモードに復元するようにした
- [x] レールの画面タブ入口は、再クリックで閉じず、開いているタブへ移動する挙動に統一した
- [x] レールからのタブ開閉アニメーションは、新規タブ作成時だけ開く演出を出し、閉じる演出はタブを実際に閉じる操作側へ限定した
- [x] タブを閉じる演出を、元の入口へ吸い込む動きではなく、タブ位置でしっとり水面に沈むように潰れて消える動きに変更した
- [x] エディタ内フロントマターのプロパティフォームで、入力欄や削除ボタンの操作がエディタ本文側に奪われないようにした
- [x] エディタ内フロントマターの既存YAMLブロック末尾からプロパティを追加できるようにし、配列値のプラスボタンを実際の追加操作にした
- [x] 右上の分割ボタン横にソースモード切替を追加し、ONの間はエディタのライブ装飾・フォーム化を外してMarkdown構文をそのまま表示するようにした
- [x] `aliases` はそのファイル自身の別名なので、エディタ内フロントマターの `aliases` 入力では他ファイル由来の候補を表示しないようにした
- [x] エディタ内フロントマターのプロパティ追加行で、IME確定やEnter入力がプロパティ追加や本文挿入として誤動作しないようにした
- [x] エディタ内フロントマターのフォーム全体を本文編集対象から外し、入力欄での全角入力がMarkdown本文へ流れ込まないようにした
- [x] エディタ内フロントマターのフォーム操作中はCodeMirror本文自体を一時的に編集不可にし、IME合成文字がフロントマター開始位置へ挿入される経路を止めた
- [x] エディタ内フロントマターの追加用インライン入力欄を撤去し、プロパティ追加と配列値追加はエディタ本文外の追加ダイアログで入力する形にした
- [x] エディタ内フロントマターのプロパティフォームを、内容を削除せずにヘッダー操作で閉じたり開いたりできるようにした
- [x] ファイルモードの検索フォームで検索方法を選んだ後、検索欄へフォーカスを戻しても検索方法候補が再表示されないようにした
- [x] ファイルモードの検索欄を押しただけでは検索方法候補が表示されないようにし、候補表示は検索方法ボタン操作に限定した

## Obsidianグラフビュー実装チェックリスト

- [x] ノードホバー時に接続関係をハイライトする
- [x] マウス/タッチの背景ドラッグでグラフを掴んで動かす
- [x] マウス/タッチでノードを掴んで移動する
- [x] ホイール・トラックパッドでグラフをズームする
- [x] キーボードでグラフをパン・ズームする
- [x] 被リンク数が多いノードほど大きく表示する
- [x] 孤立ノードの表示/非表示を切り替える
- [x] リンク方向の矢印表示を切り替える
- [x] ノードクリックでノートを開く挙動と選択詳細の扱いを整理する
- [x] ラベルフェード・ノードサイズ・リンク太さを表示設定として調整できるようにする
- [x] 中心力・反発力・リンク力・リンク距離を力学設定として調整できるようにする
- [x] グラフ設定をサイドバーではなくグラフタブ上のホバー表示にする
- [x] 検索条件に一致するノートをグループ色分けできるようにする
- [x] アクティブノート中心のローカルグラフと深さ設定を追加する
- [x] タイムラプスアニメーションはファイル履歴・時刻データに依存するため、P18のグラフUI改善では実装せず、将来の履歴/分析機能側で扱う

---

## 作業ルール

- このフェーズは大きな計画を先に固めず、ユーザーが気になった箇所を都度、少しずつ直す。
- 小さなUI変更ごとに、対象・意図・確認方法を短く扱う。
- ユーザーが「やりたい」「こうしたい」と言っただけで、仕様・チェックリスト・デザイン方針を作らない。
- まずユーザーへ、何が嫌か、何を変えたいか、どこまで変えてよいかを確認する。
- UI/UX方針は提案として出せる。ただし正本化・実装はユーザーが明示的に許可した範囲だけ行う。
- デザイン原則の変更は `../../ui/DESIGN.md` に関わるため、ユーザーが明示した場合だけ編集する。
- 実装済みの変更は戻さない。戻す場合はユーザーが明示的に指示したときだけ行う。

---

## 最初に確認すること

このフェーズでは、大きな変更に入る前だけ次を1問ずつ確認する。
小さな違和感修正は、ユーザーが具体的に対象と方向を示した場合、その範囲で都度直す。

- いま一番気になる画面・状態はどこか
- 何が見づらい、分かりにくい、気持ち悪いと感じるか
- 変えてよい範囲はどこまでか
- 実アプリ目視確認を行うか、コード上の確認に留めるか

---

## 完了条件

- ユーザーが指定したUX/UI修正対象に対して、変更範囲が合意されている
- 実装した場合は型チェック・自動テストが通っている
- 実アプリ目視確認が必要な項目は、確認済みまたは未確認理由が明記されている
- 実装したUX/UI判断が、この文書に実施したことだけのチェックリストとして記録されている
