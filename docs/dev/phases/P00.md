# P00

Relicを `v0.0.1` としてリブートするための準備フェーズ。

---

## 目的

- 過去の文書管理を整理し、現行判断に使う文書を減らす
- AI作業ルールを `AGENTS.md` に集約する
- フェーズ管理を、一覧とフェーズ本文だけの構成にする
- 日誌を廃止し、作業記録をフェーズ本文へ集約する

---

## ルール・制約

- Git履歴は消さない
- AI作業ルールは `AGENTS.md` だけを正本にする
- 日誌は作らない
- 仕様書とモックは、現時点では現行正本として持たない
- フェーズ本文には、日時つきで作業したことを記録する
- アプリ本体コードは、ユーザーが明示するまで変更しない

---

## 作業記録

### 2026-05-24 12:12 JST

- `P00` ブランチを作成した後、標準命名に合わせて `codex/P00` へリネームした。
- ユーザー指示により、AI作業ルールを `AGENTS.md` に集約する方針にした。
- ユーザー指示により、フェーズ本文を `docs/dev/phases/P00.md` とし、余計な接尾辞を付けない方針にした。
- ユーザー指示により、日誌、仕様書、モック、分散していた開発ルール文書を廃止対象にした。

### 2026-05-24 12:14 JST

- `AI.md` と `CLAUDE.md` を削除し、AI作業ルールを `AGENTS.md` に集約した。
- `docs/dev/phases.md` をP00だけのフェーズ一覧に作り直し、`docs/dev/phases/P00.md` を作成した。
- 旧フェーズ文書、日誌、仕様書、モック、分散していた開発ルール文書を削除した。
- `docs/INDEX.md`、`README.md`、`docs/product/project.md`、`docs/product/glossary.md`、`docs/architecture/data-model.md`、`docs/ui/screens-macos.md` から削除済み文書への参照を整理した。
