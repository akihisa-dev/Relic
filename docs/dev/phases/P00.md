# P00.md

Relicのリブート準備フェーズです。

---

## フェーズの目的

- 文書構造を整理し、ここから `v0.0.1` として扱える状態にする。
- アプリ本体の名称を `ワークスペース`、`ファイル`、`フォルダ` に戻した状態を維持する。
- AIルールと開発運用ルールを `AGENTS.md` に集約する。

---

## ルール・制約

- アプリ本体の名称変更は行わない。
- 旧仕様書、旧モック、旧日誌、旧フェーズ文書は現行判断に使わない。
- 日誌は作らず、この文書に日時付きで作業記録を書く。
- フェーズ固有のルールを `AGENTS.md` に混ぜない。
- プロジェクト全体のルールをこの文書に混ぜない。
- 更新ごとにコミットとプッシュを行う。

---

## 作業記録

### 2026-05-24 12:36 JST

- `codex/P00` ブランチでP00の文書リブートを開始した。
- AIルールを `AGENTS.md` に集約する方針にした。
- フェーズ管理を `docs/dev/phases.md` と `docs/dev/phases/P00.md` に整理する方針にした。
- 日誌、旧仕様書、旧モック、旧フェーズ文書、分散した開発ルール文書を廃止する方針にした。

### 2026-05-24 12:40 JST

- 現在状態を `v0.0.1` として扱うことにした。
- アプリ本体の `app/package.json` の `version` を `0.0.1` にした。
- バージョン管理ルールを `AGENTS.md` に追加することにした。
