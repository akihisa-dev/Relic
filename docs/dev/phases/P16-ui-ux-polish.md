# P16-ui-ux-polish.md

Relicの本当のUI/UX洗練フェーズの正本。

このフェーズでは、AIが先に方針・チェックリスト・実装内容を作らない。
ユーザーの「こうしたい」「こう見える」「ここが嫌だ」という感覚を出発点にし、必ずヒアリングしてから方針を決める。

---

## フェーズの目的

Relicを、書く道具として気持ちよく、分かりやすく、使い続けたくなるUIへ整える。

特に、以下を重視する。

- ユーザー本人の違和感を最優先する
- 見た目の派手さではなく、日常的に書くための落ち着きと清潔感を重視する
- AIが勝手に「つまらなさ」の原因や解決策を決めない
- デザイン原則は `../ui/DESIGN.md` を正とし、変更が必要な場合もユーザーの明示判断を待つ

---

## UI/UX判断と実施

このフェーズでは、方向性・実施・確認・残りを同じ項目の中に必ずセットで記録する。
やったこと、確認したこと、問題点、残作業を混同しないため、以降はテーマごとにチェックリストで管理する。
未実施・未確認の場合も省略せず、明示する。

### 色と印象

チェックリスト:

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [ ] 未実装 | 色と印象 | 現状はオレンジを使いすぎている。オレンジは通常操作ではなく、最終決定・不可逆操作・危険・強い注意など、ユーザーに一呼吸置かせたい場面へ絞る | 未実施 | 未確認 | 画面単位でオレンジの使用箇所を洗い出し、残す箇所・下げる箇所を決める |
| [ ] 未実装 | ブルーの役割 | ブルーは情報、案内、リンク、現在の対象、補助的な状態表示に使う | 未実施 | 未確認 | どの画面でブルーを情報・案内・現在地として使うか、ユーザーの違和感を起点に決める |
| [ ] 未実装 | ライトテーマの印象 | モノクロは主軸だが、黒い印象ではなく、白をメインにしたクリーンな印象にする | 未実施 | 未確認 | 白い余白、薄いグレーの区切り、必要最小限のアクセントで画面を整える |
| [ ] 未実装 | 装飾と強調 | シンプルなデザインにしたい。色・装飾・説明・強調を増やす方向ではなく、必要な差だけで整える | 未実施 | 未確認 | 具体的にどの画面から色・余白・アクセントを調整するかを決める |

### フロントマターUI

チェックリスト:

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [x] 実装済み / 目視済み | 右パネルの役割 | 右パネルは本文外の補助情報に寄せ、フロントマター編集の主導線から外す | 右パネルのフロントマター専用編集UIを撤去し、アウトラインとリンクの補助情報に戻した | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリ（`pnpm start`）で、フロントマター編集UIが右パネルへ戻っていないことを目視確認 | なし |
| [x] 実装済み / 目視済み | 本文先頭のプロパティフォーム | フロントマターは右パネルの専用編集欄ではなく、MD本文先頭のYAMLをObsidian式のプロパティフォームとして見せる | MDファイル先頭のYAMLフロントマターを、エディタ本文上ではプロパティフォームとして表示するようにした。折りたたみ、プロパティ追加・削除、フロントマターなしファイルでのYAMLブロック新規作成に対応した | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリで、既存フロントマターのプロパティフォーム・配列pill・追加行が本文先頭に表示されることを目視確認。フロントマターなしファイルのプロパティ追加からYAMLブロックが新規作成されることを実操作確認 | なし |
| [x] 実装済み / 自動確認済み | 入力補助 | フロントマターの実体はMDファイル内の構造を保ち、UIは入力補助として日付・真偽値・候補入力などを支援する | 登録済みフィールドの入力能力をエディタ内フォームに反映した。`date` は日付入力、`boolean` はチェックボックス、`select` / `multi-select` / `tags` は候補付き入力として扱うようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。この項目単独の実アプリ目視確認記録はなし | 必要なら実アプリで入力種別ごとの手触りを確認する |
| [x] 実装済み / 自動確認済み | 候補とテンプレート | 入力候補やテンプレートは入力補助に留め、本文そのものの構造はアプリ独自の隠しメタデータにしない | ワークスペース内Markdownから既存フロントマター値候補を収集し、検索とプロパティ入力の候補に使えるようにした。フロントマターテンプレートは作成後に編集できるようにし、フィールド名変更時はテンプレート側も追従し、フィールド削除時はテンプレートからも除外するようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。この項目単独の実アプリ目視確認記録はなし | 必要ならテンプレート編集と候補表示を実アプリで再確認する |
| [x] 実装済み / 目視済み | 配列・タグ編集 | array / tags 系の値は、プロパティフォーム上で個別に編集・削除できるようにする | array / tags 系の値を、プロパティフォーム上で個別に編集・削除できるようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリで、既存フロントマターの配列pillが本文先頭に表示されることを目視確認 | なし |
| [x] 実装済み / 自動確認済み | YAML書式保持 | 本文そのものの構造は保護し、コメント・空行・フィールド順・クォート・1行配列を不用意に壊さない | プロパティ編集時に、トップレベルのYAMLコメント行・空行・フィールド順をできるだけ保持するようにした。単純なスカラー値の同一行にあるインラインコメント、単純な文字列スカラーのクォート、1行配列として書かれたYAML値を保持するようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。この項目単独の実アプリ目視確認記録はなし | 必要なら実ファイルで保存後の差分を確認する |
| [x] 実装済み / 目視済み | 複雑YAML値 | ネストしたオブジェクトなどの複雑なYAML値はフォーム編集で壊さず、必要な場合だけYAML入力で直接編集できるようにする | 複雑なYAML値は読み取り表示にし、フォーム編集で壊さないようにした。複雑なYAML値はフォーム上のYAML入力で直接編集できるようにした。不正なYAML入力は本文へ反映せず、入力欄をエラー状態にするようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリで、複雑YAML値をフォーム上のYAML入力から編集でき、ファイルへ保存されることを実操作確認。不正なYAML入力がファイルへ反映されないことを実操作確認 | なし |

### 動作とリアクション

共通方向性:

- すべての動作は、結果だけがパッと現れるのではなく、操作に見合った短い過程を持つ
- モーションは装飾ではなく、押す・引く・閉じる・開く・選ぶ・移動する・なぞるなどの操作に対する手触りとして扱う
- ユーザーがしたアクションに対して、アプリはその動作から想像されるリアクションを返す
- ボタンは、ユーザーが押したなら、視覚上も押されなければならない
- 反応は大げさにせず、書く道具の落ち着きを壊さない範囲で、ユーザーが「いま何が起きたか」を身体感覚で追えるようにする
- 閉じる動作は、突然消すのではなく、少し押し込まれてから縮む・抜ける・吸い込まれるような過程を持たせる
- 選択・切り替えは、重心や現在地が移ったことが分かる反応にする
- 開閉・展開は、面や情報が押し出される・折りたたまれる感覚にする
- ドラッグ・移動は、掴んでいる対象と移動先の受け入れ状態が分かる反応にする

チェックリスト:

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [x] 実装済み / 未目視 | タブを閉じる | 閉じる動作は、突然消すのではなく、少し押し込まれてから縮む・抜ける・吸い込まれるような過程を持たせる | バツボタン、コンテキストメニュー、他を閉じる、右側を閉じる、すべて閉じる、`⌘W` で同じ閉じる反応を通るようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | タブを開く | 新しい対象が列へ入ってくる過程を持たせる | タブが列へ短く入ってくる動きを付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | タブを選ぶ | 選択・切り替えは、重心や現在地が移ったことが分かる反応にする | 現在タブが軽く受け止める動きを付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | ボタン全般 | ボタンは、ユーザーが押したなら、視覚上も押されなければならない | ボタン全体に、押したとき少し沈む基本リアクションを付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで強すぎるボタン・弱すぎるボタンを確認 |
| [x] 実装済み / 未目視 | サイドバー開閉 | 開閉は、面や情報が押し出される・折りたたまれる感覚にする | 内側の内容が左へ畳まれる / 左から戻る反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | 右パネル開閉 | 開閉は、面や情報が押し出される・折りたたまれる感覚にする | 内側の内容が右へ畳まれる / 右から戻る反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | サイドバーのビュー切り替え | ビュー切り替えは、中身が入れ替わったことが分かる反応にする | ファイル、検索、Git、ツール、設定の内容が左側から短く入れ替わる反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | サイドバー幅のドラッグ変更 | ドラッグ・移動は、掴んでいる対象と移動先の受け入れ状態が分かる反応にする | ドラッグ中にサイドバー境界とリサイズハンドルが反応し、掴んでいる状態が分かるようにした。ドラッグ中は幅変更の通常アニメーションを止め、追従感を優先した | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。強さ・視認性を調整 |
| [x] 実装済み / 未目視 | ファイルツリーのフォルダ開閉 | 展開は、子項目が開く・収納される過程を持たせる | 矢印が回転し、子項目が短く展開される反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | ファイルツリーの選択 | 選択は、対象行が選ばれたことを軽く受け止める反応にする | 対象行が軽く受け止める反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | ファイルツリーのドラッグ移動先 | ドラッグ先は、受け入れ可能な場所であることが分かる反応にする | 受け入れ先が少し膨らむ反応を付けた。ドラッグ中の対象行にも軽い移動・透過・アクセント色の反応を付け、掴んでいる対象が分かるようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。複数選択ドラッグ時の見え方は必要なら追加調整 |
| [x] 実装済み / 未目視 | ファイル作成・複製・削除 | 新規出現、複製、退場が分かる反応にする | ファイルツリーで後から追加された行に短い出現反応を付け、新規作成・複製で対象行が入ってきたことを分かるようにした。削除メニュー操作時は対象行に短い退場反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。削除確定後の完全な退場タイミングは必要なら追加調整 |
| [x] 実装済み / 未目視 | ファイル名変更・移動先入力・ファイルツリーのリネーム入力 | ラベルから入力欄へ変形したことが分かる反応にする | ラベルから入力欄へ変わる反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | コマンドパレット・クイックスイッチャー | 呼び出し時に面として浮き上がり、選択行が受け止める反応にする | 面として浮き上がる反応、選択行が軽く受け止める反応を付けた。Escape、外側クリック、項目選択で閉じるときに、面と背景が短く退場する反応を通すようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 257テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | トースト | 通知が表示されたことが分かるよう、下から短く出てくる反応にする | トースト表示時に下から短く出てくる反応を付けた。クリックまたは表示時間経過で閉じるときも、下へ抜ける短い退場反応を通すようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 255テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | 分割表示の開始 | ペインが割れて新しい面が差し込まれる感覚にする | 右ペインが右から差し込まれる反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。分割解除時の戻る反応は未実装 |
| [x] 実装済み / 未目視 | 右パネルのアウトライン / リンク切り替え | 内容が入れ替わることが分かる反応にする | 内容が右側から短く入れ替わる反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | 右パネル内のアウトライン項目・リンク項目 | 選ぶ対象として軽く動く反応にする | 項目に軽く動く反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | ツールバーのドロップダウン・インライン入力ダイアログ | 押したボタンから小さな面が開く感覚にする | ドロップダウンと入力ダイアログが、押したボタンから開く小さな面として出る反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。閉じる退場反応は未実装 |
| [x] 実装済み / 未目視 | ツールバーのドロップダウン項目 | 選ぶ対象として軽く動く反応にする | ドロップダウン項目に軽く動く反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | ツールバー操作の本文反映 | 操作が本文へ届いたことが分かる反応にする | ツールバー操作後、対象エディタ面が短く受ける反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | エディタ内プロパティフォーム | フォーム全体・各行・入力・削除・エラーが操作に応じた反応を持つようにする | フォーム全体と各行の入場、折りたたみ矢印回転、入力フォーカス、削除ボタン沈み、不正YAML入力の揺れとエラー色を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。速さ・強さを調整 |
| [x] 実装済み / 未目視 | 検索結果・Git関連リスト・リンク一覧 | 結果が更新されたこと、項目を選べることが分かる反応にする | 結果として短く入ってくる反応と、項目が軽く受け止める反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで手触り確認。読み込み中であることの反応は未実装 |
| [x] 実装済み / 未目視 | モーション抑制設定 | OS側でモーションを抑える設定の場合は、遷移時間を最小化する | タブ、ボタン、ファイルツリー、モーダル、トースト、パネル切り替え、ツールバー、入力変形、プロパティフォーム、結果リスト系で `prefers-reduced-motion` に対応した | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。実アプリ目視確認は未実施 | 実アプリで確認。対象漏れがあれば追加 |

---

## 最初に読む

- `../ui/DESIGN.md`
- `../ui/screens-macos.md`
- `../ui/navigation.md`

必要になったときだけ読む:

- 挙動の正解を確認する: `../spec/`
- 実装規約とテスト方針を確認する: `conventions.md`, `testing.md`
- 過去の経緯を確認する: `../journal/` の該当日

---

## 作業ルール

- このフェーズは大きな計画を先に固めるのではなく、ユーザーが気になった箇所を都度、少しずつ直していく。
- ユーザー判断で実装したUI/UX変更は、この文書の「UI/UX判断と実施」に方向性・実施・確認・残りをセットで記録する。日誌だけに閉じ込めない。
- 小さなUI変更ごとに、対象・意図・確認方法を短く扱う。
- ユーザーが「やりたい」「こうしたい」と言っただけで、仕様・フェーズ・チェックリスト・デザイン方針を作らない。
- AIが勝手に作った方針やチェックリストをユーザーが確認すること自体が無駄になるため、確認させる前に作らない。
- まずユーザーへヒアリングし、何が嫌か、何を変えたいか、どの程度変えてよいかを確認する。
- ヒアリングの結果、ユーザーが「作ってよい」と明示した場合だけ、方針・チェックリスト・文書・実装案を作る。
- UI/UX方針は、提案として出すことはできる。ただし正本化・実装はユーザーが明示的に許可した範囲だけ行う。
- デザイン原則の変更は `../ui/DESIGN.md` に関わるため、ユーザーが明示した場合だけ編集する。
- 実装済みの変更は戻さない。戻す場合はユーザーが明示的に指示したときだけ行う。

## 最初に確認すること

このフェーズでは、大きな変更に入る前だけ次を1問ずつ確認する。
小さな違和感修正は、ユーザーが具体的に対象と方向を示した場合、その範囲で都度直す。

- いま一番嫌な画面・状態はどこか
- どんな印象にしたいか
- 変えてよい範囲はどこまでか
- 先にデザイン原則を直すか、画面単位で直すか
- 実アプリ目視確認を行うか、コード上の確認に留めるか

---

## 完了条件

- ユーザーがUI/UX方針に納得している
- 方針が `../ui/DESIGN.md` または現在フェーズ正本と矛盾していない
- 実装した場合は型チェック・自動テストが通っている
- 実アプリ目視確認が必要な項目は、確認済みまたは未確認理由が明記されている
