# P16-ui-ux-polish.md

Relicの本当のUI/UX洗練フェーズの正本。

このフェーズでは、AIが先に方針・チェックリスト・実装内容を作らない。
ユーザーの「こうしたい」「こう見える」「ここが嫌だ」という感覚を出発点にし、必ずヒアリングしてから方針を決める。

---

## フェーズの目的

Relicを、書く道具として気持ちよく、分かりやすく、使い続けたくなるUIへ整える。

特に、以下を重視する。

- ユーザー本人の違和感を最優先する
- 見た目の派手さではなく、日常的に書くための落ち着きと清潔感を重視する
- AIが勝手に「つまらなさ」の原因や解決策を決めない
- デザイン原則は `../ui/DESIGN.md` を正とし、変更が必要な場合もユーザーの明示判断を待つ

---

## UI/UX判断と実施

このフェーズでは、方向性・実施・確認・残りを同じ項目の中に必ずセットで記録する。
やったこと、確認したこと、問題点、残作業を混同しないため、以降はテーマごとにチェックリストで管理する。
未実施・未確認の場合も省略せず、明示する。

### 色と印象

チェックリスト:

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [x] 実装済み / 実アプリ確認済み | 色と印象 | 現状はオレンジを使いすぎている。オレンジは通常操作ではなく、最終決定・不可逆操作・危険・強い注意など、ユーザーに一呼吸置かせたい場面へ絞る | CSS上の暖色使用箇所を洗い出し、Markdownハイライトの黄色をブルー系の情報表現へ変更した。エラー・危険表示のハードコード赤を `--error` / `--error-bg` へ寄せた。オレンジは `--attention` と置換確定操作に限定した | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 261テスト）。開発版Electronアプリ（`pnpm start`）で、ファイルツリー、本文、右リンクパネル、検索・Git・設定ビューを目視確認し、置換確定操作以外でオレンジが強く常用されていないことを確認した | なし |
| [x] 実装済み / 実アプリ確認済み | ブルーの役割 | ブルーは情報、案内、リンク、現在の対象、補助的な状態表示に使う | Markdownハイライトを黄色からブルー系の薄い情報表現へ変更し、ブルーを本文内の補助的な状態表示にも使えるよう整理した。アクセントの淡い背景・さらに淡い背景・リングを `--accent-soft` / `--accent-softer` / `--accent-ring` として整理し、選択・ドラッグ・フォーカス・読み込み表示の青を揃えた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 261テスト）。開発版Electronアプリ（`pnpm start`）で、本文リンク、選択中ファイル、リンク一覧、サイドバーの現在ビュー、Gitの通常操作ボタンを目視確認し、ブルーが案内・現在地・通常操作として読めることを確認した | なし |
| [x] 実装済み / 実アプリ確認済み | ライトテーマの印象 | モノクロは主軸だが、黒い印象ではなく、白をメインにしたクリーンな印象にする | タブバー、ステータスバー、ツールバー、検索結果、Git差分リスト周辺の直書きグレーを既存トークンへ寄せ、白い面と薄い区切りの関係を揃えた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 261テスト）。開発版Electronアプリ（`pnpm start`）で、メインエディタ、左レール、サイドバー、右リンクパネル、検索・Git・設定ビューを目視確認し、ライトテーマ全体が白主体で、境界も見失わない範囲に収まっていることを確認した | なし |
| [x] 実装済み / 実アプリ確認済み | 装飾と強調 | シンプルなデザインにしたい。色・装飾・説明・強調を増やす方向ではなく、必要な差だけで整える | ハイライト・エラー・危険表示をデザイントークンに寄せ、個別の暖色ハードコードを減らした。アクセントの濃淡と主要な無彩色境界をトークンへ寄せ、個別指定を減らした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 261テスト）。開発版Electronアプリ（`pnpm start`）で、ファイルツリー、本文、リンク一覧、検索・Git・設定ビューを目視確認し、必要以上に強いアクセントや大きな有彩色の塗りがないことを確認した | なし |

### フロントマターUI

チェックリスト:

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [x] 実装済み / 自動確認済み | 右パネルの役割 | 右パネルは本文外の補助情報に寄せ、フロントマター編集の主導線から外す | 右パネルのフロントマター専用編集UIを撤去し、アウトラインとリンクの補助情報に戻した | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリ（`pnpm start`）で、フロントマター編集UIが右パネルへ戻っていないことを目視確認 | なし |
| [x] 実装済み / 自動確認済み | 本文先頭のプロパティフォーム | フロントマターは右パネルの専用編集欄ではなく、MD本文先頭のYAMLをObsidian式のプロパティフォームとして見せる | MDファイル先頭のYAMLフロントマターを、エディタ本文上ではプロパティフォームとして表示するようにした。折りたたみ、プロパティ追加・削除、フロントマターなしファイルでのYAMLブロック新規作成に対応した | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリで、既存フロントマターのプロパティフォーム・配列pill・追加行が本文先頭に表示されることを目視確認。フロントマターなしファイルのプロパティ追加からYAMLブロックが新規作成されることを実操作確認 | なし |
| [x] 実装済み / 自動確認済み | 入力補助 | フロントマターの実体はMDファイル内の構造を保ち、UIは入力補助として日付・真偽値・候補入力などを支援する | 登録済みフィールドの入力能力をエディタ内フォームに反映した。`date` は日付入力、`boolean` はチェックボックス、`select` / `multi-select` / `tags` は候補付き入力として扱うようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。この項目単独の実アプリ目視確認記録はなし | 必要なら実アプリで入力種別ごとの手触りを確認する |
| [x] 実装済み / 自動確認済み | 候補とテンプレート | 入力候補やテンプレートは入力補助に留め、本文そのものの構造はアプリ独自の隠しメタデータにしない | ワークスペース内Markdownから既存フロントマター値候補を収集し、検索とプロパティ入力の候補に使えるようにした。フロントマターテンプレートは作成後に編集できるようにし、フィールド名変更時はテンプレート側も追従し、フィールド削除時はテンプレートからも除外するようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。この項目単独の実アプリ目視確認記録はなし | 必要ならテンプレート編集と候補表示を実アプリで再確認する |
| [x] 実装済み / 自動確認済み | 配列・タグ編集 | array / tags 系の値は、プロパティフォーム上で個別に編集・削除できるようにする | array / tags 系の値を、プロパティフォーム上で個別に編集・削除できるようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリで、既存フロントマターの配列pillが本文先頭に表示されることを目視確認 | なし |
| [x] 実装済み / 自動確認済み | YAML書式保持 | 本文そのものの構造は保護し、コメント・空行・フィールド順・クォート・1行配列を不用意に壊さない | プロパティ編集時に、トップレベルのYAMLコメント行・空行・フィールド順をできるだけ保持するようにした。単純なスカラー値の同一行にあるインラインコメント、単純な文字列スカラーのクォート、1行配列として書かれたYAML値を保持するようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。この項目単独の実アプリ目視確認記録はなし | 必要なら実ファイルで保存後の差分を確認する |
| [x] 実装済み / 自動確認済み | 複雑YAML値 | ネストしたオブジェクトなどの複雑なYAML値はフォーム編集で壊さず、必要な場合だけYAML入力で直接編集できるようにする | 複雑なYAML値は読み取り表示にし、フォーム編集で壊さないようにした。複雑なYAML値はフォーム上のYAML入力で直接編集できるようにした。不正なYAML入力は本文へ反映せず、入力欄をエラー状態にするようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト）。開発版Electronアプリで、複雑YAML値をフォーム上のYAML入力から編集でき、ファイルへ保存されることを実操作確認。不正なYAML入力がファイルへ反映されないことを実操作確認 | なし |

### 右クリックメニュー

Obsidianの右クリックメニューを参考に、Relicでも「対象の近くで自然に出る操作」を揃える。
調査では、Obsidianはファイルエクスプローラー、タブ、リンク、本文上の対象ごとに右クリックメニューを持ち、ファイル作成・リネーム・移動・削除・別ペイン/別ウィンドウで開く・パスコピーなどを扱っていた。
Relicでは、外部ウィンドウや未実装のIPCが必要な操作は後回しにし、既存機能と安全に接続できる単位から1つずつ実装する。

チェックリスト:

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [x] 実装済み / 実アプリ確認済み | ファイルツリー基本操作 | ファイル・フォルダの右クリックメニューは、Obsidianに近い密度で、開く・別ペインで開く・ピン留め・リネーム・複製・コピー・削除など、対象に自然な操作を集める | 既存機能だけで安全に足せる範囲として、開く、分割表示中の別ペインで開く、ピン留め/解除、パスをコピーを追加した。メニュー項目が増えたため、画面端で内側へ収める高さ見積もりも更新した | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 273テスト）。開発版Electronアプリ（`pnpm start`）で、ファイル右クリックメニューに開く、ピン留め、パスをコピー、名前を変更、複製、ゴミ箱に移動が表示され、対象行の近くに表示されることを目視確認 | なし |
| [x] 実装済み / 自動確認済み | ファイルツリー作成・移動 | フォルダ右クリックから、そのフォルダ配下への新規ファイル・新規フォルダ作成、移動操作へ進めるようにする | フォルダ配下への新規ファイル作成、新規フォルダ作成、ファイル/フォルダ/複数選択の移動、Markdownリンクコピー、Finder表示を追加した。フォルダ作成IPCは既存のルート作成を保ったまま `parentFolder` を受けられるようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- Editor.test.tsx App.test.tsx folders.test.ts` 通過（31ファイル / 279テスト） | 移動先入力は暫定的にプロンプト。将来、候補付きピッカーに置き換える |
| [x] 実装済み / 自動確認済み | タブ右クリック | タブ右クリックは、閉じる系だけでなく、複製、パスコピー、Markdownリンクコピー、別ペインで開くなど、開いているファイルに対する操作を集める | タブメニューに開く、複製、ピン留め/解除、パスコピー、Markdownリンクコピー、Finder表示、分割表示中の別ペインで開くを追加し、既存の閉じる系操作と同じメニューにまとめた | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 278テスト） | なし |
| [x] 実装済み / 自動確認済み | 本文内リンク・右パネルリンク | リンク上の右クリックは、開く、別ペインで開く、リンク文字列コピー、対象パスコピー、未作成ファイルの作成へ進めるようにする | 右パネルのアウトゴーイングリンク/バックリンクに右クリックメニューを追加した。開く、分割表示中の別ペインで開く、Markdownリンクコピー、対象パスコピー、Finder表示を実行できる。未作成リンクは既存のリンクオープン処理と同じく作成して開ける | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 278テスト） | 本文中のインラインリンク上専用メニューは、CodeMirror上のリンクヒットテストを安定させてから追加する |
| [x] 実装済み / 実アプリ確認済み | エディタ本文 | 本文上の右クリックは、OS標準の切り取り・コピー・貼り付けを邪魔せず、Markdownエディタとして有用な追加操作だけを載せる | CodeMirror本文に右クリックメニューを追加し、コピー、カット、ペースト、すべて選択を表示した。ライブプレビュー表の専用メニューとは衝突しないよう、表のDOM上では既存の表メニューを優先する。Electron実アプリではrenderer側の独自本文メニューを出さず、main側の編集領域用ネイティブメニューに任せるようにした。非Electron環境・自動テスト用のフォールバックでは、右クリック時にCodeMirrorが選択を空にしてコピー/カットが無効表示になる問題を避けるため、右クリック前の選択範囲を保持し、コピー/カット項目自体はグレーアウトさせないようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- Editor.test.tsx` 通過（31ファイル / 280テスト）。開発版Electronアプリで `RelicCopyCutTest-0510` を選択し、本文右クリックメニューに `取り消し` / `カット` / `コピー` / `ペースト` / `すべて選択` が出ること、`コピー` 実行後にクリップボードが `relic-before-copy-0510-1835` から `RelicCopyCutTest-0510` へ変わること、`カット` 実行後に本文から文字が消えてクリップボードへ同じ文字列が入ることを確認した。カット後は `⌘Z` で本文を戻し、さらに検証用入力も `⌘Z` で戻した | 追加のMarkdown専用操作は、利用頻度を見てから増やす |

### 動作とリアクション

共通方向性:

- すべての動作は、結果だけがパッと現れるのではなく、操作に見合った短い過程を持つ
- モーションは装飾ではなく、押す・引く・閉じる・開く・選ぶ・移動する・なぞるなどの操作に対する手触りとして扱う
- ユーザーがしたアクションに対して、アプリはその動作から想像されるリアクションを返す
- ボタンは、ユーザーが押したなら、視覚上も押されなければならない
- 反応は大げさにせず、書く道具の落ち着きを壊さない範囲で、ユーザーが「いま何が起きたか」を身体感覚で追えるようにする
- 閉じる動作は、突然消すのではなく、少し押し込まれてから縮む・抜ける・吸い込まれるような過程を持たせる
- 選択・切り替えは、重心や現在地が移ったことが分かる反応にする
- 開閉・展開は、面や情報が押し出される・折りたたまれる感覚にする
- ドラッグ・移動は、掴んでいる対象と移動先の受け入れ状態が分かる反応にする

チェックリスト:

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [x] 実装済み / 自動確認済み | タブを閉じる | 閉じる動作は、突然消すのではなく、少し押し込まれてから縮む・抜ける・吸い込まれるような過程を持たせる | バツボタン、コンテキストメニュー、他を閉じる、右側を閉じる、すべて閉じる、`⌘W` で同じ閉じる反応を通るようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | タブを開く | 新しい対象が列へ入ってくる過程を持たせる | タブが列へ短く入ってくる動きを付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | タブを選ぶ | 選択・切り替えは、重心や現在地が移ったことが分かる反応にする | 現在タブが軽く受け止める動きを付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | ボタン全般 | ボタンは、ユーザーが押したなら、視覚上も押されなければならない | ボタン全体に、押したとき少し沈む基本リアクションを付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | サイドバー開閉 | 開閉は、面や情報が押し出される・折りたたまれる感覚にする | 内側の内容が左へ畳まれる / 左から戻る反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | 右パネル開閉 | 開閉は、面や情報が押し出される・折りたたまれる感覚にする | 内側の内容が右へ畳まれる / 右から戻る反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | サイドバーのビュー切り替え | ビュー切り替えは、中身が入れ替わったことが分かる反応にする | ファイル、検索、Git、ツール、設定の内容が左側から短く入れ替わる反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 実アプリ確認済み | サイドバー幅のドラッグ変更 | ドラッグ・移動は、掴んでいる対象と移動先の受け入れ状態が分かる反応にする。スクロールバーと幅変更ハンドルは重ねず、幅変更はスクロールバーの右側にある専用の掴み代で行えるようにする | ドラッグ中にサイドバー境界とリサイズハンドルが反応し、掴んでいる状態が分かるようにした。ドラッグ中は幅変更の通常アニメーションを止め、追従感を優先した。サイドバー本文のスクロール領域を少し内側に収め、スクロールバーの右側へリサイズ専用の細い掴み代を作った | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 272テスト）。開発版Electronアプリ（`pnpm start`）で、設定サイドバーのスクロールバー右側に幅変更用の掴み代が表示されることを目視確認 | なし |
| [x] 実装済み / 自動確認済み | ファイルツリーのフォルダ開閉 | 展開は、子項目が開く・収納される過程を持たせる | 矢印が回転し、子項目が短く展開される反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | ファイルツリーの選択 | 選択は、対象行が選ばれたことを軽く受け止める反応にする | 対象行が軽く受け止める反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | ファイルツリーのドラッグ移動先 | ドラッグ先は、受け入れ可能な場所であることが分かる反応にする | 受け入れ先が少し膨らむ反応を付けた。ドラッグ中の対象行にも軽い移動・透過・アクセント色の反応を付け、掴んでいる対象が分かるようにした。展開済みフォルダ内の余白へのドロップがルート移動として扱われる問題を修正し、フォルダの中身エリアと中のファイル行も親フォルダへのドロップ先として扱うようにした。中身エリアへドラッグしたときは、背景・左の枝線・余白がゆっくり開いて「ここへ吸い込まれる」反応を出すようにした。移動後に出る行は跳ね返りを抑え、静かに定着する反応へ調整した。同じ親フォルダへのドロップ、親フォルダから子孫フォルダへのドロップは移動対象外にした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 265テスト） | なし |
| [x] 実装済み / 自動確認済み | ファイル作成・複製・削除 | 新規出現、複製、退場が分かる反応にする | ファイルツリーで後から追加された行に短い出現反応を付け、新規作成・複製で対象行が入ってきたことを分かるようにした。削除メニュー操作時は対象行に短い退場反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | ファイル名変更・移動先入力・ファイルツリーのリネーム入力 | ラベルから入力欄へ変形したことが分かる反応にする | ラベルから入力欄へ変わる反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | コマンドパレット・クイックスイッチャー | 呼び出し時に面として浮き上がり、選択行が受け止める反応にする | 面として浮き上がる反応、選択行が軽く受け止める反応を付けた。Escape、外側クリック、項目選択で閉じるときに、面と背景が短く退場する反応を通すようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 257テスト） | なし |
| [x] 実装済み / 自動確認済み | トースト | 通知が表示されたことが分かるよう、下から短く出てくる反応にする | トースト表示時に下から短く出てくる反応を付けた。クリックまたは表示時間経過で閉じるときも、下へ抜ける短い退場反応を通すようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 255テスト） | なし |
| [x] 実装済み / 自動確認済み | 分割表示の開始 | ペインが割れて新しい面が差し込まれる感覚にする。分割方向は左右のみとし、上下に水平分割する方式は使わない | 右ペインが右から差し込まれる反応を付けた。分割解除時は右ペインが右へ戻って抜ける短い退場反応を通してから1ペインへ戻すようにした。分割コンテナを1行グリッドに固定し、2ペイン目が次行へ流れて上下分割のように見える余地を潰した | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx Editor.test.tsx` 通過（31ファイル / 279テスト） | なし |
| [x] 実装済み / 自動確認済み | 右パネルのアウトライン / リンク切り替え | 内容が入れ替わることが分かる反応にする | 内容が右側から短く入れ替わる反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | 右パネル内のアウトライン項目・リンク項目 | 選ぶ対象として軽く動く反応にする | 項目に軽く動く反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | ツールバーのドロップダウン・インライン入力ダイアログ | 押したボタンから小さな面が開く感覚にする | ドロップダウンと入力ダイアログが、押したボタンから開く小さな面として出る反応を付けた。見出しドロップダウン、リンク入力、表入力を閉じるときにも短い退場反応を通すようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- Toolbar.test.tsx App.test.tsx` 通過（31ファイル / 259テスト） | なし |
| [x] 実装済み / 自動確認済み | ツールバーのドロップダウン項目 | 選ぶ対象として軽く動く反応にする | ドロップダウン項目に軽く動く反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | ツールバー操作の本文反映 | 操作が本文へ届いたことが分かる反応にする | ツールバー操作後、対象エディタ面が短く受ける反応を付けた | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 254テスト） | なし |
| [x] 実装済み / 自動確認済み | エディタ内プロパティフォーム | フォーム全体・各行・入力・削除・エラーが操作に応じた反応を持つようにする。ただし、本文面に常駐する追加入口は本文のノイズになるため置かない | フォーム全体と各行の入場、折りたたみ矢印回転、入力フォーカス、削除ボタン沈み、不正YAML入力の揺れとエラー色を付けた。既存フロントマター末尾の追加入力と、フロントマターなし本文先頭の新規作成入口を撤去した | `pnpm exec tsc --noEmit` 通過。`pnpm test -- Editor.test.tsx` 通過（31ファイル / 267テスト） | なし |
| [x] 実装済み / 自動確認済み | 本文内ライブ表操作 | 本文中の表操作は、ドラッグ開始・移動先追従・移動確定が安定して行えるようにする | 表の行列ハンドルのドラッグ開始処理を正規化し、pointer と mouse の二重発火でドラッグ状態が壊れないようにした。ドラッグ中の document move / up も既定動作を抑え、本文選択との干渉を減らした。ライブビュー装飾が編集後にも更新されることをテストで確認した。太字・斜体はMarkdown記号を隠すだけでなく、ライブプレビュー用の `strong` / `em` DOMへ置き換え、太字は横幅が痩せて見えないように内側余白と影で厚みを持たせ、斜体は日本語でも分かる傾きへ強めた | `pnpm exec tsc --noEmit` 通過。`pnpm test -- Editor.test.tsx editorStore.test.ts` 通過（31ファイル / 269テスト） | なし |
| [x] 実装済み / 自動確認済み | 分割表示のタブと同期 | 分割表示では左右ペインそれぞれのタブ列に開いている対象が表示され、同じファイルを左右で開いた場合は編集内容が同期される | 既存タブを別ペインで開いたとき、そのペインのタブ列にも追加するようにした。同じタブを左右で表示している場合、片側編集で更新された `content` をもう片側のCodeMirror文書へ同期するようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- Editor.test.tsx editorStore.test.ts` 通過（31ファイル / 269テスト） | なし |
| [x] 実装済み / 自動確認済み | ファイルツリーの右クリックメニュー | ファイル・フォルダの右クリックメニューは、サイドバー幅やスクロール領域に切られず、操作対象の近くに独立したメニューとして表示する | ファイルツリーの右クリックメニューを `document.body` へポータル表示し、画面基準の `fixed` 位置にした。画面端では内側へ収め、サイドバー内の `transform` や `overflow` による切れを避けるようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 270テスト） | なし |
| [x] 実装済み / 自動確認済み | 本文幅と行番号 | 本文は設定した最大幅の内側で折り返し、フォーム化したフロントマターでもMDファイル上の行として同じ行番号列に表示する。ただし、フロントマターの表示で行高を広げない | CodeMirror に行折り返しを有効化し、本文行へ `pre-wrap` / `overflow-wrap` を指定した。フロントマター全体を1ブロック置換せず、各MD行を残したまま行内表示としてプロパティ化した。フォーム部品は通常行高に収め、フロントマター部分が縦に広がらないようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- Editor.test.tsx` 通過（31ファイル / 272テスト） | なし |
| [x] 実装済み / 自動確認済み | 検索結果・Git関連リスト・リンク一覧 | 結果が更新されたこと、項目を選べることが分かる反応にする | 結果として短く入ってくる反応と、項目が軽く受け止める反応を付けた。検索中、バックリンク読み込み中、Git操作中に小さな読み込み反応を表示するようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx Toolbar.test.tsx` 通過（31ファイル / 261テスト） | なし |
| [x] 実装済み / 自動確認済み | 本文中リンクの遷移 | 本文中のMarkdownリンク・Wikiリンクは、見た目だけでなく書いている流れの中でそのまま移動できるようにする | エディタ本文のライブリンククリックを、現在ファイル基準の相対パス・見出し付きリンク・Wikiリンクとして解決し、既存ファイルを開くようにした。未作成の内部リンクは既存のリンク作成導線へ接続した。ライブ表示でリンク記法が短い表示部品に置き換わっている場合も、pointerdown / mousedown の段階で編集状態への移行を止め、その表示部品自身のクリックで開くようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test` 通過（31ファイル / 287テスト） | 実アプリで本文リンククリックの手触りを必要に応じて確認する |
| [x] 実装済み / 自動確認済み | 左レールのワークスペース名変更 | ワークスペース名変更は表示名だけの別名ではなく、ユーザーが実体として認識しているフォルダ名変更として扱う。操作はタブ名変更と同じくダブルクリックを基本にし、追加ボタンは置かない | 左レールのワークスペース名表示をダブルクリック、または右クリックメニューから名前変更できるようにした。変更時は実フォルダをリネームし、Relicの登録パス・ワークスペースID・ワークスペース別設定の保存先も新しい名前へ追従するようにした | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx workspaceService.test.ts` 通過（31ファイル / 289テスト） | 実アプリで左レールのダブルクリック・右クリックメニューの手触りを必要に応じて確認する |
| [x] 実装済み / 自動確認済み | モーション抑制設定 | OS側でモーションを抑える設定の場合は、遷移時間を最小化する | タブ、ボタン、ファイルツリー、モーダル、トースト、パネル切り替え、ツールバー、入力変形、プロパティフォーム、結果リスト系、読み込み中表示で `prefers-reduced-motion` に対応した | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx Toolbar.test.tsx` 通過（31ファイル / 261テスト） | なし |
| [x] 実装済み / 自動確認済み | 左レールのワークスペース表示 | 左レール内のワークスペース表示は、通常のレールアイコンと同じ幅・芯で揃える | ワークスペーススイッチャーのアイコンサイズ、内部グリッド、padding、高さを通常レールボタンのアイコン幅に合わせた | `pnpm exec tsc --noEmit` 通過。`pnpm test -- App.test.tsx` 通過（31ファイル / 265テスト） | なし |

---

## 最初に読む

- `../ui/DESIGN.md`
- `../ui/screens-macos.md`
- `../ui/navigation.md`

必要になったときだけ読む:

- 挙動の正解を確認する: `../spec/`
- 実装規約とテスト方針を確認する: `conventions.md`, `testing.md`
- 過去の経緯を確認する: `../journal/` の該当日

---

## 作業ルール

- このフェーズは大きな計画を先に固めるのではなく、ユーザーが気になった箇所を都度、少しずつ直していく。
- ユーザー判断で実装したUI/UX変更は、この文書の「UI/UX判断と実施」に方向性・実施・確認・残りをセットで記録する。日誌だけに閉じ込めない。
- 小さなUI変更ごとに、対象・意図・確認方法を短く扱う。
- ユーザーが「やりたい」「こうしたい」と言っただけで、仕様・フェーズ・チェックリスト・デザイン方針を作らない。
- AIが勝手に作った方針やチェックリストをユーザーが確認すること自体が無駄になるため、確認させる前に作らない。
- まずユーザーへヒアリングし、何が嫌か、何を変えたいか、どの程度変えてよいかを確認する。
- ヒアリングの結果、ユーザーが「作ってよい」と明示した場合だけ、方針・チェックリスト・文書・実装案を作る。
- UI/UX方針は、提案として出すことはできる。ただし正本化・実装はユーザーが明示的に許可した範囲だけ行う。
- デザイン原則の変更は `../ui/DESIGN.md` に関わるため、ユーザーが明示した場合だけ編集する。
- 実装済みの変更は戻さない。戻す場合はユーザーが明示的に指示したときだけ行う。

## 最初に確認すること

このフェーズでは、大きな変更に入る前だけ次を1問ずつ確認する。
小さな違和感修正は、ユーザーが具体的に対象と方向を示した場合、その範囲で都度直す。

- いま一番嫌な画面・状態はどこか
- どんな印象にしたいか
- 変えてよい範囲はどこまでか
- 先にデザイン原則を直すか、画面単位で直すか
- 実アプリ目視確認を行うか、コード上の確認に留めるか

---

## 完了条件

- ユーザーがUI/UX方針に納得している
- 方針が `../ui/DESIGN.md` または現在フェーズ正本と矛盾していない
- 実装した場合は型チェック・自動テストが通っている
- 実アプリ目視確認が必要な項目は、確認済みまたは未確認理由が明記されている
