# P17-feature-reconsideration.md

Relicの機能再考フェーズの正本。

このフェーズでは、既存機能・未実装機能・今後入れたい機能を、ユーザーへのヒアリングを起点に見直す。
AIが先に機能一覧、削除案、追加案、再設計案を作らない。

---

## フェーズの目的

Relicを「書く道具」として使い続けやすくするために、機能の必要性、重さ、導線、優先順位を再考する。

特に、以下を重視する。

- ユーザー本人が不要・重い・分かりにくいと感じている機能を起点にする
- 機能追加よりも、機能の整理、削減、統合、導線の見直しを同等以上に重視する
- 仕様変更・実装変更は、対象機能と方向性が合意されてから行う
- 機能判断は `../principles.md` と `../PLAN.md` に反しない範囲で行う

---

## 実装項目

このフェーズで実装した項目は、対象ごとの実装チェックリストとして記録する。

### 画面タブ名と言語切替・フロントマター表示崩れ 実装チェックリスト

- [x] 設定・フロントマターなどの画面タブ名を、保存済み `name` ではなく `panel` 種別と現在の翻訳から表示する
- [x] 画面タブ名を言語変更に追従させる
- [x] フロントマターのプロパティ行とラッパーの固定行高を外す
- [x] 複雑値用 textarea の高さをレイアウトに反映し、他の行や元テキストと重ならないようにする
- [x] 言語変更後に画面タブ名が更新される回帰テストを追加する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- --run App.test.tsx Editor.test.tsx` を通す（全32ファイル / 314テスト）
- [ ] 実アプリで目視確認する

### 左右パネル同時表示 実装チェックリスト

- [x] サイドバーを開いた状態で右パネルを開いても、どちらかのパネルが隠れないようにする
- [x] アプリ最小幅の固定を外す
- [x] ワークスペース全体とエディタ内レイアウトを、グリッドの `auto` 列から flex に変更する
- [x] レール、左サイドバー、右パネルを固定幅の flex item として扱う
- [x] メインエリアと中央エディタ領域を、残り幅を取って縮む flex item として扱う
- [x] パネルタブ本文とフロントマター設定フォームを、ペイン幅を基準に縮むようにする
- [x] 狭い時は追加フォームを1列に落とす
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- App.test.tsx SettingsSidebar.test.tsx` を通す（32ファイル / 313テスト）
- [ ] 実アプリで目視確認する

### エイリアスの特別サポート 実装チェックリスト

- [x] フロントマター全体ではなく、リンク概念に直接影響する `aliases` だけを固定プロパティとして特別サポートする
- [x] ワークスペース内Markdownの `aliases` を読み込むエイリアス索引を追加する
- [x] Wikiリンク解決、リンククリック、発リンク一覧、バックリンク、ファイル名検索、クイックスイッチャーでエイリアスを参照する
- [x] 既存ファイル名が存在する場合は既存パスを優先し、存在しないリンクだけエイリアスで解決する
- [x] エディタ内の `aliases` を固定のリスト型として扱う
- [x] フロントマター設定画面で `aliases` を固定プロパティとして表示する
- [x] カスタムプロパティに `aliases` を追加できないようにする
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- aliases.test.ts links.test.ts backlinks.test.ts search.test.ts SettingsSidebar.test.tsx Editor.test.tsx App.test.tsx` を通す（32ファイル / 313テスト）
- [ ] 実アプリで目視確認する
- [ ] 他のフロントマタープロパティを固定扱いにするか判断する

### フロントマター設定導線 実装チェックリスト

- [x] レールにフロントマター表示ボタンを追加する
- [x] フロントマター専用サイドバーを追加する
- [x] カスタムフィールド設定を通常設定画面から専用画面へ移す
- [x] フロントマターテンプレート設定を専用画面から外す
- [x] フロントマター機能トグルが無効のときはレールボタンを表示しない
- [x] レールボタンから専用画面を開けることを自動テストで確認する
- [x] 既存のフィールド編集テストを専用画面対象に更新する
- [x] 現在ファイルへのフィールド追加・削除は専用画面では扱わない
- [x] 候補値管理はプロパティ設定の一部として、候補チップの追加・削除で扱う
- [x] YAML確認・編集への切り替えは専用画面では扱わない
- [x] 入力タイプ表示を内部名ではなく普通の名前にする
- [x] 候補入力をカンマ区切り欄からチップ編集へ変更する
- [x] 主要な入力タイプ（テキスト、数値、日付、日時、時刻、オン/オフ、選択肢、複数候補、URL）を選べるようにする
- [x] YAMLはフロントマター全体の記述形式なので、フィールドの入力タイプから削除する
- [x] `tags` はカスタム入力タイプではなく固定プロパティとして扱う
- [x] カスタム入力タイプから長文、メール、リスト、タグを外す

### フロントマター設定画面 実装チェックリスト

- [x] フロントマター専用画面を、現在ファイル操作ではなくプロパティ設定だけに絞る
- [x] フロントマター専用画面からテンプレート管理を削除する
- [x] 入力タイプ表示を日本語・英語とも通常名に変更する
- [x] 候補をカンマ区切りではなく、チップ表示と追加入力・削除ボタンで管理する
- [x] フィールド一覧をカード状にする
- [x] 展開したフィールドで名前・入力タイプ・候補を編集できるようにする
- [x] 画面幅を抑え、追加欄を横並びにする
- [x] フィールドの種類をピル表示にする
- [x] 詳細編集部をラベル列付きの密度へ調整する
- [x] 入力タイプにテキスト、数値、日付、日時、時刻、オン/オフ、選択肢、複数候補、URLを含める
- [x] YAML入力タイプを削除する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- SettingsSidebar.test.tsx Editor.test.tsx App.test.tsx` を通す（31ファイル / 306テスト）
- [ ] 実アプリで目視確認する

### フロントマター固定プロパティとカスタム型整理 実装チェックリスト

- [x] `tags` を `aliases` と同じ固定プロパティとして扱う
- [x] タグとして扱う値をフロントマター `tags` のみにする
- [x] 本文中の `#タグ` 記法をタグ化しない
- [x] 本文中の関連づけをインラインリンク `[[...]]` に寄せる
- [x] カスタムプロパティ型をテキスト、数値、日付、日時、時刻、オン/オフ、選択肢、複数候補、URLに絞る
- [x] `tags` をカスタムプロパティ名として追加できないようにする
- [x] タグ集計・タグ検索・マージツールのタグ絞り込みをフロントマター `tags` のみに寄せる
- [x] プレビューの本文 `#タグ` クリック検索を外す
- [x] 仕様・設計・検索・Markdown関連文書を同方針へ更新する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- tags.test.ts search.test.ts SettingsSidebar.test.tsx Editor.test.tsx Preview.test.tsx App.test.tsx` を通す（32ファイル / 313テスト）
- [ ] 実アプリで目視確認する

### フロントマター設定の説明表示 実装チェックリスト

- [x] 固定プロパティカードに `aliases` と `tags` の用途説明を表示する
- [x] 新規追加欄に、選択中の入力タイプの説明を表示する
- [x] 既存プロパティ編集欄に、選択中の入力タイプの説明を表示する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- SettingsSidebar.test.tsx App.test.tsx` を通す（32ファイル / 313テスト）
- [ ] 実アプリで目視確認する

### フロントマター設定の書き方説明と1行配列整理 実装チェックリスト

- [x] 固定プロパティカードに、Markdown上での書き方例を表示する
- [x] `aliases`、`tags`、`chronicle` の単体・複数/期間の例を、すべて1行配列で表示する
- [x] カスタムプロパティにも、入力タイプごとのMarkdown上の書き方例を `[]` 付きの1行配列で表示する
- [x] `aliases`、`tags`、複数候補プロパティを、フォーム編集後は1行配列として書き戻す
- [x] 登録済みカスタムプロパティも、フォーム編集後は `[]` 付きの1行配列として書き戻す
- [x] `---` がどこか分かるように、Markdown先頭の設定欄全体の例を表示する
- [x] 既存カスタムプロパティ名の編集を、1文字ごとの即保存ではなく確定時保存に変更する
- [x] `docs/ui/DESIGN.md` を確認し、有彩色の線・グラデーションを使わずグレースケールでメリハリを付ける
- [x] `docs/spec/frontmatter.md` に固定プロパティの1行配列方針とカスタムプロパティの書き方例を追記する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- SettingsSidebar.test.tsx` を通す（全33ファイル / 320テスト）
- [x] `pnpm test -- Editor.test.tsx SettingsSidebar.test.tsx` を通す（全33ファイル / 322テスト）
- [x] `pnpm test -- SettingsSidebar.test.tsx` を通す（全33ファイル / 323テスト）
- [ ] 実アプリで目視確認する

### レール画面タブ化 実装チェックリスト

- [x] ファイルタブと画面タブをタブ状態で区別する
- [x] レール上のファイル・検索以外を、サイドバーではなくタブとして開く
- [x] Git・ツール・フロントマター・設定を画面タブとして表示する
- [x] 画面タブを開くとき、レールからタブ列へ飛ぶアニメーションを表示する
- [x] アクティブな画面タブのレールボタンをもう一度押すとタブを閉じる
- [x] 別の画面タブがアクティブでも、開いている画面タブのレールボタンを押すと閉じる
- [x] 画面タブを閉じるとき、タブ列からレールへ吸い込まれるアニメーションを表示する
- [x] 画面タブでは自動保存・リネーム・移動・削除などのファイル操作が走らないようにする
- [x] アクティブタブの上線アクセント装飾を使わない
- [x] 既存のGit/設定系テストが画面タブ表示でも通るように更新する
- [x] 型検査で画面タブとファイルタブの取り違えがないことを確認する
- [ ] 実アプリで飛ぶアニメーションの見え方と距離感を確認する
- [ ] 画面タブ内の余白・横幅・スクロールの視認性を実アプリで調整する

### 上部操作ボタンのアクティブ表示 実装チェックリスト

- [x] 分割・アウトライン・リンクの上部操作ボタンで、青い上線アクセント装飾を使わない
- [x] 上部操作ボタンのアクティブ状態だけ `box-shadow` を無効化する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `npm test -- --run App.test.tsx` を通す（1ファイル / 69テスト）
- [ ] 実アプリで目視確認する

### サイドバーのファイル開閉 実装チェックリスト

- [x] ファイル行クリック時の座標を渡せるようにする
- [x] 未表示ファイルは、ファイル行からタブ列へ飛ぶ表示を出して開く
- [x] 対象ペイン内で開いている同一ファイルは、再クリックで閉じずにアクティブにする
- [x] 開いているファイル行自体には色を付けず、選択状態にだけ色を付ける
- [x] ファイルタブを閉じるときは、タブからサイドバーのファイル行へ吸い込む表示を出す
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `npm test -- --run App.test.tsx` を通す（1ファイル / 71テスト）
- [ ] 実アプリで目視確認する

### ファイルツリー表示とタブ移動 実装チェックリスト

- [x] 開いているファイル行へ `open` 表示を追加する
- [x] 複数選択時はサイドバー見出し横に件数表示を出す
- [x] 複数選択行に薄い点線アウトラインを出す
- [x] 複数選択中の通常クリックは単一選択へ戻すだけにし、ファイルを開かない
- [x] ファイル読み込み完了前の再クリックは開く操作を取り消す
- [x] タブ移動用のストア操作を追加する
- [x] タブ列ドラッグ&ドロップで同一ペイン内の並べ替えに対応する
- [x] タブ列ドラッグ&ドロップで左右ペイン間の移動に対応する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `npm test -- --run editorStore.test.ts App.test.tsx` を通す（2ファイル / 82テスト）
- [x] `npm test -- --run App.test.tsx` を通す（1ファイル / 70テスト）
- [ ] 実アプリで目視確認する

### 新規ファイル・フォルダ作成 実装チェックリスト

- [x] 新規ファイル作成を、押した場所からタブ列へ飛ぶ表示を出してから作成・オープンするようにする
- [x] フォルダ作成を、押した場所からファイルツリーへ入る表示を出すようにする
- [x] 既存の作成後のファイルツリー出現反応を維持する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `npm test -- --run App.test.tsx` を通す（1ファイル / 71テスト）
- [ ] 実アプリで目視確認する

### `chronicle` 固定プロパティとチャートモード 実装チェックリスト

- [x] `chronicle` をフロントマターの固定プロパティとして扱う
- [x] `chronicle: [1185]` の単年を読み取る
- [x] `chronicle: [1185, 1333]` の期間を読み取る
- [x] `0` 年を無効にする
- [x] 紀元前を負数、紀元後を正数として扱う
- [x] エディタ内で `chronicle` の年入力を扱えるようにする
- [x] レールにチャートモードを追加する
- [x] チャートモードを画面タブとして開けるようにする
- [x] レール上のチャートモード表示名を、日本語は「チャート」、英語は「Charts」に変更する
- [x] 年表でファイル名を左固定列に表示する
- [x] 年表の右側グリッドに単年または期間を塗りとして表示する
- [x] 塗りの上に年または期間を表示する
- [x] 年表タブ内で表示スケールを縮小・拡大できるようにする
- [x] 年表項目クリックでファイルを開く
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- chronicle.test.ts SettingsSidebar.test.tsx Editor.test.tsx App.test.tsx` を通す（全33ファイル / 320テスト）
- [ ] 実アプリで目視確認する

### `date` 固定プロパティ 実装チェックリスト

- [x] フィールド名は汎用性と分かりやすさを優先し、`date` を採用する
- [x] `date` をフロントマターの固定プロパティとして扱う
- [x] カスタムプロパティに `date` を追加できないようにする
- [x] フロントマター設定画面に `date` の説明と単日・期間の書き方例を表示する
- [x] エディタ内で `date` の年月日入力を扱えるようにする
- [x] `date: [2026-05-12]` の単日を書き戻せるようにする
- [x] `date: [2026-05-12, 2026-05-20]` の期間を書き戻せるようにする
- [x] `docs/spec/frontmatter.md` に `date` 固定プロパティ方針を追記する
- [x] `pnpm exec tsc --noEmit` を通す
- [x] `pnpm test -- Editor.test.tsx SettingsSidebar.test.tsx` を通す（全33ファイル / 313テスト）
- [ ] 実アプリで目視確認する

### 年表の複数ガントチャート化 実装チェックリスト

- 方向性: 年表をファイルに近いリソースとして扱い、年表サイドバーで切り替えた年表をタブとして開けるようにする
- 方向性: 年表の数は `chronicle` と `date` の2個に固定する
- 実施: ワークスペース設定に `chronicle` / `date` の固定ガントチャート定義と表示対象ファイルを保存するようにした
- 実施: `chronicle` 連携は従来どおり年・期間を読み取り、`date` 連携は `date: [YYYY-MM-DD]` と `date: [YYYY-MM-DD, YYYY-MM-DD]` を読み取るようにした
- 実施: 年表サイドバーで `chronicle` / `date` をトグルで切り替え、該当プロパティを持つファイルを一覧表示するようにした
- 実施: 年表サイドバーのファイルを年表タブへドラッグ&ドロップすると、その年表に表示対象として追加されるようにした
- 実施: 年表タブのファイル名横に削除ボタンを付け、表示対象から外せるようにした
- 実施: 年表サイドバーで選んだ年表を、ファイルと同じタブ列の年表タブとして開くようにした
- 実施: レールアイコンをサイドバー表示系（ファイル・検索・チャート）とタブ表示系（Git・ツール・フロントマター・設定）に分け、間に区切り線を入れた
- 実施: レール上のモード名は、複数の時間軸チャートを扱う入口として「チャート」/ `Charts` に変更した
- 実施: 開発中に旧形式の年表データが返っても画面が落ちないように、renderer側で互換変換するようにした
- 確認: `pnpm exec tsc --noEmit` 通過
- 確認: `pnpm test -- App.test.tsx chronicle.test.ts workspaceSettings.test.ts editorStore.test.ts` 通過（全33ファイル / 317テスト）
- 残り: 実アプリでチャートトグル、ファイルのドラッグ追加、チャート上の削除、チャートタブの開閉・並べ替え、`date` 連携の横軸を目視確認する

### Git機能のブランチ・タグ削減 実装チェックリスト

- 方向性: Git機能をスリムにし、ユーザーが触る機能からブランチ管理とGitタグ管理を外す
- 実施: Git画面からブランチ作成・切り替え・未コミット時の切り替え確認・Gitタグ作成/削除/送信を削除した
- 実施: コマンドパレットからブランチ切り替えコマンドを削除した
- 実施: renderer / preload / IPC の公開APIからブランチ管理とGitタグ管理の入口を削除した
- 実施: Git内部公開口からブランチ管理・Gitタグ管理を外し、専用実装ファイルと入力検証を削除した
- 実施: Git機能の自動テストからブランチ管理・Gitタグ管理の操作テストを削除した
- 確認: `pnpm exec tsc --noEmit` 通過
- 確認: `pnpm test -- --run App.test.tsx git.test.ts gitValidation.test.ts ToolsSidebar.test.tsx` 通過（全33ファイル / 311テスト）
- 残り: 実アプリでGit画面にブランチ管理・Gitタグ管理が表示されないことを目視確認する

---

## 最初に読む

- `../principles.md`
- `../PLAN.md`

必要になったときだけ読む:

- 既存仕様を確認する: 対応する `../spec/*.md`
- UIへの影響を確認する: `../ui/screens-macos.md`, `../ui/navigation.md`, `../ui/DESIGN.md`
- 実装規約とテスト方針を確認する: `conventions.md`, `testing.md`

---

## 作業ルール

- まず、ユーザーが再考したい機能・違和感・削りたい機能・重いと感じている導線を確認する。
- AIが先に包括的な機能棚卸しや新チェックリストを作らない。
- 機能追加・削除・仕様変更・UI変更は、対象と方向性が合意されてから行う。
- 既存機能を削る場合は、影響範囲、代替手段、戻しやすさを確認する。
- 実装した場合は型チェック・自動テストで確認する。

---

## 完了条件

- ユーザーが再考したい主要機能について、残す・削る・後回し・再設計の判断ができている
- 判断結果が正本文書または該当仕様と矛盾していない
- 実装変更を行った場合は型チェック・自動テストが通っている
