# P17-feature-reconsideration.md

Relicの機能再考フェーズの正本。

このフェーズでは、既存機能・未実装機能・今後入れたい機能を、ユーザーへのヒアリングを起点に見直す。
AIが先に機能一覧、削除案、追加案、再設計案を作らない。

---

## フェーズの目的

Relicを「書く道具」として使い続けやすくするために、機能の必要性、重さ、導線、優先順位を再考する。

特に、以下を重視する。

- ユーザー本人が不要・重い・分かりにくいと感じている機能を起点にする
- 機能追加よりも、機能の整理、削減、統合、導線の見直しを同等以上に重視する
- 仕様変更・実装変更は、対象機能と方向性が合意されてから行う
- 機能判断は `../principles.md` と `../PLAN.md` に反しない範囲で行う

---

## 機能再考ログ

このフェーズでは、対象機能ごとに以下をセットで記録する。

- 方向性
- 実施
- 確認
- 残り

未実施・未確認の場合も省略しない。

| 状態 | 対象 | 方向性 | 実施 | 確認 | 残り |
|------|------|--------|------|------|------|
| [x] 実装済み / 自動確認済み | フロントマター設定導線 | 現状のフロントマターは書けるが分かりにくいため、フロントマターのフィールド・テンプレート設定を通常設定から分け、レールから開ける専用画面にする | レールにフロントマター表示ボタンを追加し、フロントマター専用サイドバーへカスタムフィールドとテンプレート設定を移した。通常設定画面から同設定は外した | `pnpm exec tsc --noEmit` 通過。`npm run test -- --run App.test.tsx SettingsSidebar.test.tsx` 通過（2ファイル / 71テスト） | 専用画面内で現在ファイルへのフィールド追加・削除、候補管理、YAML確認などをどう扱うかは次回以降に検討する |
| [x] 実装済み / 自動確認済み | レール画面タブ化 | 設定系画面の操作性と視認性を確保するため、レール上のファイル・検索以外はサイドバーではなくタブとして開く。突然タブが現れる違和感を減らすため、押した地点からタブ列へ飛ぶ挙動を付ける。画面タブはファイルと区別できるよう、タブ名の前にレールと同じアイコンを表示する | ファイルタブと画面タブを分け、Git・ツール・フロントマター・設定を画面タブとして開くようにした。画面タブでは自動保存やファイル操作が走らないようにした。レールクリック時にタブ列へ飛ぶアニメーションを追加した。画面タブ名の前にレール上と同じアイコンを表示した | `pnpm exec tsc --noEmit` 通過。`npm test` 通過（31ファイル / 298テスト）。追加で `pnpm exec tsc --noEmit` 通過。`npm test -- --run App.test.tsx` 通過（1ファイル / 71テスト） | 実アプリで飛ぶアニメーションの見え方、画面タブ内の余白・横幅・スクロールを確認して調整する |
| [x] 実装済み / 自動確認済み | 上部操作ボタンのアクティブ表示 | 分割・アウトライン・リンクの上部操作ボタンでは、青い上線アクセント装飾を使わない | 上部操作ボタンのアクティブ状態だけ `box-shadow` を無効化し、青い上線を出さないようにした | `pnpm exec tsc --noEmit` 通過。`npm test -- --run App.test.tsx` 通過（1ファイル / 69テスト） | 実アプリ目視確認は未実施 |
| [x] 実装済み / 自動確認済み | サイドバーのファイル開閉 | サイドバーのファイルも、レールの画面タブと同じく、押した場所からタブ列へ飛んで開き、同じファイルの再クリックでタブ列から戻って閉じる挙動にする。対象ペイン内で開いている同一ファイルは、アクティブでなくても再クリックで閉じる | ファイル行クリック時の座標を渡し、未表示ファイルは行からタブ列へ飛ぶ表示を出して開くようにした。対象ペイン内で開いている同一ファイルを再クリックした場合は、アクティブでなくてもタブ列からファイル行へ戻る表示を出してから閉じるようにした | `pnpm exec tsc --noEmit` 通過。`npm test -- --run App.test.tsx` 通過（1ファイル / 69テスト）。追加で `pnpm exec tsc --noEmit` 通過。`npm test -- --run App.test.tsx` 通過（1ファイル / 71テスト） | 実アプリ目視確認は未実施 |
| [x] 実装済み / 自動確認済み | ファイルツリー表示とタブ移動 | 開いているファイルはレールボタンのアクティブ表示と同じ色で示す。複数選択はサイドバー見出し横の件数表示と薄い点線アウトラインで示し、複数選択中の通常クリックではファイルを開かない。タブはドラッグで並べ替えでき、分割表示中は左右ペイン間も移動できるようにする | 開いているファイル行へ `open` 表示を追加した。複数選択時は見出し横に件数表示を出し、選択行に点線アウトラインを出すようにした。複数選択中の通常クリックは単一選択へ戻すだけでファイルを開かないようにした。ファイル読み込み完了前の再クリックは開く操作を取り消すようにした。タブ移動用のストア操作とタブ列ドラッグ&ドロップを追加し、同一ペイン内の並べ替えと左右ペイン間の移動に対応した | `pnpm exec tsc --noEmit` 通過。`npm test -- --run editorStore.test.ts App.test.tsx` 通過（2ファイル / 82テスト）。追加で `npm test -- --run App.test.tsx` 通過（1ファイル / 70テスト） | 実アプリ目視確認は未実施 |
| [x] 実装済み / 自動確認済み | 新規ファイル・フォルダ作成 | 新規ファイル作成とフォルダ作成も、押した場所から結果につながる反応に揃える。新規ファイルはタブへ、フォルダはファイルツリーへ向かう反応にする | サイドバーの新規ファイルボタンは押した場所からタブ列へ飛ぶ表示を出してから作成・オープンするようにした。フォルダ作成ボタンは押した場所からファイルツリーへ入る表示を出すようにした。既存の作成後のファイルツリー出現反応は維持した | `pnpm exec tsc --noEmit` 通過。`npm test -- --run App.test.tsx` 通過（1ファイル / 71テスト） | 実アプリ目視確認は未実施 |

### フロントマター設定導線 実装チェックリスト

- [x] レールにフロントマター表示ボタンを追加する
- [x] フロントマター専用サイドバーを追加する
- [x] カスタムフィールド設定を通常設定画面から専用画面へ移す
- [x] フロントマターテンプレート設定を通常設定画面から専用画面へ移す
- [x] フロントマター機能トグルが無効のときはレールボタンを表示しない
- [x] レールボタンから専用画面を開けることを自動テストで確認する
- [x] 既存のフィールド・テンプレート編集テストを専用画面対象に更新する
- [ ] 現在ファイルへのフィールド追加・削除を専用画面でどう扱うか決める
- [ ] 候補値管理を専用画面でどう扱うか決める
- [ ] YAML確認・編集への切り替えを専用画面でどう扱うか決める

### レール画面タブ化 実装チェックリスト

- [x] ファイルタブと画面タブをタブ状態で区別する
- [x] レール上のファイル・検索以外を、サイドバーではなくタブとして開く
- [x] Git・ツール・フロントマター・設定を画面タブとして表示する
- [x] 画面タブを開くとき、レールからタブ列へ飛ぶアニメーションを表示する
- [x] アクティブな画面タブのレールボタンをもう一度押すとタブを閉じる
- [x] 別の画面タブがアクティブでも、開いている画面タブのレールボタンを押すと閉じる
- [x] 画面タブを閉じるとき、タブ列からレールへ吸い込まれるアニメーションを表示する
- [x] 画面タブでは自動保存・リネーム・移動・削除などのファイル操作が走らないようにする
- [x] アクティブタブの上線アクセント装飾を使わない
- [x] 既存のGit/設定系テストが画面タブ表示でも通るように更新する
- [x] 型検査で画面タブとファイルタブの取り違えがないことを確認する
- [ ] 実アプリで飛ぶアニメーションの見え方と距離感を確認する
- [ ] 画面タブ内の余白・横幅・スクロールの視認性を実アプリで調整する

---

## 最初に読む

- `../principles.md`
- `../PLAN.md`

必要になったときだけ読む:

- 既存仕様を確認する: 対応する `../spec/*.md`
- UIへの影響を確認する: `../ui/screens-macos.md`, `../ui/navigation.md`, `../ui/DESIGN.md`
- 実装規約とテスト方針を確認する: `conventions.md`, `testing.md`

---

## 作業ルール

- まず、ユーザーが再考したい機能・違和感・削りたい機能・重いと感じている導線を確認する。
- AIが先に包括的な機能棚卸しや新チェックリストを作らない。
- 機能追加・削除・仕様変更・UI変更は、対象と方向性が合意されてから行う。
- 既存機能を削る場合は、影響範囲、代替手段、戻しやすさを確認する。
- 実装した場合は型チェック・自動テストで確認する。

---

## 完了条件

- ユーザーが再考したい主要機能について、残す・削る・後回し・再設計の判断ができている
- 判断結果が正本文書または該当仕様と矛盾していない
- 実装変更を行った場合は型チェック・自動テストが通っている
