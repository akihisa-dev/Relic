#!/bin/sh
set -eu

hook_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repository_root=$(git rev-parse --show-toplevel)

"$hook_dir/secret-guard.sh" --staged
node "$repository_root/app/scripts/version-policy.mjs" check-staged
