# Skill監査レポート雛形

監査結果は次の順で提示する。該当なしの節も省略しない。

## 監査範囲と制約

- 対象リポジトリとHEAD
- 監査日時
- 利用可能なSkillカタログと探索root
- repository-owned / externalの判定基準、同名entryの統合・variant件数
- 確認した規則・文書
- 取得できなかったSkill、履歴、review、外部情報
- 作業ツリーの既存差分
- 依頼モード: 監査のみ / 監査と適用 / 候補指定の適用
- 許可された変更と、許可されていない高影響操作
- 保証水準: 独立二役 / 独立passによる代替 / 単独監査
- routing証拠: `current` / `equivalent` / `stale` / `unverified` の件数
- 静的レビュー、形式検査、実行観測を分けた件数

## 1. 現在のSkill一覧

| 名前 | scope / variant / 所在地 | 目的 | 発火条件 | 主な責務 | 状態 | 問題点 | 関連Skill |
|---|---|---|---|---|---|---|---|
| `<name>` | repository-owned / external、variant、全所在地 | ... | ... | ... | `healthy` / `attention` / `stale` / `unverified` | なし、または問題ID | ... |

### 要確認・変更候補の収集台帳

全件の詳細を反復せず、状態が `attention`、`stale`、`unverified` のSkillと変更候補だけについて次を示す。全件の機械台帳は添付またはコマンド結果として参照する。

#### `<name>`

- カタログ名 / frontmatter name / folder名:
- scope / variant / カタログ所在地:
- description:
- 入力:
- 出力:
- 使用ツール:
- 参照ファイル:
- 実行スクリプト:
- 他Skillとの関係:
- 根拠:

## 2. 問題一覧

| ID | 重要度 | 対象Skill | 問題の種類 | 根拠 | 影響 | 推奨対応 |
|---|---|---|---|---|---|---|
| `P-001` | `critical` / `high` / `medium` / `low` | ... | ... | ファイル、行、実行結果、コミット | ... | ... |

問題がない場合は「確認済み範囲では該当なし」と書き、未確認を問題なしへ含めない。

### External informational findings

| カタログ名 / variant | 所在地 | finding | 監査への影響 |
|---|---|---|---|
| ... | ... | frontmatter拡張、外部相対参照、内容差など | repository-ownedの終了判定へ含めない。責務比較や未確認事項として扱う |

該当がなくても「該当なし」と書く。外部情報だけを根拠にRelic所有Skillの構造問題を確定しない。

## 3. 変更候補

| ID | 対象Skill | 変更種別 | 推奨変更 | 残す根拠・反対証拠 | 何もしない案・最小修正 | 失われ得る知識 | ゲート | 根拠 |
|---|---|---|---|---|---|---|---|---|
| `C-001` | ... | `keep` / `revise` / `rename` / `merge` / `split` / `deprecate` / `delete` / `create` / `hold` | ... | ... | ... | ... | 充足 / 未充足 | ファイル、実行、コミット |

## 4. 独立二役の照合

### メタ監査官

- 確認済み所見:
- 反証を試した結果:
- 現状維持を支持する根拠:

### 監査対応者

- 最小修正:
- 強い修正:
- 喪失・回帰リスク:
- 検証方法:

### 不一致

| 対象 | 監査官 | 対応者 | 統合判断 | `hold`の要否 |
|---|---|---|---|---|
| ... | ... | ... | ... | ... |

## 5. ルーティング評価

冒頭に形式検査、静的レビュー、実行成功、実行失敗、未実行を示し、実行証拠は `current` / `equivalent` / `stale` / `unverified` に分ける。

| 依頼文 | 期待されるSkill | 実際に選ばれそうなSkill | 誤発火候補 | 未発火の可能性 | description改善案 | 判定根拠 |
|---|---|---|---|---|---|---|
| 「...」 | ... | ... | ... | 高 / 中 / 低 | ... | ... |

## 6. 最終提案

- そのまま維持するSkill:
- 修正するSkill:
- 統合するSkill:
- 分割するSkill:
- 廃止候補:
- 新規作成候補:
- 判断を保留するSkill:

各分類にSkill名、変更候補ID、主要根拠を付ける。分類間で同じ変更を重複させない。

## 自己監査

- 対象: 監査を実行した整備用Skill
- メタ監査官:
- 監査対応者:
- 不一致:
- 責務の広さ:
- descriptionの明確さ:
- 他Skillとの重複:
- 手順量:
- 現行構成との適合:
- 判定: `keep` / `revise` / `hold`
- 自己監査回数: 1回。再帰実行なし

## 適用後再監査

- 再確認した所見ID:
- 修正者とは別の確認役:
- 解消:
- 未解消:
- 新しく生じた問題:
- 失われた知識・参照:
- 代表caseと証拠鮮度:

## 変更適用の状態

- 監査のみ: 「既存Skillは変更していない」と明記する。
- 監査と適用: 中間レポートの候補ID、適用した差分、検証、許可範囲外または`hold`で未適用の候補を分ける。
- 候補指定の適用: 再照合した候補ID、差分、検証、指定外で未適用の候補を分ける。
