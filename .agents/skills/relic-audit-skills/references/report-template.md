# Skill監査レポート雛形

監査結果は次の順で提示する。該当なしの節も省略しない。

## 監査範囲と制約

- 対象リポジトリとHEAD
- 監査日時
- 利用可能なSkillカタログと探索root
- 確認した規則・文書
- 取得できなかったSkill、履歴、review、外部情報
- 作業ツリーの既存差分

## 1. 現在のSkill一覧

| 名前 | 目的 | 発火条件 | 主な責務 | 状態 | 問題点 | 関連Skill |
|---|---|---|---|---|---|---|
| `<name>` | ... | ... | ... | `healthy` / `attention` / `stale` / `unverified` | なし、または問題ID | ... |

### 収集台帳

各Skillについて次を簡潔に示す。

#### `<name>`

- カタログ名 / frontmatter name / folder名:
- description:
- 入力:
- 出力:
- 使用ツール:
- 参照ファイル:
- 実行スクリプト:
- 他Skillとの関係:
- 根拠:

## 2. 問題一覧

| ID | 重要度 | 対象Skill | 問題の種類 | 根拠 | 影響 | 推奨対応 |
|---|---|---|---|---|---|---|
| `P-001` | `critical` / `high` / `medium` / `low` | ... | ... | ファイル、行、実行結果、コミット | ... | ... |

問題がない場合は「確認済み範囲では該当なし」と書き、未確認を問題なしへ含めない。

## 3. 変更候補

| ID | 対象Skill | 変更種別 | 現在の問題 | 推奨する変更 | 変更理由 | 期待効果 | リスク | 根拠 | 他Skillへの影響 |
|---|---|---|---|---|---|---|---|---|---|
| `C-001` | ... | `keep` / `revise` / `rename` / `merge` / `split` / `deprecate` / `delete` / `create` / `hold` | ... | ... | ... | ... | ... | ファイルまたはコミット | ... |

## 4. ルーティング評価

| 依頼文 | 期待されるSkill | 実際に選ばれそうなSkill | 誤発火候補 | 未発火の可能性 | description改善案 | 判定根拠 |
|---|---|---|---|---|---|---|
| 「...」 | ... | ... | ... | 高 / 中 / 低 | ... | ... |

## 5. 最終提案

- そのまま維持するSkill:
- 修正するSkill:
- 統合するSkill:
- 分割するSkill:
- 廃止候補:
- 新規作成候補:
- 判断を保留するSkill:

各分類にSkill名、変更候補ID、主要根拠を付ける。分類間で同じ変更を重複させない。

## 自己監査

- 対象: 監査を実行した整備用Skill
- 責務の広さ:
- descriptionの明確さ:
- 他Skillとの重複:
- 手順量:
- 現行構成との適合:
- 判定: `keep` / `revise` / `hold`
- 自己監査回数: 1回。再帰実行なし

## 変更適用の状態

- 初回監査: 「既存Skillは変更していない」と明記する。
- 承認後の適用: 実施した候補ID、差分、検証、未適用候補を分ける。
