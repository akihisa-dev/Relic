interface:
  display_name: "Relic UI Change"
  short_description: "UI変更とpointer確定・中断状態を安全に検証する"
  default_prompt: "Use $relic-change-ui to implement and verify the requested Relic UI change, separating pointer completion from cancellation across relevant states and canonical documentation."
