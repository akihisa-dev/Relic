interface:
  display_name: "Relic Frontmatter"
  short_description: "YAML往復とdrop時だけの並べ替えを安全に変更する"
  default_prompt: "Use $relic-change-frontmatter to update Relic frontmatter behavior, including confirmed-drop reordering, while preserving comments, quoting, multiline YAML, and unknown fields."
